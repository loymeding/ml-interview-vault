---
topic: Python — Структуры данных — Словари (Dict)
card_ref: "[[Python/Основы/Python — Структуры данных — Словари (Dict)]]"
domain: python
тип: coding-task
статус: готово
сложность: средняя
generated: 2026-08-31
---

# Coding Tasks — словари

Теория: [[Python/Основы/Python — Структуры данных — Словари (Dict)|карточка по словарям]]

## T001. Первая уникальная метка

Дан список строк labels. Верните первую метку, которая встречается ровно один раз. Если такой нет, верните None.

~~~python
def first_unique_label(labels):
    pass
~~~

Примеры:

~~~text
["cat", "dog", "cat", "fish"] → "dog"
["spam", "spam"] → None
[] → None
~~~

Ожидаемая сложность O(n) по времени и O(n) по памяти. Объясните, почему labels.count внутри цикла даёт O(n²).

## T002. Группировка объектов

Дан список записей с полями type и id. Верните словарь, где ключ — type, а значение — список id.

~~~python
def group_ids(records):
    pass
~~~

Обсудите решение через обычный dict, defaultdict и случай отсутствующего поля.

## T003. Объединение счётчиков

Есть несколько словарей с частотами токенов. Сложите их в один словарь.

~~~python
def merge_counts(counters):
    pass
~~~

Проверьте пустой список, повторяющиеся ключи и отрицательные значения. Сравните ручной цикл, Counter и оператор |, если версии Python это позволяют.
