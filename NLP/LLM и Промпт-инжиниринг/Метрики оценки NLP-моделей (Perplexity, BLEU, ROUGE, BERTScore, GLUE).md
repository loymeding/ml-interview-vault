---
tags: [nlp, llm, метрики, perplexity, bleu, rouge, bertscore, glue, superglue, benchmark-contamination, оценка-качества]
тип: теория и практика
уровень: senior
сложность: высокая
статус: готово
готовность: 95
создано: 2026-05-15
источники:
  - "Теория. Метрики. Оценка качества моделей"
  - "Тренировки по ML 2.0 Лекция 4: Машинный перевод (Яндекс)"
  - "Лекция. Постановка задачи языкового моделирования. Метрики качества"
предпосылки:
  - "Языковое моделирование и алгоритмы генерации текста"
  - "Архитектура Seq2Seq (Энкодер-Декодер)"
  - "Transfer Learning в NLP и семейство BERT"
связано:
  - "Безопасность и Интерпретируемость LLM (XAI, Fake Detection, Watermarking)"
  - "Продвинутые Seq2Seq (T5, BART) и RAG-системы"
сравнить-с: []
---

# Метрики оценки NLP-моделей (Perplexity, BLEU, ROUGE, BERTScore, GLUE)

> [!abstract] Суть
> Оценить качество NLP-модели сложнее, чем в классическом ML: один и тот же перевод или текст может быть правильным в десятке вариантов. Поэтому в NLP сложилась целая иерархия метрик — от математически удобной **Perplexity** (которую нельзя сравнивать между разными токенизаторами) до n-граммных **BLEU** и **ROUGE** (которые не понимают синонимов), до семантических **BERTScore** и человеческой оценки через **GLUE-бенчмарки**. Ни одна автоматическая метрика не является золотым стандартом.

## Ответ

### 1. Perplexity — «Сколько вариантов перебирает модель?»

Представьте, что модель предсказывает следующее слово в тексте. Если она предсказывает уверенно (вероятность правильного слова близка к 1) — она «не удивляется». Если сомневается — «удивляется» сильно. **Perplexity (Перплексия)** — это количественное выражение этого удивления.

Интуиция: если PP = 10, модель предсказывает следующее слово так, как будто честно выбирает из 10 равновероятных вариантов. PP = 1 — идеальная модель (никогда не ошибается). PP = размер словаря — полный рандом.

**Связь с Cross-Entropy Loss:**
Функция потерь при обучении языковой модели — это кросс-энтропия:

$$\Large H = -\frac{1}{N}\sum_{i=1}^{N}\log P(w_i \mid w_{<i})$$

Perplexity — это ровно **экспонента от кросс-энтропии**:

$$\Large PP = \exp(H) = \exp\!\left(-\frac{1}{N}\sum_{i=1}^{N}\log P(w_i \mid w_{<i})\right)$$

где:
- $N$ — количество токенов в тестовой последовательности
- $P(w_i \mid w_{<i})$ — вероятность, предсказанная моделью для $i$-го токена при условии всей предыдущей истории
- $H$ — кросс-энтропия языковой модели (то самое значение `loss` в логах обучения)

Чем ниже loss при обучении → тем ниже H → тем ниже PP. Поэтому **минимизация cross-entropy loss = минимизация Perplexity** — это одно и то же.

---

### 2. Почему нельзя сравнивать Perplexity моделей с разными токенизаторами?

Это одна из самых частых ошибок на практике.

PP считается как среднее «удивление» **на токен**. Но токены у разных моделей — разного размера:
- GPT-2 с BPE-словарём из 50k токенов разбивает «нейросеть» на `['ней', 'ро', 'сеть']` — 3 токена.
- Символьная модель разбивает на `['н', 'е', 'й', 'р', 'о', 'с', 'е', 'т', 'ь']` — 9 токенов.

**Что происходит:** символьной модели проще «угадать» следующий символ (их всего ~30-100), чем угадать следующий BPE-токен (их 50 000). Поэтому её PP в абсолютных числах будет ниже — не потому что она лучше, а потому что задача легче.

**Аналогия:** нельзя сравнивать «среднее количество бросков» кубика d6 против d100 — единицы измерения разные. PP — метрика для самоконтроля одной и той же модели, а не для сравнения архитектур с разными словарями.

---

### 3. BLEU — n-граммная точность с штрафом за краткость

BLEU (Bilingual Evaluation Understudy, 2002) долго был главным стандартом машинного перевода. Идея: проверить, насколько n-граммы (цепочки из n слов) в переводе модели совпадают с n-граммами в эталоне.

**Как работает:**
1. Считается precision по 1-, 2-, 3-, 4-граммам между гипотезой и референсом.
2. Берётся геометрическое среднее этих четырёх precision.
3. Умножается на **Brevity Penalty (BP)** — штраф за краткость.

Причем clipped precission:
Если рассматривать просто precision, то пример "the cat cat cat cat" при референсе "the cat is spleeping" никак не учтет у нас повторения слова "cat". Поэтому мы считаем:
clipped_count(g) = min(count_pred(g), count_ref(g))
И уже затем precision считаем как:

В BLEU используется не обычная, а **модифицированная n-граммная precision** (**modified / clipped precision**). Мы смотрим не на то, сколько n-грамм из эталона покрыла модель, а на то, какая доля n-грамм, **сгенерированных моделью**, действительно подтверждается референсом. Это важно именно для машинного перевода: лишние, галлюцинированные или повторённые куски в гипотезе должны ухудшать метрику. Recall здесь был бы опаснее: модель могла бы добавлять много лишних слов и всё равно покрывать эталон. Поэтому BLEU берёт precision, но клипует частоты, чтобы модель не могла бесконечно повторять одно совпавшее слово или фразу.

$$\Large \text{Count}_{clip}(g) = \min\!\left(\text{Count}_{cand}(g),\; \max_{r \in R}\text{Count}_{r}(g)\right)$$

$$\Large p_n = \frac{\sum_{g \in G_n(c)} \text{Count}_{clip}(g)}{\sum_{g \in G_n(c)} \text{Count}_{cand}(g)}$$

где:
- $g$ — конкретная $n$-грамма;
- $n$ — размер n-граммы: $1$ для униграмм, $2$ для биграмм, $3$ для триграмм, $4$ для 4-грамм;
- $c$ — гипотеза / candidate, то есть текст, который сгенерировала модель;
- $R$ — множество референсов; если референс один, максимум по $r \in R$ просто равен частоте этой n-граммы в единственном эталоне;
- $G_n(c)$ — множество n-грамм, которые встречаются в гипотезе $c$;
- $\text{Count}_{cand}(g)$ — сколько раз n-грамма $g$ встретилась в гипотезе;
- $\text{Count}_{r}(g)$ — сколько раз n-грамма $g$ встретилась в референсе $r$;
- $\text{Count}_{clip}(g)$ — засчитанное число совпадений после ограничения сверху частотой в референсе;
- $p_n$ — clipped precision для n-грамм размера $n$.

Например, если гипотеза `the cat cat cat cat`, а референс `the cat is sleeping`, то для униграммы `cat`: $\text{Count}_{cand}(\text{cat}) = 4$, $\text{Count}_{ref}(\text{cat}) = 1$, поэтому $\text{Count}_{clip}(\text{cat}) = 1$. Обычная precision ошибочно засчитала бы все повторы, а clipped precision засчитывает `cat` только один раз, потому что в эталоне оно встретилось один раз.


**Зачем нужен Brevity Penalty?** Без него модель могла бы схитрить: выдать одно единственное слово «хорошо», если оно есть в эталоне → precision по 1-граммам = 100%. BP штрафует за то, что гипотеза короче эталона:

$$\Large \text{BLEU} = BP \cdot \exp\!\left(\sum_{n=1}^{4} w_n \log p_n\right)$$

$$\Large BP = \begin{cases} 1 & \text{если } c > r \\ e^{1 - r/c} & \text{если } c \leq r \end{cases}$$

где:
- $p_n$ — precision по $n$-граммам: доля $n$-грамм из гипотезы, найденных в эталоне
- $w_n$ — вес $n$-граммы (обычно $w_n = 1/4$ для всех четырёх)
- $c$ — длина гипотезы (candidate)
- $r$ — длина эталона (reference)
- $BP = 1$ если гипотеза длиннее эталона (штраф только за краткость, не за длину)

**Пример:**
- Эталон: «Мама мыла раму сегодня»
- Гипотеза: «Мама мыла»
- Precision по 1-граммам: 2/2 = 1.0 (оба слова есть в эталоне)
- Но $BP = e^{1 - 5/2} = e^{-1.5} \approx 0.22$ → итоговый BLEU сильно снижен

**Когда лучше использовать BLEU:**

BLEU лучше подходит там, где модель должна породить текст, близкий к эталону по формулировкам, локальному порядку слов и отсутствию лишних вставок. Классический случай — **машинный перевод**.

Почему именно BLEU:
- он precision-ориентирован: штрафует лишние n-граммы в гипотезе;
- смотрит не только на отдельные слова, но и на 2-, 3-, 4-граммы, поэтому частично учитывает порядок;
- через Brevity Penalty не даёт модели выигрывать слишком короткими переводами;
- хорошо работает для сравнения моделей на одном и том же фиксированном translation benchmark.

Пример, где BLEU логичен:

```text
Reference: "The model reduced inference latency by 30 percent"

Candidate A: "The model reduced inference latency by 30 percent"
Candidate B: "The model reduced latency"
Candidate C: "The model increased inference latency by 30 percent"
```

Candidate A получит высокий BLEU: совпали и слова, и локальные n-граммы. Candidate B короче и потерял важную часть смысла — сработает Brevity Penalty и упадут длинные n-граммы. Candidate C может иметь много совпадающих слов, но ключевое слово `increased` меняет смысл; это как раз один из случаев, где BLEU может ошибиться, потому что он не понимает семантику, а смотрит на поверхностные совпадения.

**Хорошие задачи для BLEU:**
- машинный перевод;
- генерация формализованных текстов, где есть близкий эталон;
- сравнение seq2seq-моделей на одном датасете;
- автоматический regression-test качества перевода между версиями модели.

**Плохие задачи для BLEU:**
- диалоги и ассистенты;
- creative writing;
- открытые QA-ответы;
- суммаризация с сильным перефразированием;
- задачи, где один и тот же смысл можно выразить десятками разных способов.

**Когда BLEU не работает:**
- **Синонимы:** «радостный» и «счастливый» для BLEU — разные слова → ошибка
- **Перефразирование:** «He went home» и «He returned to his house» — BLEU покажет 0
- **Несколько правильных переводов:** BLEU оценивает относительно одного референса (хотя поддерживает несколько)
- **Открытая генерация:** для диалогов и сторителлинга BLEU вообще не подходит — нет единственного правильного ответа

---

### 4. ROUGE vs BLEU — Precision vs Recall

**Ключевое различие:** BLEU ориентирован на **Precision** (что из гипотезы есть в эталоне), ROUGE ориентирован на **Recall** (что из эталона покрыто гипотезой).

**ROUGE-N:**
$$\Large \text{ROUGE-N} = \frac{\sum_{\text{n-gram} \in \text{эталон}} \text{Count}_{\text{match}}(\text{n-gram})}{\sum_{\text{n-gram} \in \text{эталон}} \text{Count}(\text{n-gram})}$$

где:
- $\text{Count}_\text{match}$ — количество n-грамм, которые встречаются и в гипотезе, и в эталоне
- Знаменатель — общее количество n-грамм в **эталоне** (не в гипотезе!)

На практике ROUGE часто возвращают сразу в трёх вариантах: precision, recall и F1. Но исторически и концептуально ROUGE особенно важен именно как **recall-ориентированная** метрика: он отвечает на вопрос «сколько содержимого из эталона модель покрыла в своём ответе?». Поэтому ROUGE часто используют для суммаризации: хороший summary должен сохранить ключевые факты из исходного/эталонного summary.

Для ROUGE-N обычно считают совпадение n-грамм с клипингом по частотам:

$$\Large \text{Count}_{match}(g) = \min\!\left(\text{Count}_{cand}(g),\; \text{Count}_{ref}(g)\right)$$

$$\Large R_{\text{ROUGE-N}} = \frac{\sum_{g \in G_n(ref)} \text{Count}_{match}(g)}{\sum_{g \in G_n(ref)} \text{Count}_{ref}(g)}$$

$$\Large P_{\text{ROUGE-N}} = \frac{\sum_{g \in G_n(cand)} \text{Count}_{match}(g)}{\sum_{g \in G_n(cand)} \text{Count}_{cand}(g)}$$

$$\Large F1_{\text{ROUGE-N}} = 2 \cdot \frac{P_{\text{ROUGE-N}} \cdot R_{\text{ROUGE-N}}}{P_{\text{ROUGE-N}} + R_{\text{ROUGE-N}}}$$

где:
- $g$ — конкретная n-грамма;
- $n$ — размер n-граммы: $1$ для ROUGE-1, $2$ для ROUGE-2 и т.д.;
- $cand$ — гипотеза / candidate, то есть текст модели;
- $ref$ — эталон / reference;
- $G_n(ref)$ — множество n-грамм в эталоне;
- $G_n(cand)$ — множество n-грамм в гипотезе;
- $\text{Count}_{ref}(g)$ — сколько раз n-грамма $g$ встретилась в эталоне;
- $\text{Count}_{cand}(g)$ — сколько раз n-грамма $g$ встретилась в гипотезе;
- $R_{\text{ROUGE-N}}$ — recall: какую долю n-грамм эталона покрыла модель;
- $P_{\text{ROUGE-N}}$ — precision: какая доля n-грамм модели есть в эталоне;
- $F1_{\text{ROUGE-N}}$ — баланс между precision и recall.

**Пример ROUGE-1 и ROUGE-2:**

```text
Эталон:   "кошка сидит на коврике и смотрит в окно"
Модель:   "кошка сидит на коврике"
```

ROUGE-1 смотрит на совпадение отдельных слов.

```text
Эталонные униграммы:
кошка, сидит, на, коврике, и, смотрит, в, окно

Униграммы модели:
кошка, сидит, на, коврике

Совпали:
кошка, сидит, на, коврике
```

Получаем:

$$\Large R_{\text{ROUGE-1}} = \frac{4}{8} = 0.5$$

$$\Large P_{\text{ROUGE-1}} = \frac{4}{4} = 1.0$$

$$\Large F1_{\text{ROUGE-1}} = 2 \cdot \frac{1.0 \cdot 0.5}{1.0 + 0.5} \approx 0.67$$

Интерпретация: всё, что модель сказала, есть в эталоне, поэтому precision высокий. Но модель покрыла только половину эталонного содержания, поэтому recall всего $0.5$.

ROUGE-2 смотрит уже на пары соседних слов:

```text
Эталонные биграммы:
кошка сидит, сидит на, на коврике, коврике и, и смотрит, смотрит в, в окно

Биграммы модели:
кошка сидит, сидит на, на коврике
```

Совпали 3 биграммы из 7 эталонных:

$$\Large R_{\text{ROUGE-2}} = \frac{3}{7} \approx 0.43$$

ROUGE-2 ниже, потому что он строже: ему важно не просто наличие отдельных слов, а сохранение локального порядка и связок между словами.

**ROUGE-L:** вместо n-грамм использует длину **наидлиннейшей общей подпоследовательности** (LCS) между гипотезой и эталоном. Не требует, чтобы слова шли подряд — достаточно совпадения порядка.

Для ROUGE-L считаем длину LCS:

$$\Large R_{LCS} = \frac{LCS(cand, ref)}{|ref|}$$

$$\Large P_{LCS} = \frac{LCS(cand, ref)}{|cand|}$$

$$\Large F1_{LCS} = 2 \cdot \frac{P_{LCS} \cdot R_{LCS}}{P_{LCS} + R_{LCS}}$$

где:
- $LCS(cand, ref)$ — длина наибольшей общей подпоследовательности между гипотезой и эталоном;
- $|ref|$ — длина эталона в токенах;
- $|cand|$ — длина гипотезы в токенах.

Например:

```text
Эталон: "кошка сидит на коврике и смотрит в окно"
Модель: "кошка на коврике смотрит"
```

Общая подпоследовательность может быть:

```text
кошка -> на -> коврике -> смотрит
```

Эти слова идут в правильном порядке, хотя не обязательно подряд. Поэтому $LCS = 4$, $R_{LCS} = 4/8 = 0.5$, $P_{LCS} = 4/4 = 1.0$. ROUGE-L полезен, когда важен общий порядок фраз, но не хочется требовать точного совпадения всех соседних n-грамм.

**Как читать ROUGE:**
- **ROUGE-1** — насколько хорошо покрыты отдельные ключевые слова;
- **ROUGE-2** — насколько хорошо сохранены короткие фразы и локальные связи;
- **ROUGE-L** — насколько хорошо сохранён порядок содержания;
- **ROUGE Recall высокий, Precision низкий** — модель покрыла эталон, но добавила много лишнего;
- **ROUGE Precision высокий, Recall низкий** — модель сказала мало, зато почти всё сказанное было релевантно;
- **ROUGE F1** — компромисс, который удобно сравнивать между моделями.

**Когда лучше использовать ROUGE:**

ROUGE лучше подходит там, где важно понять, **покрыла ли модель ключевое содержание эталона**. Поэтому его чаще всего используют для **суммаризации**: summary может быть короче, переформулированным, но оно должно сохранить главные факты.

Почему именно ROUGE:
- он recall-ориентирован: проверяет, сколько эталонного содержания попало в ответ модели;
- ROUGE-1 показывает покрытие ключевых слов;
- ROUGE-2 показывает, сохранились ли короткие смысловые связки;
- ROUGE-L учитывает порядок через LCS и мягче, чем точное n-граммное совпадение;
- precision/F1-версии помогают заметить, что модель добавила слишком много лишнего.

Пример, где ROUGE логичнее BLEU:

```text
Эталонное summary:
"Компания увеличила выручку на 12%, но прибыль снизилась из-за роста расходов"

Summary A:
"Выручка компании выросла на 12%, прибыль снизилась из-за расходов"

Summary B:
"Компания показала хорошие финансовые результаты"

Summary C:
"Выручка выросла на 12%, прибыль снизилась, расходы увеличились, акции компании торговались волатильно, менеджмент провёл встречу"
```

Summary A хорошо покрывает ключевые факты эталона: рост выручки, снижение прибыли, причина в расходах. ROUGE-1/ROUGE-2/ROUGE-L будут относительно высокими. Summary B звучит правдоподобно, но почти не покрывает конкретные факты — ROUGE Recall будет низким. Summary C покрывает эталон, но добавляет лишнее; ROUGE Recall может быть высоким, а ROUGE Precision/F1 покажут, что ответ раздут.

**Хорошие задачи для ROUGE:**
- extractive/abstractive summarization;
- сравнение summary-моделей на фиксированном датасете;
- проверка, сохранились ли ключевые факты из эталонного ответа;
- QA, где эталонный ответ короткий и важно покрыть основные слова/фразы;
- оценка сжатия документов, новостей, отчётов, диалогов.

**Плохие задачи для ROUGE:**
- машинный перевод, где лишние слова и точность формулировки критичнее покрытия;
- диалоги без единственного правильного ответа;
- ответы с сильными синонимами и перефразированием;
- задачи, где фактологическая ошибка может состоять в одном слове: `увеличилась` вместо `снизилась`.

**Когда что использовать:**

| Метрика | Когда использовать | Почему |
|---|---|---|
| **BLEU** | Машинный перевод | Важно, чтобы перевод был точным — нет лишних слов |
| **ROUGE-N** | Суммаризация | Важно, чтобы краткое изложение покрывало ключевые факты из оригинала |
| **ROUGE-L** | Суммаризация, QA | Важно сохранить структуру и порядок мыслей |

**Главное:** оба не понимают семантику. «Автомобиль» и «машина» — для обоих это разные слова.

---

### 5. BERTScore — семантическая метрика через эмбеддинги

**Идея:** вместо сравнения строк сравниваем смысл через косинусное сходство контекстуальных эмбеддингов BERT.

**Как работает:**
1. Каждый токен гипотезы и эталона пропускается через BERT → получаем контекстуальные векторы.
2. Для каждого токена гипотезы находим наиболее похожий токен эталона (по косинусному сходству) — это **BERTScore Precision**.
3. Для каждого токена эталона находим наиболее похожий токен гипотезы — это **BERTScore Recall**.
4. F1 — среднее из Precision и Recall.

Чуть подробнее по шагам:

1. Берём гипотезу модели $\hat{y}$ и эталон $y$.
2. Прогоняем оба текста через BERT/RuBERT/XLM-R и получаем contextual embeddings для каждого токена.
3. Строим матрицу попарных cosine similarity между токенами гипотезы и токенами эталона.
4. Для precision: для каждого токена гипотезы ищем самый похожий токен в эталоне, затем усредняем эти максимальные сходства.
5. Для recall: для каждого токена эталона ищем самый похожий токен в гипотезе, затем усредняем эти максимальные сходства.
6. После этого считаем F1 как гармоническое среднее precision и recall.

Важно: BERTScore обычно использует именно **cosine similarity**, а не distance. Если где-то говорят «косинусное расстояние», часто имеют в виду величину $1 - \cos(a,b)$. В формулах BERTScore удобнее думать в терминах similarity: чем ближе значение к $1$, тем более похожи токены по смыслу.

Матрица сходств:

$$\Large S_{ij} = \cos(\hat{h}_j,\; h_i)$$

где:
- $S_{ij}$ — семантическое сходство между $j$-м токеном гипотезы и $i$-м токеном эталона;
- $\hat{h}_j$ — embedding $j$-го токена гипотезы;
- $h_i$ — embedding $i$-го токена эталона.

$$\Large P_\text{BERT} = \frac{1}{|\hat{y}|} \sum_{\hat{y}_j \in \hat{y}} \max_{y_i \in y} \cos(\hat{h}_j,\, h_i)$$

$$\Large R_\text{BERT} = \frac{1}{|y|} \sum_{y_i \in y} \max_{\hat{y}_j \in \hat{y}} \cos(h_i,\, \hat{h}_j)$$

$$\Large F_\text{BERT} = 2 \cdot \frac{P_\text{BERT} \cdot R_\text{BERT}}{P_\text{BERT} + R_\text{BERT}}$$

где:
- $\hat{y}$ — гипотеза (сгенерированный текст), $y$ — эталон
- $\hat{h}_j, h_i$ — контекстуальные эмбеддинги соответствующих токенов
- $\cos(\cdot)$ — косинусное сходство
- Операция $\max$ — «мягкое» выравнивание: каждый токен гипотезы находит своего ближайшего партнёра в эталоне

**Интуиция precision и recall в BERTScore:**

- **BERTScore Precision** отвечает на вопрос: «насколько всё, что сказала модель, подтверждается эталоном по смыслу?»
- **BERTScore Recall** отвечает на вопрос: «насколько модель покрыла всё важное, что было в эталоне?»
- **BERTScore F1** удобен, когда нужно сбалансировать оба требования.

Пример:

```text
Эталон:
"Модель ускорила инференс и снизила расход памяти"

Candidate A:
"Модель ускорила инференс"

Candidate B:
"Модель ускорила инференс, снизила расход памяти и улучшила качество на GPU"
```

Candidate A будет иметь высокий **BERTScore Precision**: почти всё, что он сказал, есть в эталоне. Но **Recall** будет ниже, потому что он пропустил факт про снижение расхода памяти.

Candidate B может иметь высокий **Recall**, потому что покрыл основные факты эталона. Но **Precision** может быть ниже, потому что добавил лишний факт про качество на GPU, которого нет в эталоне.

**Когда важнее BERTScore Precision:**
- RAG/QA, где опасны лишние или неподтверждённые утверждения;
- генерация ответов по документу, где нужно штрафовать галлюцинации;
- медицинские, юридические, финансовые ответы, где «добавил лишнее» может быть хуже, чем «ответил кратко»;
- factual consistency: проверяем, насколько сгенерированный текст не выходит за рамки эталона.

**Когда важнее BERTScore Recall:**
- суммаризация, где нужно покрыть все ключевые факты эталона;
- ответы на экзаменационные вопросы, где важно упомянуть все пункты;
- извлечение информации, где пропуск сущности или факта критичен;
- сравнение моделей, которые могут отвечать кратко: recall покажет, не теряют ли они существенную часть содержания.

**Когда смотреть BERTScore F1:**
- когда одинаково важны полнота и отсутствие лишнего;
- при общем сравнении моделей на одном датасете;
- когда нет явного бизнес-приоритета между precision и recall.

В некоторых реализациях BERTScore дополнительно используют **IDF-взвешивание**: редкие и информативные слова получают больший вес, а частые служебные слова — меньший. Это помогает не переоценивать совпадения вроде «и», «в», «на», «это».

**Преимущества перед BLEU/ROUGE:**
- Понимает синонимы: «радостный» ≈ «счастливый» → высокое cos-сходство
- Понимает парафразы: «пошёл домой» ≈ «вернулся к себе»
- Учитывает контекст: «bank» (берег) ≠ «bank» (банк) — BERT разделяет значения

**Ограничения:**
- Зависит от качества базовой модели BERT (для русского нужен ruBERT)
- Вычислительно дорог для больших корпусов
- Не идеален для специфической терминологии, которой нет в словаре BERT

---

### 6. GLUE и SuperGLUE — зачем нужны бенчмарки?

**Проблема:** одна метрика (BLEU, PP) не отражает «общий интеллект» модели в NLP. Разные задачи требуют разных способностей. Бенчмарки — это **наборы разнородных задач со стандартизированной оценкой**, позволяющие сравнивать модели честно.

**GLUE (General Language Understanding Evaluation, 2018):**

| Задача | Что тестирует |
|---|---|
| CoLA | Грамматическая правильность предложения |
| SST-2 | Анализ тональности (позитив/негатив) |
| MRPC | Являются ли два предложения перефразом друг друга? |
| STS-B | Семантическое сходство пары предложений (0–5) |
| QQP | Дублируют ли два вопроса друг друга? |
| MNLI | Textual Entailment: влечёт ли предложение A предложение B? |
| QNLI | Reading comprehension: содержит ли параграф ответ на вопрос? |
| RTE | Вариант MNLI на меньшей выборке |
| WNLI | Winograd: кого обозначает местоимение? |

Итоговый скор GLUE — среднее по всем задачам. Когда в 2019–2020 году модели (BERT, RoBERTa, ALBERT) превзошли человеческий уровень (89.9), задачи устарели.

**SuperGLUE (2019) — более сложные задачи:**

| Задача | Что тестирует |
|---|---|
| BoolQ | Ответ «да/нет» по фрагменту текста |
| WiC | Одно ли значение слова в двух контекстах? |
| WSC (Winograd) | Разрешение кореференции |
| MultiRC | Несколько правильных ответов на вопрос |
| ReCoRD | Заполнение пропуска с поиском по тексту |

Зачем нужны такие бенчмарки:
1. **Воспроизводимость** — стандартизированные сплиты и метрики
2. **Сравнимость** — одна таблица лидеров для всей индустрии
3. **Выявление слабостей** — 9 разных задач обнаруживают провалы модели, которые BLEU не заметит

---

### 7. Benchmark Contamination — когда скоры врут

**Что это:** тестовые данные бенчмарка оказываются в обучающей выборке модели — она запоминает ответы, а не учится рассуждать. Модель показывает высокие скоры, не обобщив ни один реальный паттерн.

**Почему это проблема для LLM:** модели обучаются на сотнях миллиардов токенов из всего интернета. Тексты вопросов и ответов GLUE/MMLU/HumanEval публично доступны → с высокой вероятностью попали в pretraining data.

**Как обнаружить:**
- **N-gram overlap анализ:** посчитать, как часто точные фразы из тестовых вопросов встречаются в обучающем корпусе
- **Перформанс на видоизменённых задачах:** если точность на «переформулированном» тесте сильно падает — модель зазубрила, а не поняла

**Как бороться:**
- **Hold-out бенчмарки:** публиковать бенчмарки после даты отсечения обучающих данных модели
- **Динамическая оценка:** регулярно генерировать новые задачи (Dynabench, LiveBench)
- **Множественные варианты ответов:** перемешивать порядок вариантов — memorized модели не адаптируются
- **Проверять утечки:** открытые отчёты о contamination (GPT-4, Llama 3 оба содержат такие секции)

---

### 8. Как оценивать LLM для открытой генерации?

Для открытой генерации (чат-боты, сторителлинг, ответы на вопросы) BLEU и ROUGE почти бесполезны — нет единственного правильного ответа.

**Иерархия методов:**

**Закрытые бенчмарки (с правильным ответом):**
- **MMLU** (57 областей знаний, multiple choice) — проверяет фактические знания
- **HumanEval** (164 задачи на кодирование) — проверяет умение писать правильный код
- **MATH** — математические задачи с проверяемым ответом
- Преимущество: автоматизируемы, нет субъективности

**Предпочтение-ориентированные методы:**
- **Chatbot Arena** (LMSys) — пользователи сравнивают два анонимных ответа, рейтинг по системе ELO. Самый честный агрегированный сигнал о предпочтениях реальных пользователей
- **Side-by-Side** (A/B оценка асессорами) — дорого, но точно

**LLM-as-Judge (GPT-4 / Claude как оценщик):**
- GPT-4 или другой сильный LLM выступает судьёй: получает вопрос + ответ модели → выставляет оценку или сравнивает два ответа
- Масштабируем и дёшев относительно людей
- **Проблема:** LLM-судья предвзят в пользу своего собственного стиля (длинные, уверенные ответы получают незаслуженно высокие оценки)

**Человеческая оценка — Золотой стандарт:**
- **Pointwise:** оценщик ставит оценку ответу от 1 до 5 по нескольким критериям (полезность, фактичность, безопасность)
- **MQM (Error Marking):** оценщик отмечает конкретные ошибки: Critical (вес 5) и Minor (вес 1); итог — сумма штрафов
- Дорого и медленно, но незаменимо для финального сравнения

## Формула / Схема

**Perplexity через Cross-Entropy:**
$$\Large PP = e^{H}, \quad H = -\frac{1}{N}\sum_{i=1}^{N}\log P(w_i \mid w_{<i})$$

где:
- $H$ — это ровно то значение `val_loss`, которое вы видите в логах обучения
- $N$ — длина тестового текста в токенах
- Следствие: **снижение loss на 0.1 ≈ умножение PP на $e^{-0.1} \approx 0.9$**

**BLEU с Brevity Penalty:**
$$\Large \text{BLEU} = BP \cdot \exp\!\left(\frac{1}{4}\sum_{n=1}^{4} \log p_n\right), \quad BP = \min\!\left(1,\; e^{1 - r/c}\right)$$

где:
- $p_n$ — модифицированная precision по $n$-граммам (с клипингом по частоте в эталоне)
- $r/c$ — отношение длины эталона к длине гипотезы

**BERTScore F1:**
$$\Large F_\text{BERT} = 2 \cdot \frac{P_\text{BERT} \cdot R_\text{BERT}}{P_\text{BERT} + R_\text{BERT}}$$

где $P_\text{BERT}$ и $R_\text{BERT}$ — средние максимальные косинусные сходства токенов гипотезы с токенами эталона (и наоборот).

## Короткий пример

```python
from evaluate import load

# --- BLEU ---
bleu = load("bleu")
result = bleu.compute(
    predictions=["Мама мыла"],
    references=[["Мама мыла раму сегодня"]]
)
print(result["bleu"])  # штраф за краткость сильно занижает

# --- ROUGE ---
rouge = load("rouge")
result = rouge.compute(
    predictions=["Мама мыла раму"],
    references=["Мама вчера мыла большую деревянную раму"]
)
print(result["rougeL"])  # LCS-based recall

# --- BERTScore ---
bertscore = load("bertscore")
result = bertscore.compute(
    predictions=["Он был счастлив"],
    references=["Он был радостен"],
    lang="ru"
)
print(result["f1"])  # высокий — «счастлив» ≈ «радостен» по смыслу
```

## Типичные ошибки

- **Сравнивать PP моделей с разными токенизаторами:** PP считается на токен — более мелкий словарь (символы, subwords) даёт математически меньшую PP без улучшения качества.
- **Полагаться на BLEU для диалогов и открытой генерации:** BLEU предполагает один-единственный правильный ответ. Для чат-ботов это не так — нет референса. Используйте LLM-as-Judge или человеческую оценку.
- **Игнорировать Brevity Penalty в BLEU:** BP=1 означает либо, что гипотеза длиннее эталона, либо что штраф не применяется. Без BP модель может выдавать бесконечно длинный текст и получать высокое BLEU.
- **Считать высокий скор GLUE «умным» моделью:** скор бенчмарка можно раздуть через contamination или специфическую настройку под конкретные задачи. GLUE — необходимое, но не достаточное условие общей компетентности.
- **Использовать ROUGE для машинного перевода:** ROUGE — recall-ориентированная метрика (сколько эталона покрыто), тогда как в переводе важнее precision (нет лишних слов). BLEU здесь логичнее.

## Каверзные вопросы

> [!question] Модель A имеет PP = 20 на русском языке, модель B — PP = 15 на том же корпусе, но с другим токенизатором. Какая модель лучше?
> Нельзя сказать. Разные токенизаторы → разные единицы измерения. Сравнивать PP можно только при **одинаковом токенизаторе** на **одном и том же тестовом корпусе**. Для честного сравнения нужно перевести обе модели к общей единице (например, PP на **символ** или **слово**) или использовать метрику, не зависящую от токенизатора (BERTScore, человеческая оценка).

> [!question] Почему у идеальной модели (которая всегда угадывает следующее слово) Perplexity = 1, а не 0?
> При $P(w_i \mid w_{<i}) = 1$ для всех $i$ получаем $\log P = 0$, $H = 0$, $PP = e^0 = 1$. Перплексия не может быть меньше 1 по определению — это **среднее геометрическое обратных вероятностей**, и минимум достигается при абсолютной уверенности. Значение 0 невозможно: это означало бы $\log P = -\infty$, то есть модель назначает нулевую вероятность правильному токену.

> [!question] BERTScore показал 0.95 для перевода. Означает ли это, что перевод хороший?
> Не обязательно. BERTScore измеряет **семантическую близость** токенов, но не проверяет: 1) фактическую точность (можно сказать противоположное правдоподобными словами), 2) грамматику (грамматически сломанное предложение из правильных слов получит высокий score), 3) стиль и уместность. Кроме того, score зависит от качества базовой модели BERT для конкретного языка.

## Проверка себя

- Почему `PP = exp(val_loss)` — это не приближение, а точное равенство?
- Объясните, зачем BLEU использует **модифицированную** precision (с клипингом), а не обычную.
- Переводчик выдал «Кот сидел на мате» вместо «Кот сидел на мате». BLEU высокий, но перевод неверный. В чём проблема BLEU?
- Почему GPT-4 как LLM-судья предвзят в пользу длинных ответов?

## Предпосылки

- [[NLP/LLM и Промпт-инжиниринг/Языковое моделирование и алгоритмы генерации текста]]
- [[NLP/Модели и архитектуры/Архитектура Seq2Seq (Энкодер-Декодер)]]
- [[NLP/LLM и Промпт-инжиниринг/Transfer Learning в NLP и семейство BERT]]

## Связано

- [[NLP/LLM и Промпт-инжиниринг/Безопасность и Интерпретируемость LLM (XAI, Fake Detection, Watermarking)]]
- [[NLP/LLM и Промпт-инжиниринг/Продвинутые Seq2Seq (T5, BART) и RAG-системы]]

## Источники

- Теория. Метрики. Оценка качества моделей
- Тренировки по ML 2.0 Лекция 4: Машинный перевод (Яндекс)
- Лекция. Постановка задачи языкового моделирования. Метрики качества

---
[[🗺️ Индекс|Назад к разделу]]
