---
topic: GPT-2 — Zero-Shot Learning и начало эры промптов
card_ref: "[[GPT-2 — Zero-Shot Learning и начало эры промптов]]"
generated: 2026-05-14
---

Failed to authenticate. API Error: 403 {"error":{"type":"forbidden","message":"Request not allowed"}}

## Q001
type: fact
difficulty: junior
key_concepts: [WebText, Data Leakage, бенчмарки на основе Wikipedia, отбор по карме Reddit]
optional_concepts: [примеры конкретных бенчмарков: TriviaQA, Natural Questions]
text: При формировании датасета WebText авторы GPT-2 намеренно исключили из него Википедию. Объясните, зачем это было сделано и как механизм отбора ссылок с Reddit помогал контролировать качество данных.
follow_up:
  - Предположим, вы всё же оставили Википедию в обучающей выборке GPT-2. На каком конкретном типе бенчмарков это проявилось бы сильнее всего и как именно исказило бы результат?

---

## Q002
type: fact
difficulty: junior
key_concepts: [Zero-Shot Learning, промпт как контекст в естественном языке, TL;DR как явный сигнал задачи]
optional_concepts: []
text: GPT-2 умеет суммаризировать текст, если в конец добавить строку `TL;DR:`, и переводить, если написать `English: Hello = French:`. Объясните механизм: почему именно эти строки работают как инструкции, если модель никогда не обучалась «суммаризировать» или «переводить» в явном виде?
follow_up:
  - Если заменить `TL;DR:` на придуманный бессмысленный токен, которого нет в WebText, — изменится ли поведение модели? Почему?

---

## Q003
type: trap
difficulty: junior
key_concepts: [Perplexity зависит от словаря токенизатора, несравнимость PP через разные токенизаторы, BPE]
optional_concepts: [пример: разбивка слова «unmistakably» в GPT-2 vs BERT WordPiece]
text: Коллега заявляет: «Я сравнил Perplexity GPT-2 и BERT на одном тестовом корпусе — GPT-2 получила PP = 18, BERT = 24, значит GPT-2 значительно лучше как языковая модель». Есть ли в этом утверждении принципиальная ошибка?
follow_up:
  - Какой метод оценки качества языковых моделей позволяет корректно сравнивать их, если токенизаторы разные? Есть ли вообще такой стандарт?

---

## Q004
type: compare
difficulty: middle
key_concepts: [Post-LayerNorm vs Pre-LayerNorm, стабильность градиентов при глубоком обучении, Residual Stream]
optional_concepts: [формулы x_{l+1} для обоих вариантов, аналогия с градиентным бустингом]
text: GPT-2 заменила Post-LayerNorm на Pre-LayerNorm. Сравните эти два подхода: как меняется поведение градиентов при обратном распространении, почему Post-LayerNorm вызывает нестабильность при большом числе слоёв и какой архитектурный эффект создаёт Pre-LayerNorm (подсказка: Residual Stream)?
follow_up:
  - Если Pre-LayerNorm стабилизирует обучение, почему в ряде современных архитектур (например, оригинальный BERT) всё ещё используется Post-LayerNorm? Какой компромисс здесь существует?
  - Представьте, что вы добавляете новые слои поверх уже обученной Pre-LayerNorm модели (continual learning). Как свойство Residual Stream влияет на то, насколько «безопасно» это делать?

---

## Q005
type: chain
difficulty: middle
key_concepts: [Zero-Shot vs Few-Shot, промпт-инжиниринг, качество GPT-2 относительно SOTA, обобщение через языковое моделирование]
optional_concepts: [BLEU-метрика, конкретные числа: BLEU GPT-2 ≈ 11.5 vs SOTA ≈ 33.5]
text: Объясните, что значит «Zero-Shot Learning» применительно к GPT-2, и почему авторы считали полученные результаты по переводу сенсационными, хотя BLEU GPT-2 был примерно втрое ниже, чем у тогдашней SOTA-модели перевода?
follow_up:
  - Как переход от Zero-Shot к Few-Shot (несколько примеров прямо в промпте) меняет требования к размеру модели? Почему GPT-2 с 1.5B параметров показывала слабый Few-Shot, тогда как GPT-3 с 175B — сильный?

---

## Q006
type: scenario
difficulty: middle
key_concepts: [Zero-Shot ограничения GPT-2, разрыв между Perplexity и task-специфической метрикой, необходимость Fine-Tuning для продакшена]
optional_concepts: [метрика BLEU, domain shift]
text: Продуктовая команда хочет внедрить GPT-2 для автоматического суммаризации тикетов технической поддержки на русском языке. Вы обнаружили, что модель генерирует связный текст и Perplexity на тестовом корпусе тикетов получилась низкая. Менеджер говорит: «Отлично, запускаем в прод». Что вы ответите и какие риски выявите, прежде чем согласиться?
follow_up:
  - Предположим, вы всё же решили файнтюнить GPT-2 на этих тикетах. Какие конкретные шаги вы предпримете, чтобы не допустить утечки данных между обучающей и тестовой выборкой, учитывая, что тикеты содержат персональные данные клиентов?

---

## Q007
type: tradeoff
difficulty: middle
key_concepts: [BPE с запретом межкатегорийных слияний, сегментация токенов, качество понимания морфологии vs размер словаря]
optional_concepts: [пример: «Dog» vs «Dog.» как разные токены, влияние на downstream задачи NER/POS]
text: GPT-2 запрещает BPE объединять символы разных категорий (буквы и пунктуацию). Это означает, что `Dog` и `Dog.` никогда не сольются в один токен. Какие преимущества это даёт для понимания смысла, и какой ценой — с точки зрения размера словаря и эффективности токенизации длинных текстов?
follow_up:
  - Представьте, что вы обучаете модель на коде (Python/SQL), где пунктуация несёт смысловую нагрузку (скобки, операторы). Нужно ли для кода ужесточать или ослаблять правило межкатегорийных слияний — и почему?

---

## Q008
type: chain
difficulty: senior
key_concepts: [масштабирование инициализации на 1/√(2N), Residual Stream как сквозной канал, стабильность дисперсии активаций, аналогия с бустингом]
optional_concepts: [формула W ~ N(0, σ²/2N), связь с He/Xavier инициализацией, доказательство сохранения дисперсии]
text: В GPT-2 веса слоёв, добавляющих вклад в Residual Stream, инициализируются с масштабированием на $\frac{1}{\sqrt{2N}}$, где $N$ — число слоёв. Объясните математически, зачем нужен этот коэффициент: что происходит с дисперсией активаций при суммировании $N$ независимых вкладов без этого масштабирования, и как именно коэффициент восстанавливает стабильность? Как эта идея соотносится с аналогией «GPT-2 как градиентный бустинг»?
follow_up:
  - Если во время обучения вы видите, что нормы активаций на последних слоях резко растут несмотря на Pre-LayerNorm и масштабированную инициализацию, — какие причины вы проверите в первую очередь и как будете диагностировать проблему?
  - При переносе архитектуры GPT-2 на 96 слоёв (вместо 48 оригинальных) нужно ли пересчитать коэффициент масштабирования? Как это повлияет на скорость сходимости на первых шагах обучения?
