---
topic: Эволюция GPT — Обучение без учителя как сжатие данных (GPT-1)
card_ref: "[[Эволюция GPT — Обучение без учителя как сжатие данных (GPT-1)]]"
generated: 2026-05-14
---

Failed to authenticate. API Error: 403 {"error":{"type":"forbidden","message":"Request not allowed"}}

## Q001
type: fact
difficulty: junior
key_concepts: [Decoder-only архитектура, Masked Multi-Head Self-Attention, причина удаления Энкодера]
optional_concepts: [Cross-Attention]
text: Какие компоненты оригинального Трансформера были убраны в GPT-1 и почему? Опишите итоговую структуру одного блока GPT-1.
follow_up:
  - Что произошло бы с механизмом Cross-Attention, если бы Энкодер всё-таки оставили, но убрали входную последовательность (подавали бы только один специальный токен)?

---

## Q002
type: trap
difficulty: junior
key_concepts: [каузальная маска, teacher forcing, причина обязательности маскирования]
optional_concepts: [утечка данных из будущего]
text: Коллега говорит: «Зачем вообще нужна односторонняя маска в Self-Attention GPT-1? Ведь на инференсе будущих токенов и так нет — модель физически их не видит. Значит, маска нужна только для скорости». Он прав?
follow_up:
  - Если убрать маску только во время обучения, но оставить авторегрессионную генерацию на инференсе — как изменится поведение модели и почему метрики на валидации могут выглядеть отлично, а реальная генерация будет деградировать?

---

## Q003
type: fact
difficulty: junior
key_concepts: [принцип MDL, связь предсказания следующего слова и сжатия, энтропия]
optional_concepts: [формула максимизации правдоподобия]
text: Объясните связь между задачей предсказания следующего токена и задачей сжатия данных без потерь. Почему говорят, что лучший языковой компрессор обязан выучить «законы мира»?
follow_up:
  - Предположим, модель обучается только на новостных текстах. Можно ли утверждать, что она выучит те же «законы мира», что и модель на смешанном корпусе? Какие аспекты мирового знания окажутся слабее?

---

## Q004
type: compare
difficulty: middle
key_concepts: [Encoder-Decoder vs Decoder-only, двунаправленный контекст, авторегрессионная генерация, задачи seq2seq]
optional_concepts: [Cross-Attention bottleneck]
text: Сравните архитектуры Encoder-Decoder (оригинальный Трансформер) и Decoder-only (GPT-1). Для каких задач каждая из них предпочтительнее и почему?
follow_up:
  - Команда хочет решить задачу машинного перевода с помощью Decoder-only модели без Энкодера. Как нужно изменить постановку задачи (формат входных данных, промпт), чтобы это работало? Какие ограничения остаются по сравнению с Encoder-Decoder подходом?

---

## Q005
type: chain
difficulty: middle
key_concepts: [двухэтапный пайплайн GPT-1, unsupervised pre-training, supervised fine-tuning, комбинированный loss]
optional_concepts: [коэффициент λ при языковом loss во время fine-tuning]
text: Опишите двухэтапный процесс обучения GPT-1. Зачем при fine-tuning авторы добавляли языковой loss к loss классификации, а не оптимизировали только целевую задачу?
follow_up:
  - Вы проводите fine-tuning GPT-1 на задаче классификации тональности (10 000 размеченных примеров). После нескольких эпох loss классификации падает, а языковой loss резко растёт. Что это говорит о процессе обучения и что вы предпримете?

---

## Q006
type: scenario
difficulty: middle
key_concepts: [teacher forcing, накопление ошибок на инференсе, распределительный сдвиг train/inference]
optional_concepts: [exposure bias, scheduled sampling]
text: Бизнес просит GPT-модель генерировать длинные аналитические отчёты (2 000–3 000 токенов). На тестовых примерах (до 200 токенов) perplexity отличная, пользователи довольны. Но при генерации полноразмерных отчётов текст через 800–1 000 токенов начинает «плыть» и терять смысловую связность. Обучение и данные не менялись. Что происходит и как это диагностировать?
follow_up:
  - Какие техники на уровне обучения или инференса позволяют частично смягчить эту проблему? Назовите хотя бы две с их trade-off.

---

## Q007
type: debug
difficulty: middle
key_concepts: [утечка данных из будущего, каузальная маска, завышенная метрика на валидации]
optional_concepts: [матрица весов Attention без маски]
text: Инженер обучает GPT-подобную модель для авторегрессионного предсказания временных рядов. Код обучения ниже (упрощённо): Self-Attention реализован без каузальной маски, но на инференсе токены подаются по одному слева направо. Perplexity на валидации неожиданно низкая — 3.2 вместо ожидаемых 18–25. На продакшене модель работает случайно. Найдите ошибку и объясните, почему метрика оказалась завышена.

```python
# Обучение
logits = transformer_block(x, mask=None)  # маска не передана
loss = cross_entropy(logits, targets)

# Инференс
for i in range(seq_len):
    next_token = model(context[:i])  # только прошлое
    context[i] = next_token
```
follow_up:
  - Как именно нужно исправить код? Опишите форму и значения тензора маски для батча из B последовательностей длиной T. Почему на инференсе ошибка не проявилась сразу при ручной проверке коротких примеров?

---

## Q008
type: tradeoff
difficulty: senior
key_concepts: [однонаправленный vs двунаправленный контекст, авторегрессия vs masked LM, компромисс генерация/понимание, zero-shot перенос]
optional_concepts: [prefix LM как гибридный подход, UniLM]
text: Вы архитектор ML-платформы. Команда спорит: использовать ли Decoder-only (GPT-стиль) или Encoder-only с двунаправленным вниманием (BERT-стиль) как базовую модель для новой платформы, которая должна поддерживать одновременно: (а) генерацию текста, (б) классификацию документов, (в) извлечение именованных сущностей, (г) вопросно-ответные системы в zero-shot режиме. Аргументируйте выбор с учётом всех четырёх требований и назовите ключевые жертвы выбранного подхода.
follow_up:
  - Предположим, вы выбрали Decoder-only. Через полгода бизнес добавляет требование: классификатор должен работать на устройствах с 2 ГБ RAM без сетевого доступа. Как изменится ваше решение и какие компромиссы появятся?
  - Существуют ли гибридные архитектуры, снимающие часть этого противоречия? Опишите идею одной из них и её ограничения.
