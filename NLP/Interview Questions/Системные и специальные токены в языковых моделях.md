---
topic: Системные и специальные токены в языковых моделях
card_ref: "[[Системные и специальные токены в языковых моделях]]"
generated: 2026-05-20
---

## Q001
type: fact
difficulty: junior
key_concepts: [специальный токен, структурная роль, id в словаре, эмбеддинг]
optional_concepts: [зависимость от обучения]
text: Чем специальный токен отличается от обычного токена в языковой модели?
follow_up:
  - Что изменится, если добавить новый специальный токен в токенизатор, но не дообучить модель на данных с этим токеном?

## Q002
type: compare
difficulty: junior
key_concepts: [bos, eos, начало последовательности, остановка генерации]
optional_concepts: [max_new_tokens]
text: Сравните назначение токенов `<bos>` и `<eos>` в авторегрессионной языковой модели.
follow_up:
  - Что может произойти на инференсе, если декодер не останавливает генерацию при выборе `<eos>`?

## Q003
type: scenario
difficulty: junior
key_concepts: [pad, attention_mask, батчинг переменной длины, игнорирование padding-позиций]
optional_concepts: [loss mask]
text: Вы обучаете модель на батчах текстов разной длины. Для выравнивания добавили `<pad>`, обучение идёт стабильно, но качество на коротких текстах заметно хуже. Что могло пойти не так?
follow_up:
  - Как бы вы проверили, что модель действительно обращает внимание на padding-позиции?

## Q004
type: trap
difficulty: junior
key_concepts: [специальные токены не магические команды, обучение на формате, role tokens]
optional_concepts: []
text: Кандидат говорит: «Если написать в промпте `<system>отвечай кратко</system>`, любая LLM обязана понять это как системную инструкцию». В чём ошибка?
follow_up:
  - Что изменится, если такая разметка действительно использовалась при instruction tuning модели?

## Q005
type: compare
difficulty: middle
key_concepts: [[CLS], [SEP], агрегированное представление, разделение сегментов]
optional_concepts: [segment embeddings]
text: Сравните роли `[CLS]` и `[SEP]` в BERT для задачи классификации пары текстов.
follow_up:
  - Если убрать `[SEP]` между двумя текстами, почему модель может хуже решать entailment или similarity?

## Q006
type: chain
difficulty: middle
key_concepts: [[MASK], masked language modeling, обучающий токен, отличие от генерации]
optional_concepts: [pretrain-finetune mismatch]
text: Почему `[MASK]` в BERT обычно называют обучающим токеном, а не токеном для обычной генерации текста?
follow_up:
  - Как изменится проблема, если пользователь начнёт отправлять реальные запросы с `[MASK]` внутри текста?
  - Почему авторегрессионные LLM обычно не используют `[MASK]` как основной механизм генерации?

## Q007
type: debug
difficulty: middle
key_concepts: [loss mask, pad token, ложный обучающий сигнал, проверка пайплайна]
optional_concepts: [label shifting]
text: В пайплайне дообучения causal LM последовательности дополняются `<pad>` справа. Attention mask выставлена правильно, но loss считается по всем позициям, включая padding. Валидационный loss выглядит хорошим, а ответы модели часто преждевременно заканчиваются или содержат мусорные хвосты. Найдите ошибку.
follow_up:
  - Как бы вы исправили вычисление loss, если `<pad>` совпадает с `<eos>`?
  - Какие симптомы в распределении предсказанных токенов помогли бы подтвердить эту проблему?

## Q008
type: scenario
difficulty: senior
key_concepts: [chat template, role tokens, совместимость токенизатора и модели, деградация после обновления]
optional_concepts: [миграция формата промпта, A/B тестирование]
text: В продакшене обновили токенизатор и chat template для ассистента, но веса модели оставили прежними. Offline-метрика на старом датасете почти не изменилась, а в реальных диалогах модель начала хуже следовать системным инструкциям и путать роли пользователя и ассистента. Что могло произойти?
follow_up:
  - Как бы вы спроектировали безопасную миграцию chat template без резкой деградации качества?
  - Что нужно логировать в продакшене, чтобы отличить проблему шаблона диалога от общего data drift?
