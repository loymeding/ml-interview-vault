---
topic: Продвинутые Seq2Seq (T5, BART) и RAG-системы
card_ref: "[[Продвинутые Seq2Seq (T5, BART) и RAG-системы]]"
generated: 2026-05-14
---

Failed to authenticate. API Error: 403 {"error":{"type":"forbidden","message":"Request not allowed"}}

## Q001
type: fact
difficulty: junior
key_concepts: [виды шума BART, Text Infilling, удаление токенов, задача предобучения]
optional_concepts: [Document Rotation]
text: Назовите и кратко опишите виды шума, которые используются при предобучении BART. Чем «удаление токенов» принципиально отличается от классического MLM в BERT?
follow_up:
  - Зачем BART вводит именно Text Infilling (замена целого span одним `[MASK]`), а не просто маскирует каждый токен отдельно — какую дополнительную сложность это создаёт для модели?

---

## Q002
type: trap
difficulty: junior
key_concepts: [архитектура Encoder-only, ограничения BERT, авторегрессионная генерация, Encoder-Decoder]
optional_concepts: []
text: Коллега предлагает дообучить BERT-large на корпусе новостей для задачи автоматической суммаризации — говорит, что у него хорошие эмбеддинги и он уже обучен на огромных данных. Что вы ответите?
follow_up:
  - Предположим, коллега всё же хочет использовать BERT в пайплайне суммаризации — в каком именно компоненте это было бы оправдано?

---

## Q003
type: fact
difficulty: junior
key_concepts: [Span Corruption, sentinel-маркеры, восстановление пропущенных span, предобучение T5]
optional_concepts: [эффективность обучения по сравнению с полным MLM]
text: Объясните метод предобучения Span Corruption в T5: что именно подаётся на вход энкодеру, что должен сгенерировать декодер, и зачем используются sentinel-маркеры (`<X>`, `<Y>`, ...)?
follow_up:
  - Почему T5 восстанавливает только пропущенные куски, а не весь исходный текст целиком — как это влияет на скорость обучения?

---

## Q004
type: compare
difficulty: middle
key_concepts: [BART vs T5 архитектура, формат Text-to-Text, задача предобучения, применимость к суммаризации]
optional_concepts: [ROUGE, predict_with_generate]
text: Вам нужно выбрать между BART и T5 для задачи абстрактной суммаризации новостных статей. Сравните эти архитектуры по принципу предобучения и формату входных данных. Какой из них вы выберете и почему?
follow_up:
  - Если датасет для файн-тюнинга очень маленький (≈5 000 пар), изменится ли ваш выбор — и почему именно шум предобучения здесь играет роль?

---

## Q005
type: chain
difficulty: middle
key_concepts: [RAG pipeline, retrieval, векторный поиск, инъекция контекста в prompt]
optional_concepts: [chunking стратегия, re-ranking]
text: Вы строите RAG-систему для корпоративного чат-бота, который должен отвечать на вопросы по внутренней документации компании. Опишите основные компоненты пайплайна от запроса пользователя до финального ответа. Где могут возникнуть узкие места?
follow_up:
  - Допустим, retrieval возвращает релевантные чанки, но LLM всё равно галлюцинирует детали, которых нет в retrieved-контексте. Какие конкретные меры вы предпримете?
  - Документация обновляется каждую неделю — как вы организуете инкрементальное обновление индекса без полной переиндексации?

---

## Q006
type: scenario
difficulty: middle
key_concepts: [качество обучающих данных, монолингвальный корпус, domain shift, ROUGE vs реальное качество]
optional_concepts: [back-translation, параллельные корпуса]
text: Команда дообучила T5-11B для перевода технических инструкций с английского на немецкий. ROUGE на тестовой выборке — 0.62, что выше базовой линии на 8 пунктов. Бизнес-заказчик жалуется, что переводы всё равно неприемлемы для публикации. В чём может быть проблема и как вы это проверите?
follow_up:
  - Если корень проблемы в данных, а не в архитектуре, какие три конкретных шага по улучшению данных вы предложите при ограниченном бюджете?

---

## Q007
type: debug
difficulty: middle
key_concepts: [утечка данных train/test, DataCollatorForSeq2Seq, токенизация целевой последовательности, паддинг]
optional_concepts: [label smoothing, ignore_index=-100]
text: Дан пайплайн файн-тюнинга BART для суммаризации на Hugging Face. ROUGE на тесте — 0.71, что кажется подозрительно высоким. Вот упрощённый код подготовки данных:

```python
full_dataset = dataset["train"].map(tokenize_fn)
train_data = full_dataset.select(range(0, 8000))
test_data  = full_dataset.select(range(8000, 10000))

def tokenize_fn(example):
    src = tokenizer(example["article"], truncation=True, max_length=512)
    tgt = tokenizer(example["summary"], truncation=True, max_length=128)
    src["labels"] = tgt["input_ids"]
    return src
```

Найдите как минимум две ошибки, объясните последствия каждой.
follow_up:
  - Исправив утечку, вы получили ROUGE=0.38. Бизнес требует минимум 0.45. Какой следующий шаг вы предпримете, не меняя архитектуру?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [масштабирование RAG, latency retrieval vs generation, обновление индекса, мониторинг качества ответов]
optional_concepts: [кэширование эмбеддингов, hybrid search BM25+dense]
text: Вы проектируете RAG-сервис для продакшена с требованиями: 10 000 RPS, p99 latency ≤ 800 мс, база знаний 50 млн документов, обновляется в реальном времени. Опишите архитектуру: как распределить бюджет latency между retrieval и generation, как организовать real-time обновление индекса без деградации качества поиска, и как детектировать, что качество ответов модели деградировало в продакшене?
follow_up:
  - Предположим, через месяц аналитика показывает, что 30% запросов приходят на узкую тематику (юридические вопросы), а общая модель даёт на них ROUGE на 15 пунктов ниже. Как вы адаптируете архитектуру без полного переобучения?
  - Ваш векторный индекс занимает 400 GB RAM. Бюджет на железо срезали вдвое. Какие компромиссы вы рассмотрите и как оцените потерю в качестве retrieval?
