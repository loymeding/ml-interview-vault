---
topic: Современные LLM (BERT vs GPT) — тонкости обучения
card_ref: "[[Современные LLM (BERT vs GPT) — тонкости обучения]]"
generated: 2026-05-14
---

Failed to authenticate. API Error: 403 {"error":{"type":"forbidden","message":"Request not allowed"}}

## Q001
type: fact
difficulty: junior
key_concepts: [Masked Self-Attention, авторегрессионная генерация, причинная маска (causal mask)]
optional_concepts: [треугольная матрица маскировки]
text: Зачем в Decoder-only архитектуре (например, GPT) применяют маскировку будущих токенов в Self-Attention? Что произойдёт, если убрать маску при обучении?
follow_up:
  - Маску убирают при инференсе или оставляют? Почему поведение модели не сломается, если генерация всё равно идёт слева направо?

---

## Q002
type: compare
difficulty: junior
key_concepts: [двунаправленный контекст BERT, однонаправленный контекст GPT, задача MLM vs CLM]
optional_concepts: [токен [MASK], teacher forcing]
text: Сравните задачи предобучения BERT (MLM) и GPT (CLM): чем они принципиально отличаются и как это ограничение задачи отражается на том, какие токены модель «видит» при обучении?
follow_up:
  - Если заменить в BERT задачу MLM на CLM, получим ли мы GPT? Что изменится в архитектуре, а что останется тем же?

---

## Q003
type: trap
difficulty: junior
key_concepts: [архитектурная несовместимость BERT с генерацией, предсказание замаскированных слов vs авторегрессия]
optional_concepts: []
text: Коллега предлагает дообучить BERT на корпусе рекламных текстов и использовать его как генератор слоганов. «BERT же трансформер — значит, умеет генерировать». Где здесь ошибка?
follow_up:
  - Существуют ли всё же способы сделать BERT-подобную модель способной к генерации (например, BERT2BERT, UniLM)? Какой ценой?

---

## Q004
type: chain
difficulty: middle
key_concepts: [случайная инициализация весов, нестабильность градиентов на старте, линейный рост LR в фазе Warm-up]
optional_concepts: [формула cosine decay, NaN в loss на первых шагах]
text: Объясните, почему при обучении LLM с сотнями миллиардов параметров нельзя сразу стартовать с максимального Learning Rate. Как Warm-up решает эту проблему?
follow_up:
  - Предположим, мы продолжаем обучение (continual pre-training) уже готовой модели на новом домене — нужен ли Warm-up снова, если веса уже «хорошие»? Аргументируйте.
  - Как изменится оптимальная длина Warm-up фазы, если мы втрое уменьшим размер батча?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [однонаправленный контекст Decoder, невозможность использования BERT для left-to-right генерации, выбор архитектуры под задачу]
optional_concepts: [beam search, temperature sampling]
text: Команда обучила BERT-large на 100 ГБ текстов службы поддержки, fine-tuned на примерах «запрос → ответ». Перплексия на валидации отличная — 12.3. На продакшн-демо модель выдаёт бессвязные фрагменты вместо связных ответов. Метрика хорошая, модель не работает. Что, скорее всего, пошло не так?
follow_up:
  - Если переходить на GPT-архитектуру для этой задачи, изменится ли способ подготовки обучающих пар «запрос → ответ»? Как именно?

---

## Q006
type: debug
difficulty: middle
key_concepts: [мастер-веса в Float32, моменты Adam в Float32, потеря мелких обновлений в Float16]
optional_concepts: [gradient scaling, underflow градиентов]
text: Инженер настроил Mixed Precision обучение: все тензоры, включая веса оптимизатора Adam (первый и второй момент), переведены в BFloat16 — «чтобы сэкономить ещё больше VRAM». Loss падает первые 2 000 шагов, затем перестаёт убывать и зависает на плато. Валидационные метрики не улучшаются. Найдите ошибку и объясните механизм поломки.
follow_up:
  - Предположим, мастер-веса вернули в Float32, но gradient scaling не настроен. Какая новая проблема может возникнуть и почему?

---

## Q007
type: tradeoff
difficulty: middle
key_concepts: [BERT — сила двунаправленного контекста для анализа, GPT — авторегрессия для генерации, latency vs качество понимания]
optional_concepts: [дистилляция, DistilBERT для продакшна]
text: Вас просят выбрать архитектуру для задачи классификации тональности отзывов в реальном времени (latency < 50 мс, 10 000 RPS). У вас есть претренированный BERT-large и GPT-2-medium. Какую модель возьмёте и почему? Какими преимуществами придётся пожертвовать?
follow_up:
  - Как изменится ваш выбор, если через месяц бизнес добавит требование: та же модель должна также генерировать краткий «совет продавцу» на основе отзыва?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [Mixed Precision inference (FP16/BF16), управление VRAM при обслуживании нескольких запросов, warm-up при возобновлении обучения после сбоя]
optional_concepts: [KV-cache в FP16, tensor parallelism, gradient checkpointing]
text: Вы деплоите LLM (70B параметров) в продакшн: модель должна обслуживать 500 одновременных пользователей со стримингом токенов (streaming). Опишите ключевые решения по управлению точностью вычислений и памятью на инференсе. Отдельно: во время ночного до-обучения (continual training) произошёл OOM-crash на шаге 15 000 из 50 000 — как правильно возобновить обучение, чтобы не потерять стабильность и не переплатить вычислениями?
follow_up:
  - KV-cache для 500 одновременных сессий при длине контекста 8 192 токена хранится в FP16. Оцените порядок потребления VRAM под кэш (модель — 70B, hidden size — 8 192, 80 слоёв, 8 KV-голов). Что делать, если памяти не хватает?
  - Если после возобновления обучения с чекпоинта loss резко «прыгает» вверх на 2–3 шага, прежде чем продолжить спуск — это нормально или признак проблемы? Как отличить одно от другого?
