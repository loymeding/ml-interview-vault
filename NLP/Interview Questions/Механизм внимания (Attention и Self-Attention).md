---
topic: Механизм внимания (Attention и Self-Attention)
card_ref: "[[NLP/Interview Questions/Механизм внимания (Attention и Self-Attention)]]"
generated: 2026-05-14
---

## Q001
type: fact
difficulty: junior
key_concepts: [контекстный вектор, узкое горлышко, потеря информации, длинные последовательности]
optional_concepts: [скрытые состояния энкодера]
text: Объясни, почему в классической архитектуре Seq2Seq без Attention возникает проблема «узкого горлышка». Что конкретно с этим вектором не так при обработке длинных предложений?
follow_up:
  - Предположим, предложение состоит всего из 5 слов. Сохраняется ли проблема узкого горлышка? Когда она становится критичной и почему?

---

## Q002
type: fact
difficulty: junior
key_concepts: [Query, Key, Value, матрицы проекции, вычисление из эмбеддингов]
optional_concepts: [несимметричные отношения, поисковые признаки]
text: Что такое Query, Key и Value в механизме Self-Attention? Как они вычисляются из входного эмбеддинга токена и зачем для каждого из них нужна отдельная обучаемая матрица?
follow_up:
  - Что произойдёт, если убрать матрицу V и передавать дальше исходный вектор X вместо взвешенной суммы значений?

---

## Q003
type: compare
difficulty: middle
key_concepts: [внимание Багданау, self-attention, разделение энкодер-декодер, параллелизм]
optional_concepts: [сложность O(n²), рекуррентность]
text: Сравни классический механизм внимания Багданау (2014) и Self-Attention из «Attention is All You Need» (2017). В чём принципиальное архитектурное отличие, и какое из них позволило отказаться от рекуррентных сетей?
follow_up:
  - Внимание Багданау считается «мягким» (soft attention). Есть ли ситуации, где «жёсткое» (hard attention) предпочтительнее, и какой ценой это достигается?

---

## Q004
type: trap
difficulty: middle
key_concepts: [масштабирование √dk, затухание градиентов, распределение softmax, размерность ключей]
optional_concepts: [эффект насыщения softmax]
text: Коллега говорит: «Деление на √dk — просто косметика, модель сама выучит нужный масштаб через веса. Уберём его, зато упростим код». Что ему ответить? Что конкретно пойдёт не так при обучении?
follow_up:
  - Если вместо √dk использовать d_k (без корня), ситуация станет лучше или хуже? Почему именно корень, а не любая другая нормировка?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [веса внимания, интерпретируемость, матрица выравнивания, диагональный паттерн]
optional_concepts: [beam search, качество перевода]
text: Команда обучила нейросетевой переводчик EN→RU. BLEU-метрика на тесте отличная, но лингвисты жалуются: модель путает порядок слов в сложных предложениях с инверсией. Продакт-менеджер просит объяснить, что происходит внутри модели. Как ты используешь механизм внимания для диагностики и что именно будешь смотреть?
follow_up:
  - Ты визуализировал матрицу весов внимания и увидел, что она почти диагональная и совсем не реагирует на дальние зависимости. О чём это говорит и как исправить?

---

## Q006
type: chain
difficulty: middle
key_concepts: [Multi-Head Attention, параллельные головы, конкатенация, специализация голов]
optional_concepts: [проекционная матрица W_O, размерность на голову d_k/h]
text: Объясни, как работает Multi-Head Attention. Зачем нужно несколько «голов», если одна уже умеет считать внимание? Что происходит с размерностью векторов внутри каждой головы?
follow_up:
  - Если увеличить количество голов с 8 до 64, сохранив общую размерность модели d_model = 512, как это повлияет на то, что каждая голова «видит»? Будет ли это полезно?
  - Можно ли после обучения посмотреть, на что специализируется каждая голова? Есть ли у этого практическое применение?

---

## Q007
type: tradeoff
difficulty: senior
key_concepts: [квадратичная сложность по длине, память GPU, latency vs качество, Local/Sparse Attention]
optional_concepts: [FlashAttention, Longformer, sliding window attention]
text: Тебе нужно обработать медицинские документы длиной до 50 000 токенов. Стандартный Self-Attention потребляет O(n²) памяти — на A100 такой батч просто не влезет. Предложи как минимум два архитектурных решения, аргументируй их трейдофф по качеству и скорости.
follow_up:
  - Ты выбрал Sparse Attention с фиксированным окном. На тестовой выборке метрика упала на 3% относительно полного внимания. Заказчик говорит: «3% — это слишком много». Какие аргументы и данные тебе нужны, чтобы принять финальное решение?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [KV-cache, инкрементальная генерация, память при инференсе, autoregressive decoding]
optional_concepts: [MQA/GQA, квантизация KV-cache]
text: Вы деплоите LLM-based сервис на основе трансформера для генерации ответов в реальном времени (latency SLA < 200 мс). Архитектор говорит использовать KV-cache. Объясни, что такое KV-cache, почему он критичен для автогрессивной генерации и какие проблемы он создаёт при масштабировании до тысяч одновременных пользователей.
follow_up:
  - Размер KV-cache растёт линейно с длиной контекста. При 10 000 активных сессий с контекстом по 4 096 токенов вы упираетесь в GPU-память. Предложи конкретную стратегию управления памятью без деградации качества для большинства запросов.
  - Multi-Query Attention (MQA) и Grouped-Query Attention (GQA) решают часть этой проблемы. В чём их суть и какой ценой они снижают потребление памяти?

## Q009
type: fact
difficulty: junior
key_concepts: [инвариантность Self-Attention к перестановкам токенов, добавление позиционного вектора к эмбеддингу, потеря информации о порядке без PE]
optional_concepts: [синусоидальное кодирование Vaswani et al. 2017]
text: Что такое позиционное кодирование (Positional Encoding) в трансформере и зачем оно нужно? Что произойдёт с предсказаниями модели, если убрать его полностью?
follow_up:
  - В оригинальном трансформере позиционное кодирование — фиксированные синусоиды, не обучаемые параметры. При каком условии обучаемые позиционные эмбеддинги окажутся предпочтительнее, и в чём их принципиальный недостаток относительно фиксированных?

---

## Q010
type: fact
difficulty: junior
key_concepts: [LayerNorm нормирует по размерности эмбеддинга каждого токена, BN нормирует по батчу, независимость LayerNorm от длины последовательности и размера батча]
optional_concepts: [нестабильность BN при малых батчах и паддинге]
text: Почему в трансформерах используется Layer Normalization, а не Batch Normalization? Объясни по какой оси нормирует каждый из этих методов и как это связано со спецификой NLP-задач.
follow_up:
  - Представь, что тебе нужно обрабатывать временные ряды фиксированной длины с очень большим батчем (512+). Изменится ли рекомендация по выбору нормировки? Как это аргументировать?

---

## Q011
type: fact
difficulty: junior
key_concepts: [авторегрессивная генерация слово за словом, маскировка будущих позиций, замена логитов на −∞ до Softmax]
optional_concepts: [causal mask, различие padding mask и causal mask]
text: Зачем в декодере трансформера первый слой Self-Attention называется Masked? Что именно маскируется, как это реализовано технически и почему без этой маскировки обучение генерации стало бы бессмысленным?
follow_up:
  - Если убрать маску только во время обучения, но оставить её на инференсе — модель обучится корректно? Опиши конкретный механизм сбоя, который возникнет.

---

## Q012
type: compare
difficulty: middle
key_concepts: [Skip Connection предотвращает затухание градиентов в глубоких сетях, LayerNorm стабилизирует масштаб активаций, совместное действие обоих компонентов в Add & Norm]
optional_concepts: [Pre-Norm vs Post-Norm, gradient highway через резидуальный путь]
text: В каждом блоке трансформера подблоки оборачиваются в Add & Norm: LayerNorm(x + Sublayer(x)). Зачем здесь два отдельных компонента — Skip Connection и LayerNorm? Можно ли убрать один из них, оставив только второй, без серьёзных последствий для обучения?
follow_up:
  - В GPT-2 и многих последующих архитектурах нормировку перенесли *перед* подблоком (Pre-Norm), а не после. Какую конкретную проблему, возникающую при очень большой глубине сети, это решает?

---

## Q013
type: chain
difficulty: middle
key_concepts: [разделение ролей Attention (межтокенное взаимодействие) и FFN (независимая трансформация каждого токена), point-wise применение FFN, нелинейная активация внутри FFN]
optional_concepts: [размерность скрытого слоя FFN обычно ≈ 4·d_model, интерпретация FFN как нейронного хранилища фактов]
text: Объясни роль Feed Forward Network внутри блока трансформера. Почему FFN применяется к каждому токену независимо (point-wise), а не сразу ко всей последовательности, как Self-Attention? Что было бы потеряно, если убрать FFN и оставить только слои Self-Attention?
follow_up:
  - Исследования (Geva et al., 2021) интерпретируют нейроны FFN как «хранилище фактов». Если это так, что произойдёт с фактическими знаниями модели при замене FFN на линейный слой без нелинейной активации — с точки зрения выразительной мощности?

---

## Q014
type: trap
difficulty: middle
key_concepts: [Softmax вносит нелинейность при вычислении весов внимания, FFN содержит нелинейные активации, зависимость весов от входа делает механизм адаптивно нелинейным]
optional_concepts: [линейная vs нелинейная аппроксимация, residual connections не добавляют нелинейность]
text: Джуниор-коллега утверждает: «Self-Attention — это просто взвешенное среднее Value-векторов, никакой нелинейности нет. Трансформер по сути линейная модель». Где конкретная ошибка в этом рассуждении? Перечисли все источники нелинейности в стандартном блоке трансформера.
follow_up:
  - Допустим, ты заменяешь Softmax в Attention на простую нормировку (divide by sum, без экспоненты). Какие конкретные свойства матрицы весов внимания изменятся и почему это ухудшит способность модели фокусироваться на релевантных токенах?

---

## Q015
type: scenario
difficulty: middle
key_concepts: [визуализация матриц внимания по конкретным токенам, Multi-Head Attention — разные головы фиксируют разные лингвистические паттерны, диагностика через паттерны внимания на словах-отрицаниях]
optional_concepts: [attention rollout для агрегации внимания по слоям, probing classifiers для диагностики представлений]
text: Команда обучила трансформерную модель для классификации тональности отзывов, accuracy на тесте — 95%. Но при ручном тестировании выясняется: модель стабильно ошибается на фразах «не плохо» и «совсем не понравилось». Продакт просит объяснить, что происходит внутри. Как ты используешь механизм внимания для диагностики? Что именно будешь смотреть и какой вывод сделаешь?
follow_up:
  - Визуализация показала, что слово «не» почти не получает внимания от соседних токенов — оно «тонет». Какие изменения в архитектуре или данных могут помочь, не меняя класс модели (трансформер)? Предложи хотя бы два варианта с обоснованием.

---

## Q016
type: tradeoff
difficulty: senior
key_concepts: [Cross-Attention явно разделяет представления источника и перевода, decoder-only требует квадратичного внимания по объединённой последовательности, KV-cache эффективнее утилизируется в encoder-decoder при инференсе]
optional_concepts: [prefix LM как компромиссная архитектура, throughput vs latency tradeoff при масштабировании]
text: Тебе нужно выбрать архитектуру для системы машинного перевода EN→RU. Вариант А: классический encoder-decoder трансформер с Cross-Attention. Вариант Б: decoder-only архитектура в стиле GPT, где исходное предложение и перевод конкатенируются в единую последовательность. Аргументируй трейдофф по качеству перевода, скорости инференса, потреблению памяти и сложности деплоя. При каком сценарии ты выберешь Вариант Б, несмотря на его очевидные накладные расходы?
follow_up:
  - Если выбранную модель нужно дообучить на узкоспециализированном домене (юридический перевод, 50k примеров), как архитектурный выбор повлияет на стратегию fine-tuning и риск catastrophic forgetting энкодерных представлений?
