---
topic: GPT-3 — Sparse Attention и In-Context Learning
card_ref: "[[GPT-3 — Sparse Attention и In-Context Learning]]"
generated: 2026-05-14
---

Failed to authenticate. API Error: 403 {"error":{"type":"forbidden","message":"Request not allowed"}}

## Q001
type: fact
difficulty: junior
key_concepts: [Local-паттерн, Strided-паттерн, разреженная матрица внимания, O(N√N)]
optional_concepts: [глобальные узлы-хабы]
text: Опишите два паттерна, из которых складывается Sparse Attention в GPT-3. Как их комбинация позволяет информации протекать через весь текст, несмотря на разреженность матрицы?
follow_up:
  - Если контекст состоит из 10 000 токенов, во сколько раз Sparse Attention теоретически экономит вычисления по сравнению с Dense Attention? Покажите через формулы сложности.

---

## Q002
type: trap
difficulty: junior
key_concepts: [заморозка весов, отсутствие градиентного шага, in-context learning, временность знания]
optional_concepts: []
text: Коллега говорит: «После Few-Shot сессии с GPT-3 модель "запомнила" новые примеры и будет использовать их в следующем запросе». Он прав или нет? Объясните механизм.
follow_up:
  - Если Few-Shot не обновляет веса, то где «хранится» знание о примерах во время инференса? Что происходит с этим знанием, когда контекст заполняется новыми токенами?

---

## Q003
type: fact
difficulty: junior
key_concepts: [Dense Attention, квадратичная сложность, длина контекста, физическое ограничение]
optional_concepts: [batch size GPT-3]
text: Почему Dense Attention из GPT-1/2 физически не позволял работать с очень длинными контекстами? Что именно растёт квадратично — память, вычисления, или и то и другое?
follow_up:
  - Предположим, удвоение длины контекста с 1024 до 2048 токенов: во сколько раз возрастут затраты памяти при Dense Attention? А при Sparse Attention?

---

## Q004
type: compare
difficulty: middle
key_concepts: [файнтюнинг обновляет веса, in-context learning замораживает веса, количество примеров, применимость]
optional_concepts: [Few-Shot ~32 примера vs тысячи размеченных для файнтюнинга]
text: Сравните парадигмы Fine-Tuning (GPT-1/BERT эпоха) и In-Context Learning (GPT-3). В каком случае каждый подход предпочтительнее с практической точки зрения? Какова цена каждого?
follow_up:
  - Представьте, что у вас есть 500 размеченных примеров и бюджет на GPU на 8 часов. Что вы выберете — файнтюнинг небольшой модели или Few-Shot на GPT-3-scale модели — и почему?
  - Как меняется ваш выбор, если завтра распределение данных резко изменится (data drift)?

---

## Q005
type: chain
difficulty: middle
key_concepts: [emergent abilities, абстрактные концепты, обобщение, не зазубривание]
optional_concepts: [перплексия, статистическое продолжение паттерна]
text: GPT-3 корректно использует выдуманное слово «блупу», встретив его только в промпте. Что это доказывает о природе обучения модели? Как это связано с механизмом In-Context Learning?
follow_up:
  - Если модель оперирует абстрактными концептами «на лету», почему она иногда уверенно галлюцинирует факты — например, называет несуществующие источники? Нет ли здесь противоречия?
  - Как бы вы разработали тест, чтобы отличить истинное обобщение от «подбора статистически вероятного паттерна» в Few-Shot задаче?

---

## Q006
type: scenario
difficulty: middle
key_concepts: [zero-shot качество ниже few-shot, примеры в промпте, формат паттерна, добавление примеров]
optional_concepts: [32 примера как практический ориентир]
text: Команда задеплоила GPT-3-scale модель для классификации тональности отзывов. Промпт: «Определи тональность отзыва: позитивная или негативная. Отзыв: {текст}». Точность — 61%, что хуже логистической регрессии. Продуктовый менеджер требует объяснений. Что, скорее всего, не так, и как это исправить без переобучения модели?
follow_up:
  - После добавления 32 примеров точность выросла до 84%. Но примеры пришлось добавить прямо в промпт, и теперь он занимает 80% контекстного окна. Как это ограничивает систему в продакшене?

---

## Q007
type: tradeoff
difficulty: middle
key_concepts: [Sparse Attention аппроксимация, Flash Attention точный результат, VRAM, аппаратные оптимизации]
optional_concepts: [HBM vs SRAM, 10× экономия памяти Flash Attention]
text: Вам нужно обслуживать модель с контекстом 32K токенов на GPU с ограниченной VRAM. Вы выбираете между Sparse Attention и Flash Attention. Аргументируйте выбор, явно назвав цену каждого решения.
follow_up:
  - Flash Attention математически эквивалентен Dense Attention, то есть его сложность всё равно O(N²). Тогда почему он вытеснил Sparse Attention, который даёт реальную алгоритмическую экономию O(N√N)?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [контекстное окно как ограниченный ресурс, управление промптом в продакшене, latency vs качество, stateless инференс]
optional_concepts: [KV-cache, батчирование запросов]
text: Вы проектируете production-систему на базе GPT-3-scale модели для многошагового диалогового агента. Контекстное окно — 4096 токенов. Пользователи ведут сессии по 50–100 обменов репликами. Опишите архитектуру управления контекстом: что происходит, когда история диалога переполняет окно, как вы обеспечиваете консистентность ответов и какие компромиссы принимаете?
follow_up:
  - В пиковые часы нагрузка возрастает в 20 раз. Батчирование запросов снижает latency, но пользователи жалуются на деградацию качества ответов. Назовите неочевидную причину, связанную именно со структурой промпта при батчировании.
  - Как изменится ваша архитектура, если регулятор обяжет вас логировать и хранить полные промпты всех пользователей 90 дней?
