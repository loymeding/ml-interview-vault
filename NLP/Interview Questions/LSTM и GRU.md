---
topic: LSTM и GRU
card_ref: "[[LSTM и GRU]]"
generated: 2026-05-14
---

## Q001
type: fact
difficulty: junior
key_concepts: [Cell State, затухание градиентов, линейная передача информации, вентили]
optional_concepts: [аналогия с residual connections]
text: Объясните, почему в классической RNN возникает проблема затухающих градиентов и как архитектура LSTM конструктивно её решает на уровне вычислительного графа?
follow_up:
  - Механизм обновления Cell State в LSTM часто сравнивают с residual connections в ResNet. В чём именно состоит эта аналогия и где она перестаёт работать?

---

## Q002
type: compare
difficulty: junior
key_concepts: [forget gate, input gate, output gate, update gate, reset gate]
optional_concepts: [количество параметров, скорость обучения]
text: Перечислите вентили LSTM и GRU и объясните, какие два вентиля LSTM «слились» в один Update gate в GRU. Какой практический выигрыш это даёт?
follow_up:
  - Если GRU меньше и быстрее, почему LSTM до сих пор используется на практике? В каком классе задач разница в качестве становится ощутимой?

---

## Q003
type: trap
difficulty: middle
key_concepts: [инициализация скрытых состояний нулями, отсутствие начального контекста, семантика нейтрального старта]
optional_concepts: [learned initialization, warm-up токены]
text: Коллега реализовал языковую модель на LSTM и инициализировал начальные скрытые состояния `h_0` и `c_0` случайным шумом из нормального распределения, мотивируя это «разнообразием стартовых условий». Что здесь концептуально неверно и к чему это приведёт на практике?
follow_up:
  - Существуют сценарии, где инициализация нулями тоже не оптимальна — например, когда модель дообучается на новом домене. Как правильно инициализировать состояния в таком случае?

---

## Q004
type: chain
difficulty: middle
key_concepts: [область значений sigmoid [0,1], область значений tanh [-1,1], семантика маскирования, центрирование данных]
optional_concepts: [стабилизация градиентов через tanh]
text: Почему во всех вентилях (forget, input, output, update, reset) используется функция активации sigmoid, тогда как для формирования кандидатов памяти — tanh? Что произошло бы, если бы вентили использовали tanh?
follow_up:
  - Если заменить tanh в кандидате памяти на ReLU, что теоретически изменится в динамике обучения? Назовите хотя бы один риск и один потенциальный плюс.

---

## Q005
type: scenario
difficulty: middle
key_concepts: [утечка данных, темпоральное разбиение выборки, look-ahead bias, последовательность данных]
optional_concepts: [feature engineering с будущими значениями]
text: Команда обучила GRU для прогнозирования оттока клиентов по временным рядам действий пользователя. Метрики на валидации идеальные — ROC-AUC 0.97. После деплоя в продакшн модель показывает ROC-AUC 0.61. Архитектура и гиперпараметры выглядят корректно. Какие конкретные ошибки в ML-пайплайне вы бы проверили в первую очередь?
follow_up:
  - Вы нашли причину: данные были перемешаны случайно перед разбивкой на train/val, а не разбиты по времени. Как правильно организовать темпоральный сплит для задачи с рекуррентной моделью, если пользователи появляются в данных в разные периоды?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [количество параметров LSTM vs GRU, скорость обучения, длинные зависимости, вычислительный бюджет]
optional_concepts: [эмпирические результаты сравнения на бенчмарках]
text: Вам нужно обучить модель для предсказания следующего слова в коротких технических тикетах (длина последовательности 20–50 токенов). Датасет — 500 тысяч примеров, время на эксперимент ограничено. Аргументируйте выбор между LSTM и GRU в этих условиях.
follow_up:
  - Условия изменились: тикеты стали содержать многостраничные описания инцидентов (500–1000 токенов). Меняет ли это ваш выбор архитектуры? Почему?

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка статистики нормализации, fit только на train-выборке, темпоральная корреляция в признаках]
optional_concepts: [layer normalization внутри RNN, online normalization]
text: Перед вами пайплайн: (1) загружаются все временные ряды, (2) вычисляется mean и std по всему датасету, (3) данные нормализуются, (4) производится разбивка 80/20 по времени, (5) LSTM обучается на train, оценивается на test. Метрики на тесте выглядят подозрительно хорошо. Найдите ошибку и объясните, как она искажает оценку качества модели.
follow_up:
  - После исправления сплита вы обнаружили, что статистики train и test сильно расходятся из-за сезонности. Какие стратегии нормализации подходят для нестационарных временных рядов при работе с LSTM?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [stateful inference, latency, горизонтальное масштабирование, сохранение скрытых состояний]
optional_concepts: [экспорт в ONNX, квантизация весов]
text: Вас просят задеплоить двухслойный LSTM для real-time scoring потока пользовательских событий: каждое новое событие должно быть оценено за 10 мс, модель должна помнить историю сессии (до 30 минут активности). Ожидаемая нагрузка — 50 000 одновременных сессий. Опишите архитектуру инференс-сервиса: как хранить скрытые состояния, как масштабироваться и какие узкие места возникнут?
follow_up:
  - Через месяц данные задрейфовали: поведение пользователей изменилось после редизайна продукта. Как организовать мониторинг дрейфа и переобучение модели без остановки сервиса и без потери накопленных скрытых состояний активных сессий?

## Q009
type: fact
difficulty: junior
key_concepts: [Teacher Forcing, накопление ошибок предсказания, стабилизация обучения, авторегрессионная генерация]
optional_concepts: [scheduled sampling]
text: Что такое Teacher Forcing и какую конкретную проблему обучения рекуррентных сетей он решает? На каком этапе (обучение или инференс) он применяется и почему именно там?
follow_up:
  - Если Teacher Forcing настолько ускоряет и стабилизирует сходимость, почему его не применяют и на этапе инференса — ведь тогда можно было бы гарантировать «правильный» вход на каждом шаге?

---

## Q010
type: compare
difficulty: junior
key_concepts: [Cell State как долгосрочная память, Hidden State как выходное состояние, линейный поток градиентов через Cell State, вентиль выхода]
optional_concepts: [аналогия с highway network]
text: Чем Cell State ($C_t$) отличается от Hidden State ($h_t$) в LSTM — по функции, по тому как они обновляются, и по роли в решении проблемы затухающих градиентов? Почему потребовалось именно два отдельных потока, а не один?
follow_up:
  - Предположим, что на всех временных шагах Forget gate выдаёт значение близкое к 1, а Input gate — близкое к 0. Что происходит с Cell State и скрытым состоянием? Есть ли задачи, где такое устойчивое поведение вентилей желательно?

---

## Q011
type: fact
difficulty: junior
key_concepts: [двунаправленная обработка, конкатенация скрытых состояний, доступность правого контекста, авторегрессионное ограничение]
optional_concepts: [BERT как реализация двунаправленности через Self-Attention]
text: В чём принципиальное отличие двунаправленной RNN от однонаправленной? Назовите одну задачу, где Bi-RNN явно предпочтительнее, и одну задачу, где её применение концептуально невозможно — с объяснением причины.
follow_up:
  - Bi-LSTM удваивает размерность выходного вектора на каждом шаге. Как это технически сказывается на следующем слое сети и что делают на практике, если нужно сохранить исходную размерность?

---

## Q012
type: chain
difficulty: middle
key_concepts: [взрывающиеся градиенты в LSTM, gradient clipping, вентили как частичная защита, рекуррентные веса Hidden State]
optional_concepts: [норма градиента, порог clipping, gradient norm monitoring]
text: Вы объясняете коллеге, что LSTM решает проблему затухающих градиентов через Cell State. Он делает вывод: «Значит, в LSTM градиенты никогда не взрываются — ведь информация течёт линейно». Прав ли он? Если нет — объясните, откуда в LSTM могут появиться взрывающиеся градиенты и как с этим бороться на практике.
follow_up:
  - Чем gradient clipping по норме вектора градиентов отличается от clipping по значению каждого элемента, и какой из них предпочтителен для RNN — почему?
  - Вы применили gradient clipping, но Loss всё равно расходится через несколько тысяч шагов. Какие ещё причины нестабильного обучения LSTM вы бы проверили?

---

## Q013
type: trap
difficulty: middle
key_concepts: [будущий контекст недоступен при авторегрессионной генерации, Bi-RNN требует полной последовательности на входе, утечка данных из будущего, несовместимость архитектуры с задачей]
optional_concepts: [causal masking в Трансформерах как способ сохранить направленность]
text: Продакт-менеджер просит улучшить качество языковой модели для автодополнения поисковых запросов в реальном времени. Коллега предлагает заменить однонаправленный LSTM на двунаправленный Bi-LSTM, аргументируя это тем, что «модель будет учитывать больше контекста и станет умнее». Что принципиально неверно в этом предложении?
follow_up:
  - Существует ли сценарий, в котором Bi-LSTM всё же оправдан в задаче, связанной с генерацией или ранжированием текста, — даже если конечный результат содержит токены последовательности?

---

## Q014
type: scenario
difficulty: middle
key_concepts: [дисбаланс длин последовательностей в train vs prod, влияние padding на финальное скрытое состояние, дистрибуционный сдвиг, PackedSequence и маскирование]
optional_concepts: [bucket sampling, взвешенная выборка по длине]
text: Команда обучила LSTM для классификации последовательностей событий (покупка или нет). Длины варьируются от 3 до 300 событий; батчи дополняются нулями до максимума в батче. Валидационный AUC = 0.89. После деплоя AUC упал до 0.72. Анализ логов показывает: в продакшне 80% запросов — клиенты с историей 3–10 событий, тогда как в обучающей выборке такие пользователи составляли лишь 5%. Что именно пошло не так и как исправить пайплайн?
follow_up:
  - Как технически гарантировать, что LSTM не включает padding-токены в финальное скрытое состояние, которое подаётся в классификационную голову? Что будет, если этого не сделать, — конкретно на уровне вычислений?

---

## Q015
type: compare
difficulty: middle
key_concepts: [Reset gate управляет формированием кандидата памяти, Forget gate управляет сохранением предыдущего состояния, разные точки применения в вычислительном графе, аналогия и различие по механизму]
optional_concepts: [формула h_t = (1−z_t)⊙h_{t-1} + z_t⊙h̃_t и роль r_t в h̃_t]
text: Сравните Reset gate в GRU и Forget gate в LSTM: в чём их функциональная схожесть, а в чём принципиальное различие по механизму действия и «точке применения» в процессе вычисления нового состояния памяти?
follow_up:
  - Если Reset gate в GRU на всех шагах выдаёт значение строго 0 — какое поведение модели это реализует? А если строго 1? К какой известной более простой архитектуре вырождается GRU в каждом из этих крайних случаев?

---

## Q016
type: debug
difficulty: senior
key_concepts: [exposure bias, разрыв между Teacher Forcing при обучении и авторегрессионным инференсом, накопление ошибок предсказания, Scheduled Sampling]
optional_concepts: [sequence-level training через REINFORCE, beam search как частичный паллиатив]
text: Команда обучает LSTM-декодер для абстрактивной суммаризации новостей (архитектура Seq2Seq). Teacher Forcing применяется на всём протяжении обучения. Train loss = 0.8, Validation loss = 0.85 — разрыв небольшой, признаков переобучения нет. После деплоя ROUGE-1 score неожиданно низкий: модель генерирует грамматически связные, но бессмысленные предложения, нередко зацикливаясь на повторяющихся фразах. Размер модели и объём данных увеличивали — не помогло. Назовите системную причину и опишите технические решения с их компромиссами.
follow_up:
  - Scheduled Sampling предлагается как решение Exposure Bias: опишите его механизм и объясните, какой новый компромисс он вносит в стабильность и скорость обучения по сравнению с чистым Teacher Forcing.
  - Модель зациклилась и генерирует один и тот же токен бесконечно. Кроме Exposure Bias, какие ещё технические причины на уровне декодирования могут приводить к такому вырожденному поведению при инференсе?
