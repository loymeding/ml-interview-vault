---
topic: LSTM и GRU
card_ref: "[[LSTM и GRU]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [Cell State, затухание градиентов, линейная передача информации, вентили]
optional_concepts: [аналогия с residual connections]
text: Объясните, почему в классической RNN возникает проблема затухающих градиентов и как архитектура LSTM конструктивно её решает на уровне вычислительного графа?
follow_up:
  - Механизм обновления Cell State в LSTM часто сравнивают с residual connections в ResNet. В чём именно состоит эта аналогия и где она перестаёт работать?

---

## Q002
type: compare
difficulty: junior
key_concepts: [forget gate, input gate, output gate, update gate, reset gate]
optional_concepts: [количество параметров, скорость обучения]
text: Перечислите вентили LSTM и GRU и объясните, какие два вентиля LSTM «слились» в один Update gate в GRU. Какой практический выигрыш это даёт?
follow_up:
  - Если GRU меньше и быстрее, почему LSTM до сих пор используется на практике? В каком классе задач разница в качестве становится ощутимой?

---

## Q003
type: trap
difficulty: middle
key_concepts: [инициализация скрытых состояний нулями, отсутствие начального контекста, семантика нейтрального старта]
optional_concepts: [learned initialization, warm-up токены]
text: Коллега реализовал языковую модель на LSTM и инициализировал начальные скрытые состояния `h_0` и `c_0` случайным шумом из нормального распределения, мотивируя это «разнообразием стартовых условий». Что здесь концептуально неверно и к чему это приведёт на практике?
follow_up:
  - Существуют сценарии, где инициализация нулями тоже не оптимальна — например, когда модель дообучается на новом домене. Как правильно инициализировать состояния в таком случае?

---

## Q004
type: chain
difficulty: middle
key_concepts: [область значений sigmoid [0,1], область значений tanh [-1,1], семантика маскирования, центрирование данных]
optional_concepts: [стабилизация градиентов через tanh]
text: Почему во всех вентилях (forget, input, output, update, reset) используется функция активации sigmoid, тогда как для формирования кандидатов памяти — tanh? Что произошло бы, если бы вентили использовали tanh?
follow_up:
  - Если заменить tanh в кандидате памяти на ReLU, что теоретически изменится в динамике обучения? Назовите хотя бы один риск и один потенциальный плюс.

---

## Q005
type: scenario
difficulty: middle
key_concepts: [утечка данных, темпоральное разбиение выборки, look-ahead bias, последовательность данных]
optional_concepts: [feature engineering с будущими значениями]
text: Команда обучила GRU для прогнозирования оттока клиентов по временным рядам действий пользователя. Метрики на валидации идеальные — ROC-AUC 0.97. После деплоя в продакшн модель показывает ROC-AUC 0.61. Архитектура и гиперпараметры выглядят корректно. Какие конкретные ошибки в ML-пайплайне вы бы проверили в первую очередь?
follow_up:
  - Вы нашли причину: данные были перемешаны случайно перед разбивкой на train/val, а не разбиты по времени. Как правильно организовать темпоральный сплит для задачи с рекуррентной моделью, если пользователи появляются в данных в разные периоды?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [количество параметров LSTM vs GRU, скорость обучения, длинные зависимости, вычислительный бюджет]
optional_concepts: [эмпирические результаты сравнения на бенчмарках]
text: Вам нужно обучить модель для предсказания следующего слова в коротких технических тикетах (длина последовательности 20–50 токенов). Датасет — 500 тысяч примеров, время на эксперимент ограничено. Аргументируйте выбор между LSTM и GRU в этих условиях.
follow_up:
  - Условия изменились: тикеты стали содержать многостраничные описания инцидентов (500–1000 токенов). Меняет ли это ваш выбор архитектуры? Почему?

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка статистики нормализации, fit только на train-выборке, темпоральная корреляция в признаках]
optional_concepts: [layer normalization внутри RNN, online normalization]
text: Перед вами пайплайн: (1) загружаются все временные ряды, (2) вычисляется mean и std по всему датасету, (3) данные нормализуются, (4) производится разбивка 80/20 по времени, (5) LSTM обучается на train, оценивается на test. Метрики на тесте выглядят подозрительно хорошо. Найдите ошибку и объясните, как она искажает оценку качества модели.
follow_up:
  - После исправления сплита вы обнаружили, что статистики train и test сильно расходятся из-за сезонности. Какие стратегии нормализации подходят для нестационарных временных рядов при работе с LSTM?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [stateful inference, latency, горизонтальное масштабирование, сохранение скрытых состояний]
optional_concepts: [экспорт в ONNX, квантизация весов]
text: Вас просят задеплоить двухслойный LSTM для real-time scoring потока пользовательских событий: каждое новое событие должно быть оценено за 10 мс, модель должна помнить историю сессии (до 30 минут активности). Ожидаемая нагрузка — 50 000 одновременных сессий. Опишите архитектуру инференс-сервиса: как хранить скрытые состояния, как масштабироваться и какие узкие места возникнут?
follow_up:
  - Через месяц данные задрейфовали: поведение пользователей изменилось после редизайна продукта. Как организовать мониторинг дрейфа и переобучение модели без остановки сервиса и без потери накопленных скрытых состояний активных сессий?
