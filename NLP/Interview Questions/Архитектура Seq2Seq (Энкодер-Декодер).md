---
topic: Архитектура Seq2Seq (Энкодер-Декодер)
card_ref: "[[Архитектура Seq2Seq (Энкодер-Декодер)]]"
generated: 2026-05-14
---

## Q001
type: fact
difficulty: junior
key_concepts: [контекстный вектор, скрытое состояние энкодера, информационное сжатие, инициализация декодера]
optional_concepts: [размерность вектора, LSTM vs GRU]
text: Что такое контекстный вектор в архитектуре Seq2Seq, откуда он берётся и как именно он передаётся декодеру?
follow_up:
  - Предположим, исходное предложение состоит из 200 токенов, а контекстный вектор имеет размерность 512. Как ты думаешь, достаточно ли 512 чисел, чтобы сохранить всю информацию из 200 токенов? Что при этом теряется?

---

## Q002
type: trap
difficulty: junior
key_concepts: [Teacher Forcing, exposure bias, разрыв между обучением и инференсом, накопление ошибок]
optional_concepts: [scheduled sampling]
text: Разработчик обучил Seq2Seq модель с Teacher Forcing и получил отличный loss. На инференсе он тоже решил использовать Teacher Forcing — подавать правильные токены из тестовой выборки на каждый шаг декодера, чтобы «помочь» модели. Метрика BLEU на тесте оказалась феноменальной. В чём принципиальная проблема такого подхода?
follow_up:
  - Как называется эффект, возникающий из-за разрыва между режимом обучения с Teacher Forcing и режимом свободной генерации на инференсе? Какие техники предложены для его смягчения?

---

## Q003
type: compare
difficulty: middle
key_concepts: [жадный поиск (greedy), Beam Search, ширина луча B, совокупная вероятность последовательности]
optional_concepts: [length penalty, вычислительная сложность O(B·V·T)]
text: Сравни жадный поиск (greedy decoding) и Beam Search при генерации последовательности. В каких случаях жадный поиск может неожиданно сработать лучше Beam Search?
follow_up:
  - Что произойдёт с качеством перевода и скоростью инференса, если увеличить ширину луча B с 5 до 50? Есть ли предел, после которого рост B перестаёт помогать?

---

## Q004
type: chain
difficulty: middle
key_concepts: [узкое место (bottleneck) контекстного вектора, деградация качества на длинных последовательностях, механизм внимания (attention), выравнивание (alignment)]
optional_concepts: [Bahdanau attention, dot-product attention]
text: Классический Seq2Seq без attention плохо справляется с длинными предложениями. Объясни, почему архитектурно это неизбежно, и предложи, как эту проблему решает механизм внимания.
follow_up:
  - Если механизм внимания позволяет декодеру «смотреть» на все скрытые состояния энкодера, какова вычислительная сложность такого механизма по длине входной последовательности? Как это влияет на обработку документов из тысяч токенов?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [распределение длин последовательностей, mismatch train/test, exposure bias, жадный поиск в продакшене]
optional_concepts: [temperature sampling, top-k/top-p]
text: Команда обучила Seq2Seq модель машинного перевода на корпусе коротких твитов (до 30 токенов). Loss на обучении и тесте — отличный, BLEU на тестовой выборке — 42. В продакшене модель получает пользовательские документы длиной 300–500 токенов. Переводы выходят бессвязными: модель зацикливается, повторяет одни и те же фразы и иногда выдаёт `<EOS>` уже на третьем слове. Назови как минимум две независимые причины такого поведения.
follow_up:
  - Как бы ты быстро проверил гипотезу о том, что проблема именно в длине входа, а не в качестве самой модели? Какой минимальный эксперимент поставил бы для диагностики?

---

## Q006
type: debug
difficulty: middle
key_concepts: [data leakage через Teacher Forcing, разрыв train/inference, корректное разбиение данных, смещение оценки качества]
optional_concepts: [scheduled sampling, curriculum learning]
text: Инженер реализует обучение Seq2Seq для суммаризации. В его коде DataLoader на каждом шаге декодера подаёт на вход `target[t]` — токен из эталонного резюме. При этом для валидации он использует тот же DataLoader, не переключая режим. Валидационный loss падает вместе с тренировочным, и команда радуется. На A/B тесте модель оказывается хуже baseline. Найди ошибку и объясни, почему она приводит именно к такому симптому.
follow_up:
  - Как правильно организовать цикл валидации для авторегрессионной модели, чтобы метрика честно отражала качество на инференсе? Что конкретно нужно изменить в коде?

---

## Q007
type: tradeoff
difficulty: senior
key_concepts: [latency vs quality, ширина луча B, авторегрессионная генерация, параллелизм]
optional_concepts: [speculative decoding, non-autoregressive декодирование]
text: Продуктовая команда требует, чтобы модель машинного перевода отвечала за 100 мс на предложение длиной до 30 токенов. Текущая модель с Beam Search (B=10) даёт BLEU=38 при задержке 350 мс. Инженеры предлагают два варианта: (А) уменьшить B до 1 (greedy), (Б) уменьшить размер модели вдвое при B=5. Какой вариант ты выберешь и почему? Какие дополнительные данные тебе нужны для решения?
follow_up:
  - Есть ли архитектурные альтернативы авторегрессионному декодированию, которые принципиально ускоряют инференс без деградации качества? Какие компромиссы они вносят?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [стриминговая генерация токенов, stateful декодер, горизонтальное масштабирование, batching при авторегрессии]
optional_concepts: [dynamic batching, KV-cache, continuous batching]
text: Тебе нужно задеплоить Seq2Seq модель машинного перевода как высоконагруженный сервис: 10 000 RPS, пользователи ожидают первый токен не позднее чем через 200 мс (time-to-first-token), а полный ответ — через 1 секунду. Опиши архитектуру сервиса: как организовать батчинг, как отдавать токены стримингом, какие узкие места возникнут при масштабировании?
follow_up:
  - Декодер авторегрессионен: каждый следующий токен зависит от предыдущего. Как это фундаментально ограничивает возможность параллельного батчинга по сравнению с энкодером? Какие техники позволяют частично обойти это ограничение?

## Q009
type: fact
difficulty: junior
key_concepts: [n-граммы, точность совпадения с эталоном, несколько правильных переводов, диапазон [0, 1]]
optional_concepts: [штраф за краткость (brevity penalty)]
text: Что такое метрика BLEU и почему для оценки качества машинного перевода нельзя использовать обычную точность (accuracy)?
follow_up:
  - BLEU = 0.42 — это хороший результат или плохой? От чего зависит интерпретация этого числа?
  - Модель получила BLEU = 1.0 на тестовой выборке из одного предложения. Это гарантирует идеальный перевод?

## Q010
type: fact
difficulty: junior
key_concepts: [стартовый токен SOS, токен конца последовательности EOS, условие остановки декодера, переменная длина выходной последовательности]
optional_concepts: []
text: Для чего в архитектуре Seq2Seq нужны специальные токены `<SOS>` и `<EOS>`? Что произойдёт с декодером на инференсе, если убрать токен `<EOS>` из словаря?
follow_up:
  - Что случится, если токен `<EOS>` случайно окажется в середине целевой последовательности в обучающей выборке — как это повлияет на обученную модель?

## Q011
type: trap
difficulty: junior
key_concepts: [убывающая отдача при увеличении B, компромисс качество/разнообразие, вычислительная стоимость Beam Search, оптимальное B ≈ 5–10]
optional_concepts: [нормировка по длине (length normalization)]
text: Коллега утверждает: «Чем больше ширина луча B в Beam Search, тем лучше итоговое качество перевода — поэтому нужно всегда выставлять B как можно больше». Согласен ли ты с этим? Что реально происходит при очень большом B?
follow_up:
  - При очень большом B модели машинного перевода начинают генерировать короткие и «безопасные» переводы чаще, чем при малом B. Почему это происходит и как с этим бороться?

## Q012
type: compare
difficulty: middle
key_concepts: [фиксированный контекстный вектор vs динамический, взвешенная сумма всех скрытых состояний энкодера, веса внимания α_ij, узкое горлышко]
optional_concepts: [визуализация матрицы внимания как инструмент интерпретируемости]
text: Сравни, как формируется контекстный вектор в базовой Seq2Seq и в Seq2Seq с механизмом внимания (Bahdanau). Что принципиально меняется в том, что декодер «видит» на каждом шаге генерации?
follow_up:
  - Если в модели с attention декодер всё равно инициализируется последним скрытым состоянием энкодера, а не всеми состояниями сразу — теряем ли мы что-то принципиальное от механизма внимания?
  - Можешь ли ты описать шаги вычисления весов внимания: от «сырого» score до взвешенной суммы?

## Q013
type: scenario
difficulty: middle
key_concepts: [доменный сдвиг (domain shift), распределение обучения ≠ распределению инференса, галлюцинации как симптом уверенной генерации вне домена, диагностика через анализ ошибок]
optional_concepts: [файн-тюнинг на целевом домене, back-translation для низкоресурсных доменов]
text: Команда обучила Seq2Seq модель перевода на корпусе новостных статей и получила BLEU=45 на новостном тест-сете. После деплоя для перевода юридических договоров BLEU упал до 12, а переводы содержат факты и термины, которых не было в оригинале. Назови минимум две независимые причины такого поведения и предложи способ диагностики каждой из них.
follow_up:
  - Юридические параллельные данные недоступны. Какие методы позволяют адаптировать модель к новому домену без размеченных пар перевод–оригинал?
  - Как отличить «галлюцинацию» (модель добавляет несуществующий факт) от «неточного перевода» (модель передаёт смысл иначе) при ручной проверке выборки?

## Q014
type: chain
difficulty: middle
key_concepts: [суммирование vs усреднение кросс-энтропий, смещение в пользу длинных последовательностей, маска padding-токенов, нормировка лосса по длине]
optional_concepts: [label smoothing, перплексия как нормированная мера]
text: Функция потерь Seq2Seq суммирует кросс-энтропии по всем шагам декодера. Почему это создаёт проблему при обучении на батчах с последовательностями разной длины, и как это обычно решается?
follow_up:
  - Если не исключать padding-токены при вычислении лосса, как это исказит обучение и почему модель может «научиться» предсказывать паддинги?
  - Если перейти с суммирования на усреднение по длине, как изменится эффективная скорость обучения для коротких и длинных предложений в одном батче?

## Q015
type: trap
difficulty: middle
key_concepts: [exposure bias, накопление собственных ошибок на инференсе, distribution mismatch между обучением и генерацией, scheduled sampling]
optional_concepts: []
text: Модель Seq2Seq обучена с Teacher Forcing, показывает низкий train loss. На инференсе Teacher Forcing не используется — модель генерирует в авторегрессивном режиме. На коротких предложениях качество хорошее, но на предложениях длиной 40+ токенов появляются повторы и бессмыслица. Разработчик уверен: «Teacher Forcing на инференсе мы не применяем, значит проблема не в нём». Прав ли он? В чём истинная причина деградации?
follow_up:
  - Что такое Scheduled Sampling и как он пытается смягчить exposure bias? Есть ли у этого подхода принципиальный изъян?
  - Если бы мы обучали модель только на очень коротких примерах (до 10 токенов), как бы это усугубило или смягчило проблему на длинных предложениях?

## Q016
type: tradeoff
difficulty: senior
key_concepts: [многоязычная модель с language token, интерференция языков, стоимость поддержки N отдельных моделей, zero-shot translation между парами без параллельных данных]
optional_concepts: [дисбаланс данных по языкам, catastrophic forgetting при дообучении одного языка]
text: Твоей команде нужно поддержать перевод между 10 языками (90 пар). Предложены два подхода: (А) обучить 90 отдельных двуязычных Seq2Seq моделей, (Б) обучить одну многоязычную модель с языковым токеном. Какой подход ты выберешь и при каких условиях твой выбор изменится? Какие метрики и ограничения инфраструктуры влияют на решение?
follow_up:
  - Один из 10 языков — low-resource язык с объёмом параллельных данных в 100× меньше остальных. Как это меняет выбор между подходами А и Б?
  - Многоязычную модель нужно срочно улучшить для одного языка, не потеряв качество на остальных девяти. Опиши свой план дообучения.
