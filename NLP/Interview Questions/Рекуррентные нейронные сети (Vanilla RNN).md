---
topic: Рекуррентные нейронные сети (Vanilla RNN)
card_ref: "[[Рекуррентные нейронные сети (Vanilla RNN)]]"
generated: 2026-05-14
---

## Q001
type: fact
difficulty: junior
key_concepts: [скрытое состояние h_t, марковское свойство, зависимость только от h_{t-1} и x_t]
optional_concepts: [аналогия с суффициентной статистикой]
text: Что означает «марковское свойство» применительно к обновлению скрытого состояния в Vanilla RNN? Как именно оно выражено в формуле обновления?
follow_up:
  - Если мы принимаем, что вся история «закодирована» в h_{t-1}, чем ограничена ёмкость такого кодирования при фиксированном hidden_size, скажем, 128?

---

## Q002
type: trap
difficulty: junior
key_concepts: [инициализация h_0 нулями, отсутствие информации до первого шага, нулевое среднее]
optional_concepts: [Xavier-инициализация неприменима к h_0]
text: Коллега предлагает инициализировать начальное скрытое состояние h_0 случайными значениями из нормального распределения, аргументируя это тем, что случайная инициализация помогает в обучении нейросетей. Он прав?
follow_up:
  - Существуют ли архитектурные сценарии, где ненулевая инициализация h_0 всё же обоснована? Приведите пример.

---

## Q003
type: compare
difficulty: middle
key_concepts: [фиксированный размер входа MLP, переменная длина последовательности RNN, разделяемые веса по времени, учёт порядка токенов]
optional_concepts: [parameter sharing как форма регуляризации]
text: Чем принципиально отличается подход MLP от RNN при обработке текстовых последовательностей разной длины? Почему нельзя просто «дополнить» вход до максимальной длины и подать в MLP?
follow_up:
  - Предположим, мы всё же padding-ом выровняли все предложения до 512 токенов и обучили MLP. Какие конкретные артефакты в качестве предсказаний мы, скорее всего, увидим на коротких предложениях?

---

## Q004
type: chain
difficulty: middle
key_concepts: [BPTT, затухающий градиент, перемножение якобианов по шагам времени, невозможность обновить ранние веса]
optional_concepts: [норма произведения матриц, спектральный радиус W_hh]
text: Объясните механику алгоритма BPTT: как именно градиент «течёт» от последнего шага к первому и почему при последовательности длиной 50 шагов он фактически обнуляется?
follow_up:
  - Если мы обрежем BPTT и будем распространять градиент только на последние k=10 шагов (Truncated BPTT), какую цену мы платим с точки зрения качества модели и что выигрываем?
  - Как Truncated BPTT влияет на способность модели запоминать зависимости, которые находятся дальше k шагов?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [взрыв градиентов, gradient clipping, норма градиента, расходящееся обучение]
optional_concepts: [мониторинг нормы градиента как диагностика]
text: Вы обучаете RNN на задаче предсказания временного ряда. Loss на первых 100 батчах стабильно убывает, затем резко улетает в NaN и больше не восстанавливается. Логи не показывают ошибок. Что произошло и как это исправить?
follow_up:
  - Вы добавили gradient clipping с порогом 5.0, NaN пропал, но сходимость стала очень медленной. Как подобрать оптимальный порог и на что ориентироваться?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [tanh ограничивает взрыв активаций, ReLU решает затухание в глубоких сетях, рекуррентное умножение на одни и те же веса]
optional_concepts: [leaky ReLU как компромисс, gradient clipping как частичная альтернатива]
text: Исследователь предлагает заменить tanh на ReLU в Vanilla RNN, обосновывая это тем, что ReLU устраняет затухание градиентов в глубоких сверточных сетях. Оцените это предложение: что выиграем и что потеряем конкретно в рекуррентной архитектуре?
follow_up:
  - При каком условии ReLU в RNN всё же можно использовать относительно безопасно? Подсказка: подумайте об инициализации W_hh.

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка данных через скрытое состояние, сброс h между независимыми сэмплами, stateful vs stateless режим]
optional_concepts: [batch_first, правильное разбиение на батчи при stateful-режиме]
text: Команда обучает RNN для классификации пользовательских сессий (каждая сессия — независимая последовательность кликов). Модель показывает accuracy 0.97 на валидации, но в A/B-тесте на проде деградирует до 0.61. Код использует `nn.RNN` в PyTorch, и скрытое состояние `hn` после каждого батча передаётся в следующий батч как начальное состояние. В чём ошибка и как её исправить?
follow_up:
  - После исправления метрика на проде поднялась до 0.79 — всё ещё хуже валидации. Какие ещё источники расхождения train/prod стоит проверить в контексте RNN?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [инкрементальный инференс, сохранение и передача h_t между запросами, latency vs stateless простота, управление состоянием на стороне сервиса]
optional_concepts: [TTL кэша состояния, горизонтальное масштабирование и sticky sessions]
text: Вы деплоите RNN-модель для real-time скоринга потока событий от пользователей (IoT-устройства шлют событие каждые 2 секунды, 100 000 устройств одновременно). Бизнес требует, чтобы каждое новое событие мгновенно учитывалось в скоре без переобработки всей истории. Спроектируйте архитектуру инференса: где и как хранить скрытое состояние h_t для каждого устройства, и какие проблемы возникнут при горизонтальном масштабировании?
follow_up:
  - Устройство может пропасть на 30 минут (плохой сигнал) и затем возобновить поток. Как должна вести себя система с кэшированным h_t: сбросить до нулей или использовать старое? Аргументируйте.

## Q009
type: fact
difficulty: junior
key_concepts: [разделение параметров, независимость от длины последовательности, трансляционная инвариантность по времени, уменьшение числа параметров]
optional_concepts: [индуктивное смещение к стационарности]
text: Объясните, зачем в RNN одни и те же матрицы весов $W_{xh}$, $W_{hh}$, $W_{hy}$ применяются на каждом временном шаге, а не отдельные матрицы для каждого шага. Какие практические преимущества это даёт?
follow_up:
  - Если ввести отдельные матрицы весов для каждого шага (как в MLP, развёрнутом во времени), что изменится в требованиях к данным и в способности модели обобщаться на последовательности другой длины?

---

## Q010
type: trap
difficulty: junior
key_concepts: [последовательная зависимость h_t от h_{t-1}, невозможность параллельного вычисления скрытых состояний, sequential bottleneck]
optional_concepts: [Transformer как параллельная альтернатива]
text: Стажёр предлагает ускорить обучение RNN: «Давайте вычислим скрытые состояния для всех временных шагов батча одновременно — это же матричные операции, GPU справится мгновенно». Что не так с этой идеей?
follow_up:
  - Существуют ли архитектуры, которые умеют обрабатывать последовательности параллельно? Какую принципиальную цену они за это платят по сравнению с RNN?

---

## Q011
type: compare
difficulty: junior
key_concepts: [диапазон tanh [-1, 1] vs sigmoid [0, 1], симметрия относительно нуля, zero-centering градиентов, величина производной в центре области]
optional_concepts: [проблема смещения градиента у sigmoid]
text: Почему в формуле обновления скрытого состояния RNN используется `tanh`, а не `sigmoid`, хотя обе функции ограничивают выходные значения? В чём принципиальная разница для рекуррентной архитектуры?
follow_up:
  - Если бы вы заменили `tanh` на `sigmoid` в hidden state, как это сказалось бы на динамике скрытых состояний при длинных последовательностях — и почему именно так?

---

## Q012
type: chain
difficulty: middle
key_concepts: [иерархические временные признаки, усиление затухания градиентов по глубине, два потока градиентов (по времени и по глубине), рост числа параметров]
optional_concepts: [dropout между слоями, residual connections в глубоких RNN]
text: Коллега предлагает «сделать RNN глубже» — добавить второй и третий рекуррентный слой поверх первого. Разверните цепочку последствий: какие задачи это поможет решить и какие новые проблемы появятся?
follow_up:
  - При добавлении второго рекуррентного слоя возникают два потока градиентов: по времени внутри каждого слоя и по глубине между слоями. Какой из них затухает быстрее и почему?
  - Как можно смягчить проблему затухания в глубокой RNN, не меняя базовую ячейку на LSTM или GRU?

---

## Q013
type: scenario
difficulty: middle
key_concepts: [обрезание последовательностей до max_seq_len, distribution shift по длине, потеря контекста длинных документов, max_seq_len как скрытый гиперпараметр]
optional_concepts: [dynamic padding, bucket batching]
text: Команда обучила RNN-классификатор на корпусе, где 99% текстов содержат не более 100 токенов — все длинные последовательности обрезались до этой длины. F1 на тесте составил 0.91. После деплоя выяснилось, что 20% продакшн-трафика — юридические документы длиной 500–800 токенов. F1 в проде упал до 0.54. Что произошло и как это исправить?
follow_up:
  - Переобучить модель на длинных документах невозможно — разметки нет. Какие архитектурные или эвристические решения позволят хотя бы частично восстановить качество на длинных текстах без переобучения?

---

## Q014
type: trap
difficulty: middle
key_concepts: [утечка данных из будущего, bidirectional RNN требует полной последовательности заранее, каузальность, online vs offline inference]
optional_concepts: [look-ahead bias в финансовых временных рядах]
text: Аналитик предлагает использовать двунаправленную (Bidirectional) RNN для прогнозирования продаж на следующую неделю: «Она видит контекст с обеих сторон — значит, лучше понимает паттерны в данных». Оцените это предложение.
follow_up:
  - В каком классе задач Bidirectional RNN полностью оправдана и не создаёт проблем с утечкой данных из будущего? Приведите конкретный пример.

---

## Q015
type: compare
difficulty: middle
key_concepts: [Truncated BPTT как аппроксимация полного градиента, экономия памяти и вычислений, потеря долгосрочных зависимостей за пределами окна k, размер окна как гиперпараметр]
optional_concepts: [stateful vs stateless обучение, качество аппроксимации градиента]
text: При обучении RNN на очень длинных последовательностях (например, 10 000 шагов) применяют Truncated BPTT — обратное распространение только через последние $k$ шагов, а не через всю последовательность. Сравните этот подход с полным BPTT: что выигрываем и что теряем?
follow_up:
  - Как выбор размера окна $k$ влияет на способность модели улавливать долгосрочные зависимости? Есть ли теоретически обоснованный способ подобрать $k$, или это чисто эмпирический гиперпараметр?

---

## Q016
type: chain
difficulty: senior
key_concepts: [растрата памяти на padding-токены, некорректный градиент через padding, PackedSequence в PyTorch, masking при вычислении loss]
optional_concepts: [bucket batching и его влияние на репрезентативность батча, динамический padding]
text: Вы обучаете RNN на датасете с сильно варьирующейся длиной последовательностей (от 5 до 2000 токенов). Наивный подход — дополнить все последовательности нулями до длины 2000. Разверните цепочку проблем этого решения: от расхода памяти до корректности градиентов и итогового качества модели.
follow_up:
  - Коллега предлагает bucket batching: сортировать последовательности по длине и формировать батчи из похожих по длине примеров. Это решает проблему с памятью, но какие новые риски для обучения это создаёт?
  - Как `nn.utils.rnn.pack_padded_sequence` в PyTorch решает проблему градиентов через padding-токены? Что конкретно происходит внутри рекуррентного слоя при использовании упакованной последовательности?
