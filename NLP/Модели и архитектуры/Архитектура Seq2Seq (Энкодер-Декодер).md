---
tags: [нейронные-сети, rnn, seq2seq, энкодер-декодер, машинный-перевод, nlp]
тип: theory
уровень: middle
сложность: средняя
статус: готово
готовность: 90
создано: 2026-04-27
источники:
  - "Нейросети для работы с последовательностями"
  - "Лекция. Машинный перевод, архитектура Seq2Seq"
предпосылки:
  - "Рекуррентные нейронные сети (Vanilla RNN)"
  - "LSTM и GRU"
  - "Языковое моделирование и алгоритмы генерации текста"
связано:
  - "Механизм внимания (Attention и Self-Attention)"
  - "Продвинутые Seq2Seq (T5, BART) и RAG-системы"
сравнить-с: []
---

# Архитектура Seq2Seq (Энкодер-Декодер)

> [!abstract] Суть
> Архитектура Seq2Seq предназначена для перевода одной последовательности в другую (когда их длины не совпадают, например, при машинном переводе). Она состоит из двух частей: **энкодера**, который сжимает исходный текст в единый контекстный вектор, и **декодера**, который на основе этого вектора пошагово генерирует новый текст.

## Ответ

**Проблема генерации последовательностей**
Базовые RNN отлично справляются с задачами формата many-to-one (классификация текста) или синхронного many-to-many (где длина входа равна длине выхода, например, определение частей речи). Однако для задач машинного перевода они не подходят: мы заранее не знаем длину перевода, и между словами оригинала и перевода нет взаимно однозначного соответствия. Решением стала архитектура Seq2Seq (Sequence-to-Sequence).

**1. Энкодер (Кодировщик)**
Это рекуррентная сеть (обычно LSTM или GRU), которая читает исходное предложение токен за токеном. Её задача — «впитать» в себя смысл всего предложения. Скрытое состояние (hidden state) самого последнего рекуррентного блока становится **контекстным вектором**. 
*Интересный хак:* Часто энкодер читает предложение задом наперёд. Это делается для того, чтобы последние слова, которые увидит энкодер (и которые сильнее всего отразятся в векторе контекста), были первыми словами оригинального предложения, что сильно упрощает декодеру старт генерации.

**2. Декодер (Декодировщик)**
Это вторая рекуррентная сеть, которая генерирует итоговый текст. Скрытое состояние её нулевого блока инициализируется контекстным вектором, полученным от энкодера — таким образом декодер получает «сжатое представление» оригинала. 
Генерация идёт пошагово: на вход подаётся специальный токен начала строки (например, `<SOS>` или `begin of sentence`), декодер предсказывает первое слово перевода, затем предсказанное слово подаётся на вход следующему шагу, и так до тех пор, пока декодер не выдаст токен конца строки `<EOS>`.

**Тонкости инференса: Beam Search**
На этапе генерации логично было бы на каждом шаге просто выбирать слово с максимальной вероятностью (жадная стратегия). Но это может завести модель в тупик: самое вероятное слово на текущем шаге может привести к маловероятному или семантически неверному продолжению всей фразы. Поэтому используют **Beam Search**: алгоритм на каждом шаге поддерживает в памяти $B$ (обычно около 10) самых вероятных гипотез-цепочек и развивает их параллельно, выбирая в конце ту, у которой максимальна совокупная вероятность всей последовательности.

**Проблема узкого горлышка и классический Attention (Bahdanau, 2014)**
Базовая Seq2Seq жмёт всю информацию о предложении из 50 слов в один вектор фиксированной длины. Для длинных предложений это ведёт к неизбежной потере деталей о первых словах. Механизм внимания решает это, давая декодеру доступ ко **всем** скрытым состояниям энкодера на каждом шаге:

1. Текущее состояние декодера $s_i$ сравнивается с каждым состоянием энкодера $h_j$ → получаем «сырые» оценки важности
2. Оценки нормируются Softmax → веса $\alpha_{ij}$ (в сумме дают 1)
3. Формируется новый вектор контекста $c_i$ специально для шага $i$ — взвешенная сумма всех $h_j$

Когда декодер генерирует слово «pomme» (яблоко), механизм автоматически назначает максимальный вес английскому «apple».

**Метрики качества машинного перевода**
Accuracy и кросс-энтропия плохо оценивают перевод — одну фразу можно перевести десятью правильными способами. Ниже — эволюция метрик от примитивных к современным.

**Базовые метрики (уровень слов):** Precision, Recall, F-мера и расстояние Левенштейна. Фундаментальный изъян: смотрят на слова изолированно и **не учитывают порядок слов** — текст из правильных, но перемешанных слов получит высокий score.

**BLEU (Bilingual Evaluation Understudy)** — долгий индустриальный стандарт. Считает точность совпадения n-грамм (1, 2, 3 и 4-граммы) между переводом и эталоном, усредняет их и добавляет **Brevity Penalty** — штраф за краткость (чтобы нельзя было схитрить, выдав одно правильное слово). BLEU ∈ [0, 1].

*Проблема BLEU:* требует **точного (буквального) совпадения** — не понимает синонимов и парафраза. На современных моделях перестаёт коррелировать с реальным качеством.

**Нейросетевые метрики (современный стандарт):**
- **BLEURT, COMET** — оценивают *семантическую близость* к эталону через Transformer-эмбеддинги. Даже при использовании других слов, но с тем же смыслом — высокий score.
- **COMET-QE (Quality Estimation)** — оценивает качество **без эталона**: модель предсказывает оценку только по паре «оригинал – перевод». Критически важно там, где у аннотаторов нет времени создавать референсы для каждого примера.
- **GEMBA** — использует LLM (GPT-4) для поиска конкретных ошибок в переводе.

**Ручная оценка — Золотой стандарт:**
- **Side-by-Side (SbS):** человек-асессор сравнивает два перевода и выбирает лучший; отлично для A/B-тестов, но не даёт абсолютной цифры.
- **MQM (Error Marking):** асессор не ставит оценку от 0 до 100 (высокая субъективность), а находит конкретные ошибки и указывает критичность. **Major** (смысловые искажения, штраф = 5) и **Minor** (стиль, шероховатости, штраф = 1). Итоговый score = сумма штрафов (меньше = лучше). Наиболее объективный и интерпретируемый подход.

**Тонкости обучения: Teacher Forcing**
При обучение декодера возникает проблема: если на ранних этапах обучения модель случайно предскажет неверное слово, то на следующем шаге это ошибочное слово будет подано ей на вход, и модель будет учиться генерировать правильный перевод, опираясь на мусорный контекст. Чтобы этого избежать, используют **Teacher Forcing** (принудительное обучение учителем): на этапе тренировки на вход следующего шага декодера подаётся не то, что он сам предсказал шаг назад, а *истинный* токен из обучающей выборки.

## Формула / Схема

Общая схема работы Seq2Seq:
$$[x_1, x_2, x_3] \xrightarrow{\text{Encoder}} \text{Context Vector} \xrightarrow{\text{Decoder}} [y_1, y_2]$$
Целевая функция обучения — минимизация суммы кросс-энтропий:
$$\Large Loss = - \sum_{t=1}^{T} \log p(y_t \mid X, y_{<t})$$

где:
- $T$ — длина целевой последовательности (число токенов в переводе)
- $y_t$ — истинный $t$-й токен целевой последовательности
- $y_{<t}$ — все истинные токены до позиции $t$ (Teacher Forcing при обучении)
- $X$ — исходная последовательность (вход энкодера)
- $p(\cdot)$ — вероятность следующего токена, предсказанная декодером

**Механизм классического Attention (Bahdanau / Luong):**

Шаг 1 — вычислить «сырой» score (насколько состояние $h_j$ важно для текущего шага $s_i$):
$$\Large e_{ij} = v^T \tanh(W_1 s_i + W_2 h_j) \quad \text{(Additive / Bahdanau)}$$
$$\Large e_{ij} = s_i^T \cdot h_j \quad \text{(Dot-Product / Luong)}$$

где:
- $s_i$ — текущее скрытое состояние декодера на шаге $i$
- $h_j$ — скрытое состояние энкодера на позиции $j$
- $v, W_1, W_2$ — обучаемые параметры (только в Bahdanau)

Шаг 2 — нормализовать в веса:
$$\Large \alpha_{ij} = \text{Softmax}(e_{ij})$$

Шаг 3 — взвешенная сумма → вектор контекста для шага $i$:
$$\Large c_i = \sum_{j} \alpha_{ij} h_j$$

## Короткий пример

Если мы переводим фразу «Я не люблю ML», энкодер «проглотит» эти 4 токена и сформирует вектор размерности, скажем, 512. Декодер получит вектор 512 и токен `<SOS>`. На первом шаге он предскажет «I», на втором шаге (получив на вход «I») предскажет «don't», затем «like», «ML» и, наконец, `<EOS>`.

## Типичные ошибки

* **Использование жадного поиска (Argmax) в продакшене:** Выбор только самого вероятного следующего слова на каждом шаге генерации без использования Beam Search часто приводит к зацикливаниям модели или неестественным грамматическим конструкциям.
* **Отсутствие Teacher Forcing при обучении:** Попытка учить декодер, подавая ему на вход его же собственные сырые предсказания, сделает сходимость крайне нестабильной и долгой.

## Каверзные вопросы

> [!question] Мы используем двунаправленную RNN (Bi-RNN) в Энкодере. Можно ли также использовать двунаправленную сеть в Декодере?
> Нет. Декодер работает в **авторегрессионном** режиме: генерирует слова последовательно, слева направо. В момент генерации слова $y_t$ слов $y_{t+1}$ и далее ещё физически не существует — двигаться «справа налево» по будущему тексту невозможно.

> [!question] Можно ли использовать архитектуру энкодер-декодер для задач One-to-Many (например, Image Captioning — генерация описания по картинке)?
> Да. Для этого достаточно изменить энкодер: вместо рекуррентной сети взять свёрточную нейросеть (CNN), которая превратит изображение в плотный вектор признаков. Этот вектор точно так же подаётся декодеру в качестве контекстного вектора для инициализации его скрытого состояния, после чего декодер по словам генерирует описание.

## Проверка себя

* Зачем в оригинальной Seq2Seq статье энкодер читал исходное предложение в обратном порядке (задом наперёд)?
* Как алгоритм Beam Search помогает бороться с недостатками жадной генерации слов?
* В чём заключается суть приема Teacher Forcing и на каком этапе (обучения или инференса) он применяется?

## Предпосылки

- [[Рекуррентные нейронные сети (Vanilla RNN)]]
- [[LSTM и GRU]]

## Связано

- [[NLP/LLM и Промпт-инжиниринг/Механизм внимания (Attention и Self-Attention)]]

## Источники

- Нейросети для работы с последовательностями
- Лекция. Машинный перевод, архитектура Seq2Seq
- Тренировки по ML 2.0 Лекция 4: Машинный перевод (Яндекс)
- [Сырой текст к карточкам.txt](file:///c:/%D0%A0%D0%B0%D0%B1%D0%BE%D1%87%D0%B8%D0%B9%20%D1%81%D1%82%D0%BE%D0%BB/DataScienceDB/%D0%A1%D1%8B%D1%80%D0%BE%D0%B9%20%D1%82%D0%B5%D0%BA%D1%81%D1%82%20%D0%BA%20%D0%BA%D0%B0%D1%80%D1%82%D0%BE%D1%87%D0%BA%D0%B0%D0%BC.txt)


---
[[🗺️ Индекс|Назад к разделу]]