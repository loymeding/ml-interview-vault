---
tags: [главная]
cssclasses: [wide-page]
---

# 🏠 Deep Learning

> [!tip] Цель раздела
> Отдельный раздел для тем по Deep Learning, чтобы нейросети, архитектуры и практические DL-вопросы не смешивались с classic ML.

---

## 📊 Прогресс

```dataview
TABLE WITHOUT ID
  length(rows) as "📚 Заметок",
  length(filter(rows, (r) => r.статус = "готово")) as "✅ Готово",
  length(filter(rows, (r) => r.статус = "черновик")) as "✏️ Черновик",
  length(filter(rows, (r) => r.сложность = "высокая")) as "🔴 Сложных"
FROM "Deep Learning"
WHERE статус
  AND file.folder != "Deep Learning"
  AND file.folder != "Deep Learning/Шаблоны"
GROUP BY true
```

---

## 📋 Следующие в очереди

```dataview
TASK FROM "Deep Learning/Банк вопросов"
WHERE !completed
LIMIT 8
```

---

## 🧭 Навигация

> [!abstract] Ключевые файлы
> [[🏠 База знаний]] — общая главная всего vault
> [[🗺️ Индекс]] — локальный индекс раздела
> [[Банк вопросов]] — очередь тем по Deep Learning
> [[📚 Источники]] — карта источников для раздела
> [[📅 По дате]] — карточки по хронологии

---

## 📂 Подразделы

- `Основы`
- `Обучение`
- `Архитектуры`
- `Компьютерное зрение`
- `NLP и Transformers`
- `Практика и инструменты`

---

## Стартовая логика изучения

1. Начать с `Основы` и `Обучение`.
2. Затем переходить к `Архитектуры`.
3. После этого дробить изучение по прикладным направлениям: `Компьютерное зрение` и `NLP и Transformers`.
4. Практические библиотеки, фреймворки и пайплайны держать в `Практика и инструменты`.
