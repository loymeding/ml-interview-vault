---
topic: Операция свёртки и её параметры
card_ref: "[[Операция свёртки и её параметры]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [parameter sharing, kernel, sliding window, reduction of parameters]
optional_concepts: [формула количества параметров]
text: Объясните принцип parameter sharing в свёрточном слое. Почему он принципиально уменьшает число параметров по сравнению с полносвязным слоем на изображении 224×224×3?
follow_up:
  - Если в слое 64 фильтра размером 3×3 и вход имеет 3 канала — сколько обучаемых параметров содержит этот слой (без bias)? Покажите расчёт.

---

## Q002
type: trap
difficulty: junior
key_concepts: [Batch Normalization, bias, избыточность параметра, вычитание среднего]
optional_concepts: [порядок слоёв Conv → BN → ReLU]
text: Коллега написал слой `nn.Conv2d(..., bias=True)`, сразу за которым стоит `nn.BatchNorm2d`. Он считает, что это безопасно — bias просто «немного помогает». В чём ошибка и зачем вообще выключать bias в такой связке?
follow_up:
  - Есть ли ситуации, когда Conv → BN, но bias всё равно имеет смысл оставить? Например, при inference с batch_size=1.

---

## Q003
type: compare
difficulty: middle
key_concepts: [stride=2, MaxPooling, уменьшение карты признаков, обучаемые параметры]
optional_concepts: [AveragePooling, информационные потери]
text: Для уменьшения пространственного разрешения карты признаков вдвое можно использовать либо свёртку со stride=2, либо слой MaxPooling 2×2. Сравните оба подхода: что обучается, что теряется, когда предпочесть один другому?
follow_up:
  - Некоторые современные архитектуры (например, All-CNN) полностью отказались от Pooling-слоёв в пользу stride. Какие практические проблемы при этом возникают?

---

## Q004
type: chain
difficulty: middle
key_concepts: [stride, размер выхода, receptive field, глубина сети]
optional_concepts: [dilated convolution как альтернатива]
text: У вас входная карта признаков 64×64. Вы хотите получить выход 8×8, не используя Pooling. Опишите, как этого достичь только через параметр stride нескольких свёрточных слоёв, и что происходит с receptive field при каждом шаге.
follow_up:
  - Если вместо увеличения stride вы используете dilated convolution (atrous) с dilation=2, как изменится receptive field и размер выхода? Почему это полезно в задачах сегментации?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [инвариантность к сдвигу, пространственные связи, утечка данных в тест, distribution shift]
optional_concepts: [аугментация, train/test split по времени или источнику]
text: Команда обучила CNN для детекции дефектов на конвейерной ленте. Точность на тестовой выборке — 98%. После деплоя модель стабильно промахивается. Камера осталась та же, освещение не менялось. Какие гипотезы вы проверите первыми и почему именно они связаны со свёрточной архитектурой?
follow_up:
  - Оказалось, что дефекты в train-set всегда были в центре кадра, а в продакшене они появляются у краёв. Какой параметр свёртки или какой приём обучения мог бы смягчить эту проблему? Чего всё равно не хватило бы?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [kernel size, receptive field, число параметров, вычислительная стоимость]
optional_concepts: [два последовательных 3×3 вместо одного 5×5, depthwise separable convolution]
text: Архитектор предлагает заменить два последовательных слоя Conv 3×3 одним слоем Conv 5×5, аргументируя это «тем же receptive field, но более простой конфигурацией». Вы хотите возразить. Постройте аргументацию через параметры, нелинейности и FLOPs.
follow_up:
  - Предложите третий вариант — depthwise separable convolution с тем же receptive field. Как изменится число параметров и в каком случае вы откажетесь от него несмотря на выигрыш?

---

## Q007
type: debug
difficulty: senior
key_concepts: [форма входного тензора, нормализация данных, утечка данных, воспроизводимость]
optional_concepts: [gradient flow, weight initialization]
text: Пайплайн: изображения загружаются, нормализуются `(pixel / 255.0)`, подаются в CNN. Loss стабильно убывает на train. На val метрика не растёт с эпохи 1. Ниже — фрагмент кода подготовки данных. Найдите ошибку и объясните, почему она специфична именно для свёрточных сетей:
```python
transform = transforms.Compose([
    transforms.Resize((224, 224)),
    transforms.ToTensor(),          # нормирует в [0,1]
    transforms.Normalize(mean=[0.485, 0.456, 0.406],
                         std=[0.229, 0.224, 0.225])
])

# val_loader использует тот же transform, но:
val_dataset = datasets.ImageFolder('val/', transform=None)
```
follow_up:
  - Представьте, что transform был применён корректно к обоим split-ам, но mean и std были посчитаны по val-выборке, а не по train. К каким последствиям это приведёт и как свёрточные слои в начале сети отреагируют на смещение входного распределения?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [inference latency, memory footprint, quantization, batch size в продакшене]
optional_concepts: [TorchScript / ONNX export, kernel fusion]
text: Вы деплоите CNN для real-time анализа видеопотока (30 FPS, 1080p). Модель содержит 20 свёрточных слоёв. На GPU A100 latency одного кадра — 40 мс, что слишком медленно. Опишите системный план оптимизации, начиная с уровня свёрточных операций и заканчивая инфраструктурными решениями. Какие компромиссы вы примете осознанно?
follow_up:
  - После квантизации INT8 точность упала на 3%. Заказчик не принимает деградацию выше 1%. Вы не можете поменять железо. Какие техники на уровне архитектуры свёрточных слоёв помогут вернуть точность без возврата к FP32?
