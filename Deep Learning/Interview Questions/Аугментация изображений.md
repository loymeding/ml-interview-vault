---
topic: Аугментация изображений
card_ref: "[[Аугментация изображений]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [аугментация как регуляризация, искусственное увеличение выборки, устойчивость к вариациям]
optional_concepts: [переобучение на обучающей выборке]
text: Объясни, зачем нужна аугментация изображений. Что именно она решает с точки зрения обучения модели?
follow_up:
  - Если у нас уже есть 1 миллион уникальных изображений в обучающей выборке — нужна ли всё равно аугментация? Аргументируй.

---

## Q002
type: trap
difficulty: junior
key_concepts: [аугментация только на train, детерминированный пайплайн на test, утечка случайности в оценку]
optional_concepts: [Test-Time Augmentation как исключение]
text: Коллега говорит: «Чем больше аугментации — тем лучше, поэтому я добавил RandomHorizontalFlip и ColorJitter в пайплайн и для train, и для test». Что здесь не так?
follow_up:
  - Есть техника Test-Time Augmentation (TTA), где аугментация применяется именно на тесте. Как она работает и не противоречит ли это тому, что ты только что сказал?

---

## Q003
type: compare
difficulty: middle
key_concepts: [Cutout — маскирование части объекта, Mixup — интерполяция между примерами, сглаживание функции потерь, регуляризация]
optional_concepts: [Beta-распределение в Mixup, мягкие метки (soft labels)]
text: Сравни аугментации Cutout и Mixup: в чём принципиальное различие их механизма действия и какую проблему каждая из них решает лучше?
follow_up:
  - Можно ли применять Cutout и Mixup одновременно в одном пайплайне? Есть ли риски взаимной нейтрализации их эффектов?

---

## Q004
type: scenario
difficulty: middle
key_concepts: [логика предметной области, семантически недопустимые преобразования, смещение распределения данных]
optional_concepts: [class confusion при неверной аугментации]
text: Команда обучила детектор дорожных знаков. На валидации accuracy = 94%, но в продакшене модель часто ошибается на знаках «Главная дорога» и «Уступи дорогу» — они почти зеркальны. Выяснилось, что в пайплайне был включён RandomVerticalFlip. Почему это проблема и как она привела именно к таким ошибкам?
follow_up:
  - Как бы ты аудировал пайплайн аугментации перед запуском обучения, чтобы поймать подобные ошибки на раннем этапе?

---

## Q005
type: chain
difficulty: middle
key_concepts: [выбор аугментаций под домен, физическая допустимость преобразований, баланс между разнообразием и смыслом]
optional_concepts: [AutoAugment, RandAugment как автоматический подбор]
text: Тебе дали задачу — классифицировать гистологические снимки тканей под микроскопом (патологоанатомия). С чего начнёшь подбор аугментаций и как будешь рассуждать о том, что применять допустимо, а что нет?
follow_up:
  - Предположим, врачи сообщили, что цвет окраски среза критически важен для диагностики. Как это ограничение меняет твой выбор цветовых аугментаций?
  - Если размеченных данных всего 500 изображений, как это влияет на стратегию и интенсивность аугментации?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [вычислительная стоимость аугментации, скорость итерации vs качество регуляризации, online vs offline аугментация]
optional_concepts: [GPU bottleneck при CPU-аугментации, кеширование аугментированных данных]
text: У тебя есть два варианта: (A) применять тяжёлые аугментации (Mixup + Cutout + ColorJitter) онлайн на каждой эпохе, (B) предгенерировать аугментированный датасет в 5× размере и обучаться на нём. Какой вариант выбрать и при каких условиях ответ меняется?
follow_up:
  - Если обучение идёт на слабом CPU и одной GPU, и GPU простаивает 40% времени в ожидании данных — как это меняет твой выбор и что ты сделаешь?

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка данных через аугментацию до сплита, завышенная метрика на валидации, независимость train/val пайплайнов]
optional_concepts: [воспроизводимость через seed, стратифицированный сплит]
text: Посмотри на этот пайплайн и найди скрытую ошибку:

```python
# Загружаем все данные
all_images = load_dataset("cats_vs_dogs")

# Аугментируем весь датасет
augmented = []
for img in all_images:
    augmented.append(img)
    augmented.append(horizontal_flip(img))
    augmented.append(color_jitter(img))

# Делаем сплит
train, val = train_test_split(augmented, test_size=0.2, random_state=42)

# Обучаем модель
model.fit(train, val)
```

Validation accuracy = 99%. Что здесь не так и почему метрика такая высокая?
follow_up:
  - Как правильно переписать этот пайплайн? Опиши порядок шагов.
  - Если ошибку не поймать до деплоя — как она проявится в продакшене и по каким признакам её можно диагностировать постфактум?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [онлайн-аугментация в инференсе (TTA), версионирование пайплайна аугментации, train/serve skew, воспроизводимость эксперимента]
optional_concepts: [feature store для предобработки, A/B тест аугментационных стратегий]
text: Твоя команда обновила пайплайн аугментации: добавили новые преобразования, изменили параметры Normalize. Модель переобучена и показывает лучшее качество в офлайн-экспериментах. Как безопасно выкатить это обновление в продакшен, учитывая, что сервис обрабатывает 10 млн изображений в день и уже работает старая версия модели?
follow_up:
  - Предположим, через неделю после деплоя метрики в продакшене начали медленно деградировать, хотя офлайн-качество осталось прежним. Какие гипотезы ты выдвинешь в первую очередь и как будешь их проверять?
