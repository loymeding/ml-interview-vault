---
topic: Полносвязные нейронные сети (MLP)
card_ref: "[[Полносвязные нейронные сети (MLP)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [нелинейность, линейная комбинация, вырождение в линейную модель]
optional_concepts: [теорема Цыбенко]
text: Объясните, почему нейронная сеть без функций активации между слоями эквивалентна одной линейной модели, сколько бы слоёв в ней ни было.
follow_up:
  - Если заменить все ReLU на тождественную функцию f(x) = x только в одном из трёх слоёв — останется ли сеть нелинейной? Почему?

---

## Q002
type: trap
difficulty: junior
key_concepts: [инициализация нулями, симметрия градиентов, breaking symmetry]
optional_concepts: [Xavier/He инициализация]
text: Стажёр инициализировал все веса сети нулями, обосновав это тем, что при обучении веса всё равно сдвинутся в нужную сторону. Где ошибка в его рассуждении?
follow_up:
  - Bias-нейроны тоже принято инициализировать нулями — это не вызывает ту же проблему симметрии? Объясните, почему bias является исключением.

---

## Q003
type: compare
difficulty: middle
key_concepts: [ширина vs глубина, иерархические признаки, число параметров, универсальная аппроксимация]
optional_concepts: [экспоненциальный рост ширины, vanishing gradient]
text: Теорема Цыбенко гарантирует, что сеть с одним широким скрытым слоем может аппроксимировать любую непрерывную функцию. Сравните такую «широкую» архитектуру с «глубокой» сетью с меньшим числом нейронов на слой: в каких случаях выгоднее каждый подход и почему на практике выбирают глубину?
follow_up:
  - Представьте задачу с жёстким ограничением на латентность инференса (< 1 мс на CPU). Как ограничение меняет ваш выбор между шириной и глубиной?

---

## Q004
type: chain
difficulty: middle
key_concepts: [forward pass, функция потерь, backpropagation, обновление весов]
optional_concepts: [цепное правило, вычислительный граф]
text: Опишите, что происходит с одним обучающим примером от момента подачи на вход сети до момента обновления весов. Пройдитесь по каждому шагу.
follow_up:
  - Допустим, вы заметили, что loss на первой эпохе падает, а потом намертво застревает. Какие причины вы проверили бы в первую очередь, опираясь на описанную вами цепочку?
  - Как изменится процесс, если вместо одного примера подать мини-батч из 32 объектов?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [утечка данных (data leakage), нормировка на тестовой выборке, train/test split]
optional_concepts: [StandardScaler fit_transform vs transform]
text: Команда обучила MLP для предсказания оттока клиентов. Accuracy на тесте — 97%, но после деплоя модель работает случайным образом. Коллега показывает вам пайплайн: данные нормируются через `StandardScaler.fit_transform` по всему датасету до разбивки на train/test, затем модель обучается на train-части. Что не так и как это исправить?
follow_up:
  - Предположим, вы исправили нормировку, но accuracy на тесте всё равно подозрительно высока — 94%. Какие другие источники утечки данных вы бы проверили в этом пайплайне?

---

## Q006
type: debug
difficulty: middle
key_concepts: [роль bias, сдвиг разделяющей гиперплоскости, обучаемые параметры]
optional_concepts: [геометрическая интерпретация, случай данных с ненулевым средним]
text: Дана сеть: данные предварительно центрированы (mean = 0), и разработчик решил убрать bias во всех слоях, чтобы сократить число параметров. На синтетических данных сеть работает отлично. При переходе на реальные данные качество резко падает. Найдите причину.
follow_up:
  - Есть ли слои, где отсутствие bias некритично или даже стандартно? Приведите пример и объясните логику.

---

## Q007
type: tradeoff
difficulty: senior
key_concepts: [полносвязные слои и высокая размерность, CNN inductive bias, число параметров, переобучение]
optional_concepts: [пространственная инвариантность, рецептивное поле]
text: Вам нужно классифицировать медицинские снимки размером 512×512 пикселей. Технический директор предлагает использовать MLP, аргументируя это тем, что «нейросеть всё равно всё выучит сама». Вы предлагаете свёрточную архитектуру. Аргументируйте свой выбор: что конкретно теряется при использовании MLP на изображениях высокого разрешения?
follow_up:
  - Предположим, датасет содержит всего 500 снимков и дообучать предобученную CNN запрещено по требованиям заказчика (данные не могут покидать изолированный контур). Меняет ли это ваш выбор архитектуры?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [батчевый инференс, latency vs throughput, сериализация модели, версионирование]
optional_concepts: [ONNX/TorchScript экспорт, A/B тестирование весов]
text: Ваш MLP обрабатывает транзакции в реальном времени: 50 000 запросов в секунду, p99-латентность не должна превышать 5 мс. Опишите, как вы задеплоите модель и что сделаете, чтобы при выходе новой версии модели не было деградации качества и сервис не лёг.
follow_up:
  - Через месяц после деплоя вы замечаете, что распределение входных признаков начало дрейфовать (data drift). Как вы это обнаружите и какие автоматические меры предусмотрите в системе?
