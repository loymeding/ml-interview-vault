---
topic: Pooling слои (Max и Average)
card_ref: "[[Pooling слои (Max и Average)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [max pooling, окно пулинга, инвариантность к сдвигу, выбор максимума]
optional_concepts: [stride, kernel_size]
text: Что такое Max Pooling и почему он обеспечивает инвариантность к небольшим сдвигам объекта на изображении?
follow_up:
  - Если объект сдвинется не на 1–2 пикселя, а на 10 — сохранится ли инвариантность? Что будет с предсказанием?

---

## Q002
type: compare
difficulty: junior
key_concepts: [max pooling, average pooling, сохранение фона, детектирование признаков]
optional_concepts: [global average pooling]
text: Чем Max Pooling отличается от Average Pooling по смыслу? В каких задачах каждый из них предпочтительнее?
follow_up:
  - Представь, что ты строишь классификатор медицинских снимков, где важно не пропустить слабые диффузные аномалии фона. Какой пулинг выберешь и почему?

---

## Q003
type: trap
difficulty: middle
key_concepts: [пулинг по пространственным осям, количество каналов неизменно, H и W уменьшаются]
optional_concepts: [формула размера выхода]
text: Коллега утверждает: «После MaxPool2d(2,2) тензор `[B, 64, 28, 28]` превращается в `[B, 32, 14, 14]` — пулинг уменьшает и пространство, и каналы». Он прав?
follow_up:
  - Если нам нужно уменьшить именно число каналов в два раза, какую операцию следует использовать вместо пулинга?

---

## Q004
type: chain
difficulty: middle
key_concepts: [global average pooling, свёртка в вектор без параметров, произвольный размер входа]
optional_concepts: [AdaptiveAvgPool2d, flatten vs GAP]
text: Зачем в современных CNN в конце ставят Global Average Pooling вместо Flatten + Dense? Что это меняет в архитектуре?
follow_up:
  - Допустим, после GAP ты всё равно добавил полносвязный слой с 1024 нейронами. Теряется ли тогда преимущество «произвольного размера входа»?
  - Как GAP влияет на интерпретируемость модели (подсказка: Class Activation Maps)?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [stride пулинга, размер выхода, несоответствие разрешения train/prod]
optional_concepts: [AdaptiveAvgPool2d, padding]
text: Модель обучена на изображениях 224×224 и показывает 97% accuracy на тесте. На проде изображения приходят в разрешении 256×256, и модель внезапно падает с ошибкой размерности. В архитектуре используется MaxPool2d(2,2) трижды и Flatten перед классификатором. В чём проблема и как её исправить?
follow_up:
  - Если заменить Flatten на Global Average Pooling, решится ли проблема? Появятся ли новые ограничения?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [обучаемые параметры, потеря информации, stride=2 в свёртке vs max pooling]
optional_concepts: [вычислительная стоимость, современные архитектуры без пулинга]
text: Перед тобой выбор: уменьшить разрешение карты признаков через `MaxPool2d(2,2)` или через `Conv2d(..., stride=2)`. Оба дают одинаковый spatial size на выходе. Как аргументированно выбрать между ними?
follow_up:
  - В условиях жёсткого ограничения по latency на мобильном устройстве какой вариант предпочтительнее и почему?

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка через train/test split, data leakage, пулинг как источник зависимости патчей]
optional_concepts: [overlapping patches, корреляция соседних окон]
text: Датасет патологических снимков разрезан на патчи 32×32 и разбит на train/val случайным образом по патчам. Модель с несколькими блоками Conv→MaxPool обучается до 99% val accuracy, но на снимках новых пациентов даёт 60%. Пайплайн технически корректен, нормировка верна. Найди источник проблемы.
follow_up:
  - Как правильно организовать split, чтобы исключить эту утечку?
  - Повлияет ли замена MaxPool на Average Pooling на severity проблемы?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [GAP для произвольного входа, батчинг изображений разного размера, AdaptiveAvgPool2d]
optional_concepts: [padding до фиксированного размера, tile-based inference, throughput vs latency]
text: Тебе нужно задеплоить CNN-классификатор документов в продакшн, где изображения приходят в произвольном разрешении от 100×100 до 4000×4000. Требование — батчинг для GPU throughput. Опиши архитектурные решения на уровне пулинговых слоёв и препроцессинга, которые позволят эффективно обрабатывать такой поток.
follow_up:
  - При очень высоком разрешении (4000×4000) GAP в конце сети «схлопнет» огромный тензор в один вектор. Какие детали могут потеряться и как это компенсировать?
  - Как изменится решение, если SLA требует < 50ms latency на запрос при пиковой нагрузке 500 rps?
