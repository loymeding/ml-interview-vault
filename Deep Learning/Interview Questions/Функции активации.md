---
topic: Функции активации
card_ref: "[[Функции активации]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [нелинейность, суперпозиция линейных функций, выразительная мощность сети]
optional_concepts: [универсальная теорема аппроксимации]
text: Что произойдёт с нейронной сетью, если убрать все функции активации из всех слоёв? Чем это плохо с математической точки зрения?
follow_up:
  - А если оставить одну функцию активации — только на последнем слое? Станет ли сеть нелинейной?

---

## Q002
type: compare
difficulty: junior
key_concepts: [центрированность относительно нуля, затухание градиентов, направление обновления весов]
optional_concepts: [зигзагообразное обновление весов]
text: Чем tanh отличается от sigmoid, и почему tanh предпочтительнее в скрытых слоях глубокой сети?
follow_up:
  - Оба страдают от затухания градиентов — почему же тогда ни sigmoid, ни tanh не используются в современных глубоких архитектурах вместо ReLU?

---

## Q003
type: trap
difficulty: middle
key_concepts: [dying ReLU, нулевой градиент при x ≤ 0, необратимость «смерти» нейрона]
optional_concepts: [Leaky ReLU как решение, learning rate как триггер]
text: Коллега говорит: «ReLU — лучшая функция активации, потому что она полностью решает проблему затухающих градиентов». Согласны ли вы с этим утверждением?
follow_up:
  - При каких конкретных условиях обучения нейрон с ReLU с наибольшей вероятностью «умрёт»? Как это связано с learning rate и инициализацией весов?

---

## Q004
type: chain
difficulty: middle
key_concepts: [dying ReLU, Leaky ReLU как решение, ненулевой градиент для отрицательной области]
optional_concepts: [параметрический PReLU, ELU]
text: Вы обучаете глубокую сеть с ReLU и замечаете, что после нескольких эпох значительная часть нейронов перестала активироваться и их выходы всегда равны нулю. Что происходит и как это исправить?
follow_up:
  - Вы заменили ReLU на Leaky ReLU с α=0.01 — проблема ушла. Как выбрать оптимальное значение α, не перебирая всё вручную?
  - Предположим, что «умерло» 80% нейронов в конкретном слое, но потери на валидации всё равно снижаются. Стоит ли вмешиваться?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [затухание градиентов, sigmoid в скрытых слоях, обновление весов, инициализация]
optional_concepts: [gradient clipping, мониторинг норм градиентов]
text: Команда обучает 10-слойную сеть для бинарной классификации. Везде стоит sigmoid. Loss на трейне падает первые 2 эпохи, затем полностью перестаёт снижаться и зависает на одном значении. Метрики случайного классификатора. Что, скорее всего, происходит и с чего начать диагностику?
follow_up:
  - Вы логируете нормы градиентов по слоям и видите, что у первых слоёв они в тысячи раз меньше, чем у последнего. Какой следующий шаг?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [dying ReLU vs вычислительная простота, Leaky ReLU overhead, скорость обучения vs стабильность]
optional_concepts: [бизнес-ограничения на latency инференса]
text: Вы строите модель для продакшена с жёстким ограничением по latency. ReLU работает быстрее, но на вашем датасете исторически «умирает» ~15% нейронов. Leaky ReLU стабильнее, но чуть медленнее. Как вы будете принимать решение — что выбрать?
follow_up:
  - А если выяснится, что latency у обоих вариантов идентична после компиляции через TorchScript? Меняет ли это ваш выбор и почему?

---

## Q007
type: debug
difficulty: senior
key_concepts: [Kaiming инициализация, дисперсия активаций, взрыв/затухание градиентов, ReLU и множитель 2]
optional_concepts: [Xavier инициализация, batch normalization как костыль]
text: Пайплайн обучения: 20-слойная сеть с ReLU, инициализация весов — Xavier (Glorot), оптимизатор Adam, batch normalization отсутствует. После первого форвард-пасса вы логируете среднее и дисперсию активаций по слоям и видите, что к 10-му слою дисперсия активаций близка к нулю. Найдите ошибку и объясните, почему именно здесь она возникает.
follow_up:
  - Вы поставили Kaiming инициализацию — дисперсия стабилизировалась. Но коллега предлагает просто добавить BatchNorm после каждого слоя вместо замены инициализации. Какие аргументы «за» и «против» такого решения?
  - Kaiming инициализация предполагает fan-in. Как она меняется для сверточных слоев по сравнению с полносвязными?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [инференс latency, аппаратная оптимизация ReLU, выбор функции активации под hardware, quantization]
optional_concepts: [GELU в трансформерах, fused kernels]
text: Вы деплоите большую модель на мобильные устройства (ARM Cortex). Архитектура использует GELU во всех скрытых слоях (как в BERT). Модель точная, но не укладывается в бюджет latency. Вам предлагают заменить GELU на ReLU. Как вы будете оценивать этот трейдофф и какой процесс принятия решения предложите команде?
follow_up:
  - После замены GELU на ReLU latency улучшилась на 30%, но качество просело на 2%. Заказчик говорит, что 2% критичны. Какие промежуточные варианты вы рассмотрите, не возвращаясь к полноценному GELU?
