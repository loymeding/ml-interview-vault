---
topic: Инициализация весов (Xavier и Kaiming)
card_ref: "[[Инициализация весов (Xavier и Kaiming)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [симметрия нейронов, одинаковые градиенты, невозможность обучения разных признаков]
optional_concepts: [проблема дисперсии]
text: Почему нельзя инициализировать все веса нейронной сети нулями? Что произойдёт при обратном распространении ошибки?
follow_up:
  - А если инициализировать не нулями, а одной и той же константой, например, 0.01 для всех весов — решит ли это проблему симметрии?

---

## Q002
type: trap
difficulty: junior
key_concepts: [ReLU обнуляет отрицательные активации, затухание дисперсии, Kaiming компенсирует потерю дисперсии]
optional_concepts: [формула Var(w) = 2/n_in]
text: Коллега говорит: «Xavier — универсальная инициализация, её можно безопасно использовать с любой функцией активации, в том числе с ReLU». Вы согласны? Что произойдёт в глубокой сети с ReLU, если применить Xavier?
follow_up:
  - Если мы всё же используем Xavier с ReLU и видим затухание сигнала, какие симптомы мы заметим при логировании norm весов и активаций по слоям?

---

## Q003
type: compare
difficulty: middle
key_concepts: [предположение о симметрии активации, компенсация обнуления ReLU, формулы дисперсии Xavier и Kaiming]
optional_concepts: [n_in vs (n_in + n_out)]
text: Сравните методы инициализации Xavier и Kaiming: в чём принципиальное различие их предпосылок, и как это различие отражается в формулах? Для каких функций активации предназначен каждый метод?
follow_up:
  - Что будет, если применить Kaiming-инициализацию к сети с функцией активации Tanh? Дисперсия сигнала вырастет или упадёт по слоям — и почему?

---

## Q004
type: chain
difficulty: middle
key_concepts: [дисперсия сигнала через слои, ReLU обнуляет ~50% активаций, накопительный эффект в глубоких сетях]
optional_concepts: [Batch Normalization как альтернативное решение]
text: Представьте 10-слойную сеть с ReLU без какой-либо нормализации. Объясните шаг за шагом: как меняется дисперсия активаций при прямом проходе, если использовать случайную нормальную инициализацию с маленьким std (например, 0.01)? К чему это приведёт при обучении?
follow_up:
  - Если добавить Batch Normalization после каждого слоя, становится ли выбор инициализации неважным? Или инициализация всё равно влияет на скорость сходимости на первых итерациях?
  - Как изменится анализ, если заменить ReLU на Leaky ReLU с alpha=0.01?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [проблема симметрии,градиенты идентичны для клонов нейронов, модель не обучается разным признакам]
optional_concepts: [диагностика через визуализацию весов и активаций]
text: Команда обучила MLP для классификации изображений. Loss на первой эпохе падает, но потом намертво застревает на значении, соответствующем случайному угадыванию (1/num_classes). Веса были инициализированы нулями. Архитектура и данные проверены — всё корректно. Почему модель не обучается? Как это диагностировать и исправить?
follow_up:
  - Если вы заменили инициализацию на случайную нормальную с std=1e-8 — проблема симметрии исчезла, но loss всё равно почти не падает. Что теперь не так?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [разные слои требуют разной инициализации, активации определяют выбор метода, риски единообразной инициализации]
optional_concepts: [поканальная инициализация, PyTorch nn.init API]
text: Вы строите сеть, где первые три слоя используют Tanh, а следующие пять — ReLU. Нужно выбрать единую стратегию инициализации для всей сети. Аргументируйте: применить Xavier ко всем слоям, Kaiming ко всем слоям, или инициализировать каждую группу слоёв отдельно своим методом? Какие риски у каждого варианта?
follow_up:
  - Как изменится ваш выбор, если это не обычный MLP, а Transformer, где есть и линейные слои с ReLU (FFN), и слои внимания без нелинейности?

---

## Q007
type: debug
difficulty: senior
key_concepts: [Kaiming предполагает fan_in, fan_out определяет направление, mode='fan_out' vs mode='fan_in']
optional_concepts: [формула Var(w) = 2/n_out для обратного прохода]
text: Инженер реализовал кастомный слой и инициализировал его так: `nn.init.kaiming_normal_(weight, mode='fan_out', nonlinearity='relu')`. Сеть обучается, но сходится значительно медленнее, чем ожидалось, особенно в первые эпохи. При логировании видно, что норма градиентов у первых слоёв аномально мала. В чём ошибка и как её исправить?
follow_up:
  - Когда `mode='fan_out'` в Kaiming всё же является правильным выбором — при каких условиях он предпочтительнее `fan_in`?
  - Если после исправления инициализации градиенты первых слоёв нормализовались, но loss стал нестабильным (скачет), что стоит проверить следующим?

---

## Q008
type: chain
difficulty: senior
key_concepts: [производная активации определяет нужный масштаб дисперсии, Xavier и Kaiming — частные случаи общего принципа, аналитический вывод формулы]
optional_concepts: [gain-параметр в PyTorch, эмпирическая калибровка через forward pass статистику]
text: Вы разрабатываете архитектуру с кастомной функцией активации `f(x) = x * sigmoid(x)` (Swish/SiLU). Ни Xavier, ни Kaiming не рассчитаны на неё напрямую. Как вы подойдёте к выбору инициализации? Опишите шаги вывода правильной дисперсии весов для этой функции активации.
follow_up:
  - PyTorch предоставляет параметр `gain` в `nn.init.xavier_normal_`. Как бы вы численно оценили правильное значение gain для Swish, если аналитический вывод затруднён?
  - Если после выбора инициализации вы добавляете LayerNorm перед каждой активацией, нужно ли пересматривать формулу инициализации — и если да, то почему?
