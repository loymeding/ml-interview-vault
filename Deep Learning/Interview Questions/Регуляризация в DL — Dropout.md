---
topic: Регуляризация в DL — Dropout
card_ref: "[[Регуляризация в DL — Dropout]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [inverted dropout, масштабирование на 1/(1-p), математическое ожидание активаций, согласованность train/inference]
optional_concepts: [формула Bernoulli-маски]
text: Зачем во время обучения с Dropout выжившие нейроны делятся на (1−p), а не просто зануляются без компенсации?
follow_up:
  - Что произойдёт с предсказаниями модели, если масштабирование сделать не на этапе обучения, а вручную на этапе инференса — и как это изменит prod-деплой?

---

## Q002
type: trap
difficulty: junior
key_concepts: [model.eval(), отключение dropout на инференсе, нестабильность предсказаний, воспроизводимость результатов]
optional_concepts: [torch.no_grad()]
text: Коллега утверждает: «Я не вызываю model.eval() перед инференсом, но в среднем по большой выборке метрика получается нормальная — значит, это не проблема». Он прав?
follow_up:
  - Придумай конкретный бизнес-сценарий, где нестабильность единичного предсказания (а не усреднённая метрика) привела бы к реальным потерям.

---

## Q003
type: compare
difficulty: middle
key_concepts: [Dropout как случайное выключение нейронов, L2-регуляризация как штраф за большие веса, механизм предотвращения переобучения, совместное использование]
optional_concepts: [коадаптация нейронов]
text: Сравни Dropout и L2-регуляризацию (weight decay): как каждый из них борется с переобучением, и есть ли смысл применять оба одновременно?
follow_up:
  - Если ты добавляешь оба метода и модель начала недообучаться — с какого из двух ты начнёшь ослаблять гиперпараметры и почему?

---

## Q004
type: chain
difficulty: middle
key_concepts: [сверточные слои vs полносвязные, количество параметров, пространственная корреляция, рекомендуемые значения p]
optional_concepts: [DropBlock как альтернатива для CNN]
text: Почему для сверточных слоёв рекомендуют p=0.1–0.2, а для полносвязных p=0.5? Объясни с точки зрения архитектуры сети.
follow_up:
  - Тебе нужно поставить Dropout в трансформере (self-attention + FFN). Куда именно ты его поместишь и с каким p — и изменится ли рассуждение, если входная последовательность очень длинная?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [model.eval(), детерминированность инференса, среднее vs единичное предсказание, воспроизводимость]
optional_concepts: [Monte Carlo Dropout]
text: Вы обучили модель для скоринга кредитных заявок. На офлайн-тесте AUC=0.91. После деплоя бизнес жалуется: один и тот же клиент при повторных запросах получает разные решения — иногда одобрение, иногда отказ. Потенциальная причина — в коде инференса. Что ты проверишь в первую очередь?
follow_up:
  - Представь, что model.eval() уже стоит, но проблема сохраняется. Какие ещё источники недетерминизма ты проверишь в пайплайне?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [переобучение vs недообучение, ёмкость модели, dropout rate как гиперпараметр, диагностика через train/val кривые]
optional_concepts: [learning rate scheduling совместно с dropout]
text: У тебя небольшой датасет (5 000 примеров) и MLP с 3 полносвязными слоями. Ты хочешь поставить Dropout p=0.5 на все слои, чтобы бороться с переобучением. Твой тимлид говорит: «Это слишком агрессивно, снизь до p=0.2». Как ты аргументируешь своё решение или соглашаешься с ним?
follow_up:
  - Какие метрики или графики ты построишь, чтобы эмпирически подтвердить правоту одной из сторон, не прибегая к полному перебору гиперпараметров?

---

## Q007
type: debug
difficulty: senior
key_concepts: [утечка данных через маску dropout, фиксация random seed, разделение train/val, детерминизм воспроизведения эксперимента]
optional_concepts: [torch.manual_seed vs numpy.random.seed]
text: Исследователь замечает странность: валидационный loss почему-то чуть ниже тренировочного на каждой эпохе, хотя модель нигде явно не видит val-данные. Архитектура: стандартный MLP с Dropout(p=0.5). В коде нет явных ошибок с индексами батчей. Что могло пойти не так?
follow_up:
  - Как ты изменишь процедуру валидации, чтобы исключить все возможные источники утечки, связанные с состоянием модели, а не только с данными?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [Monte Carlo Dropout, неопределённость предсказания (uncertainty), оставление dropout активным на инференсе, усреднение N стохастических прогонов]
optional_concepts: [Bayesian Deep Learning, калибровка вероятностей]
text: Продуктовая команда хочет, чтобы модель классификации не просто выдавала класс, но и сообщала «насколько она уверена» в предсказании — для флагирования сложных случаев на ручную проверку. Dropout уже есть в архитектуре. Как ты используешь это для оценки неопределённости без переобучения новой модели?
follow_up:
  - Сервис обрабатывает 10 000 запросов в секунду, а каждый MC Dropout прогон требует N=30 форвард-пассов. Как ты адаптируешь решение под latency-бюджет в 50 мс, не теряя качество оценки неопределённости?
  - Как ты проверишь, что полученные confidence-оценки реально коррелируют с точностью модели, а не являются артефактом архитектуры?
