---
topic: Знаковые архитектуры CNN
card_ref: "[[Знаковые архитектуры CNN]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [затухание градиентов, skip-connection, остаточный блок, тождественное преобразование]
optional_concepts: [формула производной с +1, инициализация весов нулями]
text: Какую проблему решают остаточные связи (skip-connections) в ResNet и за счёт чего это работает математически?
follow_up:
  - Что произойдёт с остаточным блоком, если веса свёрточных слоёв внутри него инициализировать нулями — как будет вести себя блок в самом начале обучения?

---

## Q002
type: compare
difficulty: junior
key_concepts: [стопка свёрток 3x3 в VGG, параллельные ветви Inception, число параметров, нелинейность]
optional_concepts: [свёртки 1x1 для снижения размерности]
text: В чём принципиальное отличие подхода VGG к построению глубокой сети от подхода GoogLeNet/Inception? Почему VGG использует именно свёртки 3×3, а не 5×5 или 7×7?
follow_up:
  - Inception также содержит свёртки 5×5 в параллельных ветвях — не противоречит ли это логике VGG? Как Inception компенсирует вычислительную стоимость этих ветвей?

---

## Q003
type: trap
difficulty: middle
key_concepts: [вспомогательные классификаторы, затухание градиентов при обучении, потери только на этапе тренировки, inference без вспомогательных ветвей]
optional_concepts: [вес потерь вспомогательного классификатора ~0.3]
text: Коллега утверждает: «Вспомогательные классификаторы в GoogLeNet используются при инференсе — их предсказания усредняются с основным выходом, что повышает итоговую точность». Согласны ли вы с этим утверждением?
follow_up:
  - Если вспомогательные классификаторы отключают на инференсе, то как авторы ResNet решили ту же проблему затухания градиентов, не прибегая к этому механизму вообще?

---

## Q004
type: scenario
difficulty: middle
key_concepts: [transfer learning, заморозка слоёв, малый датасет, предобученные веса]
optional_concepts: [выбор learning rate для head vs backbone, GlobalAveragePooling]
text: Вы Data Scientist на производстве. Задача — классифицировать дефекты деталей на конвейере (5 классов). В вашем распоряжении 4 000 размеченных изображений и 2 дня на разработку. Обучать ResNet с нуля или использовать Transfer Learning? Опишите ваш пайплайн пошагово.
follow_up:
  - Предположим, вы заморозили backbone и обучили только голову — val accuracy 71%. Коллега советует разморозить все слои и дообучить сеть целиком. Какие риски возникают и как их минимизировать?
  - Через месяц на конвейере появился новый тип дефекта, которого не было в тренировочных данных. Как обновить модель без переобучения с нуля, если новых примеров всего 200?

---

## Q005
type: chain
difficulty: middle
key_concepts: [depthwise convolution, pointwise convolution 1x1, снижение числа вычислений, разделение по каналам]
optional_concepts: [сравнение числа параметров: стандартная vs depthwise separable]
text: Объясните, чем depthwise convolution в MobileNet отличается от стандартной свёртки. Почему это снижает число вычислений?
follow_up:
  - После depthwise-свёртки каналы обрабатываются независимо — как MobileNet возвращает взаимодействие между каналами и зачем это нужно?
  - Если depthwise convolution настолько эффективна, почему ResNet и VGG до сих пор используют стандартные свёртки в большинстве применений?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [скорость инференса, точность модели, вычислительные ограничения устройства, depthwise separable convolutions]
optional_concepts: [квантизация, knowledge distillation]
text: Вы деплоите классификатор изображений в мобильное приложение. ResNet-50 даёт accuracy 78%, MobileNetV2 — 74%. Требование product-команды: ответ модели менее 100 мс на среднебюджетном Android-телефоне. ResNet-50 укладывается в 340 мс, MobileNetV2 — в 60 мс. Какую модель выбрать и как аргументировать выбор перед стейкхолдерами?
follow_up:
  - Можно ли приблизить точность MobileNetV2 к уровню ResNet-50, не нарушив ограничение по latency? Какие техники рассмотрите?

---

## Q007
type: debug
difficulty: senior
key_concepts: [transfer learning pipeline, разрушение предобученных весов высоким LR, заморозка слоёв, разница train/val accuracy]
optional_concepts: [поведение BatchNorm в режимах train/eval, поэтапное размораживание]
text: Команда обучила ResNet-50 методом transfer learning для бинарной классификации медицинских снимков. Пайплайн: загрузили веса ImageNet, заменили последний FC-слой, обучили 15 эпох с `lr=1e-3` и optimizer Adam без каких-либо изменений остальных гиперпараметров. Результат: train accuracy 99%, val accuracy 54% (при baseline случайного угадывания 50%). Команда говорит: «Нужно собрать ещё данных — явное переобучение». Найдите настоящую причину провала и предложите исправления.
follow_up:
  - После того как вы заморозили backbone и понизили learning rate, val accuracy выросла до 81%. Как решить, какие именно слои backbone стоит разморозить дальше и в каком порядке?
  - Предположим, в датасете оказалось, что изображения из val-выборки были сделаны на другом аппарате с иным контрастом. Как это меняет вашу диагностику?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [data drift, мониторинг качества модели в продакшене, pipeline переобучения, distribution shift]
optional_concepts: [continual learning, статистические тесты на дрейф данных]
text: Вы задеплоили ResNet-based классификатор товаров для e-commerce в продакшен. Модель работает 8 месяцев. Бизнес сообщает, что в последние 2 месяца точность заметно упала — клиенты жалуются на неверные категории. Логов предсказаний нет, разметки новых данных нет. Опишите систему диагностики и восстановления качества, которую вы выстроите с нуля.
follow_up:
  - Предположим, вы обнаружили, что дрейф данных связан с сезонностью: каждое лето появляются новые коллекции товаров. Как изменить архитектуру пайплайна, чтобы модель адаптировалась к этому автоматически?
  - Вам предстоит выбрать между полным переобучением ResNet раз в квартал и инкрементальным дообучением на новых данных ежемесячно. Какие риски у каждого подхода и какой KPI поможет сделать выбор?
