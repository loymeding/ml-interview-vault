---
topic: Метод обратного распространения ошибки (Backpropagation)
card_ref: "[[Метод обратного распространения ошибки (Backpropagation)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [Chain Rule, локальный градиент, глобальный градиент, обратный проход]
optional_concepts: [граф вычислений]
text: Объясните, что такое Chain Rule и зачем он нужен в алгоритме Backpropagation. Как локальный и глобальный градиенты участвуют в вычислении итогового обновления весов?
follow_up:
  - Представьте, что в середине сети стоит не дифференцируемая операция (например, `argmax`). Как это ломает стандартный Backprop и как с этим обычно борются?

---

## Q002
type: trap
difficulty: junior
key_concepts: [накопление градиентов, optimizer.zero_grad(), PyTorch, обновление весов]
optional_concepts: [gradient accumulation как намеренный приём]
text: Коллега написал цикл обучения в PyTorch и забыл вызывать `optimizer.zero_grad()` перед каждым шагом. Модель всё равно обучается и лосс падает. Значит ли это, что ошибки нет и можно оставить как есть?
follow_up:
  - В каких сценариях накопление градиентов без обнуления используется намеренно? Каковы риски, если при этом неправильно масштабировать learning rate?

---

## Q003
type: compare
difficulty: middle
key_concepts: [Forward pass, Backward pass, кэширование активаций, память vs вычисления]
optional_concepts: [gradient checkpointing]
text: Сравните Forward pass и Backward pass с точки зрения вычислительной нагрузки и потребления памяти. Почему без кэширования промежуточных активаций невозможно корректно выполнить обратный проход?
follow_up:
  - Вы обучаете очень глубокую сеть и GPU-память заканчивается именно из-за кэшированных активаций. Предложите компромиссное решение и объясните, чем придётся пожертвовать.

---

## Q004
type: chain
difficulty: middle
key_concepts: [градиент по весам, транспонирование матрицы, формула ∇W = X^T · δ, размерности тензоров]
optional_concepts: [broadcasting, батчевая обработка]
text: Объясните, почему в формуле градиента по матрице весов линейного слоя `∇W = X^T · δ` используется транспонирование входа. Как убедиться, что размерности в этом выражении согласованы?
follow_up:
  - Теперь усложним: батч состоит из $N$ примеров. Как формула меняется, и нужно ли усреднять градиент по батчу перед обновлением весов? Что произойдёт, если не усреднять при изменении размера батча вдвое?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [vanishing gradient, функция активации, насыщение нейронов, глубина сети]
optional_concepts: [инициализация весов, Batch Normalization]
text: Команда обучает 20-слойную сеть с активацией `sigmoid` на каждом слое. Лосс на первых эпохах падает, затем полностью перестаёт уменьшаться, а веса первых слоёв почти не изменяются. Тимлид говорит «просто увеличьте learning rate». Вы подозреваете другую причину. Что происходит и как это исправить?
follow_up:
  - Вы перешли на `ReLU`, и проблема частично исчезла, но теперь часть нейронов всегда выдаёт ноль и перестаёт обучаться. Как называется этот эффект и что с ним делают?

---

## Q006
type: debug
difficulty: middle
key_concepts: [утечка данных через граф, detach / stop_gradient, вычислительный граф, градиент не течёт]
optional_concepts: [requires_grad, in-place операции]
text: Разработчик реализовал кастомный слой в PyTorch. Forward pass выглядит корректно, но при вызове `.backward()` градиент до этого слоя не доходит — веса предыдущих слоёв не обновляются. При этом ошибок нет. Опишите три наиболее вероятные причины такого поведения и как каждую из них диагностировать.
follow_up:
  - Одна из причин — случайное использование `.numpy()` внутри вычислительного графа. Почему это обрывает градиенты, и как правильно конвертировать тензор без разрыва графа, если это действительно нужно?

---

## Q007
type: tradeoff
difficulty: senior
key_concepts: [gradient checkpointing, компромисс память-вычисления, recomputation, глубокие сети]
optional_concepts: [mixed precision training, offloading на CPU]
text: Вы обучаете трансформер на 48 GB GPU. Без оптимизаций модель не помещается в память из-за кэшированных активаций. Перед вами два варианта: (A) использовать gradient checkpointing — пересчитывать активации на обратном проходе вместо хранения; (B) уменьшить размер батча вдвое. Аргументированно выберите подход для сценария, где критична скорость сходимости, и для сценария, где критична пропускная способность (throughput).
follow_up:
  - Как изменится ваш выбор, если на сервере доступно 512 GB CPU RAM и поддерживается offloading? Какие новые узкие места возникают при таком подходе?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [динамический граф вычислений, autograd, инкрементальное обучение, online learning в продакшене]
optional_concepts: [катастрофическое забывание, версионирование градиентов]
text: Вы деплоите модель, которая должна дообучаться на новых данных в реальном времени (online learning) — каждые несколько секунд приходит батч из производства, модель делает backward и обновляет веса. Опишите архитектурные риски такого подхода именно с точки зрения корректности Backpropagation и управления вычислительным графом. Что может пойти не так и как это предотвратить?
follow_up:
  - Предположим, поток данных внезапно начинает содержать только один класс (label shift). Как это влияет на градиенты конкретных слоёв сети и какие метрики в мониторинге позволят поймать эту деградацию раньше, чем она скажется на бизнес-метриках?
