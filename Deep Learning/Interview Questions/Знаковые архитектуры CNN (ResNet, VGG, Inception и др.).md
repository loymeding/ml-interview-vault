---
topic: Знаковые архитектуры CNN (ResNet, VGG, Inception и др.)
card_ref: "[[Знаковые архитектуры CNN (ResNet, VGG, Inception и др.)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [декомпозиция свёрток, свёртки 3x3, количество параметров, нелинейность]
optional_concepts: [рецептивное поле]
text: Почему VGG использует стопки свёрток 3×3 вместо одной большой свёртки 5×5 или 7×7? Что конкретно это даёт?
follow_up:
  - Если замена 5×5 на две 3×3 выгодна по параметрам, почему тогда просто не использовать свёртки 1×1 везде и сделать сеть ещё дешевле?

---

## Q002
type: fact
difficulty: junior
key_concepts: [Global Average Pooling, Flatten, количество параметров, переобучение]
optional_concepts: [Network in Network]
text: Чем Global Average Pooling отличается от операции Flatten перед полносвязным слоем, и почему переход на GAP резко снижает объём модели?
follow_up:
  - Есть ли ситуации, когда GAP хуже Flatten — например, когда важна точная пространственная информация об объекте на изображении?

---

## Q003
type: compare
difficulty: middle
key_concepts: [skip connections, сложение тензоров, конкатенация тензоров, потребление памяти]
optional_concepts: [DenseNet, gradient flow]
text: В ResNet остаточные связи реализованы через поэлементное сложение, а в DenseNet — через конкатенацию каналов. Объясни принципиальную разницу и почему авторы ResNet выбрали сложение, несмотря на то что конкатенация сохраняет больше информации.
follow_up:
  - Если память GPU строго ограничена (например, 4 ГБ), а нужно максимальное качество — что выберешь: глубокий ResNet или неглубокий DenseNet? Аргументируй.

---

## Q004
type: trap
difficulty: middle
key_concepts: [свёртка 1×1, уменьшение размерности, Inception-блок, вычислительная стоимость]
optional_concepts: [bottleneck архитектура]
text: В Inception-блоке перед свёртками 3×3 и 5×5 всегда стоят свёртки 1×1. Студент утверждает: «Свёртки 1×1 — бесполезный балласт, они не меняют пространственный размер карты признаков и только добавляют лишние слои». Он прав?
follow_up:
  - Свёртка 1×1 также используется в ResNet в так называемом «bottleneck»-блоке. Какую роль она там выполняет и зачем это нужно при переходе от ResNet-34 к ResNet-50?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [transfer learning, domain shift, заморозка слоёв, тонкая настройка]
optional_concepts: [feature extractor, fine-tuning стратегия]
text: Команда взяла ResNet-50, предобученную на ImageNet, заморозила все слои кроме последнего FC, обучила на датасете рентгеновских снимков лёгких (2 класса: норма / пневмония). Accuracy на тесте — 97%. На следующий день модель завернули врачи: «Она постоянно ошибается на реальных снимках». Что, скорее всего, пошло не так, и как это диагностировать?
follow_up:
  - Предположим, что данные действительно репрезентативны, и дело в стратегии заморозки. Как изменить подход к fine-tuning, чтобы низкоуровневые признаки лучше адаптировались к медицинским изображениям?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [depthwise convolution, скорость инференса, точность модели, мобильный деплой]
optional_concepts: [latency vs accuracy Pareto-фронт]
text: Продуктовая команда просит встроить классификатор изображений прямо в мобильное приложение (Android, средний смартфон, <100 мс на кадр). У тебя есть две обученные модели: ResNet-50 (точность 92%, 25М параметров) и MobileNetV2 (точность 88%, 3.4М параметров). Что выберешь и почему? Есть ли вариант, при котором правильный ответ — ни один из двух?
follow_up:
  - Заказчик говорит: «4% точности — это слишком много терять, давай ResNet, просто сожмём его». Какие техники сжатия ты бы предложил, и какой порядок их применения наиболее разумен?

---

## Q007
type: chain
difficulty: senior
key_concepts: [остаточные связи, затухание градиентов, backpropagation, производная сложной функции]
optional_concepts: [инициализация нулями, тождественное преобразование]
text: Объясни математически, почему skip connection вида $X_{out} = F(X_{in}) + X_{in}$ решает проблему затухающих градиентов. Что именно происходит с производной при обратном распространении ошибки через такой блок?
follow_up:
  - Если при инициализации веса блока $F$ близки к нулю, блок ведёт себя как тождественное преобразование. Почему это свойство важно в начале обучения, и что произойдёт, если инициализировать веса слишком большими значениями?
  - Представь, что ты строишь сеть из 500 остаточных блоков. Градиент всё равно может деградировать? При каких условиях?

---

## Q008
type: system_design
difficulty: senior
key_concepts: [квантизация модели, батчинг инференса, версионирование моделей, latency vs throughput]
optional_concepts: [TorchScript / ONNX экспорт, model warm-up]
text: Тебе нужно задеплоить ResNet-50 в production: сервис классификации изображений товаров для маркетплейса, пиковая нагрузка — 5 000 запросов в секунду, требование по latency — p99 < 80 мс, бюджет на GPU-кластер фиксирован. Опиши архитектуру решения и ключевые технические решения по оптимизации модели и инфраструктуры.
follow_up:
  - Через три месяца дата-сайентисты обучили ResNet-50 v2 с точностью на 3% выше. Как организовать обновление модели без даунтайма и откатиться, если новая версия окажется хуже на реальном трафике?
