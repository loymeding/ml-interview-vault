---
tags: [индекс, навигация]
cssclasses: [wide-page]
---

# 🗺️ Индекс — Deep Learning

> [!abstract] Навигация
> Локальный индекс раздела Deep Learning. Новые карточки автоматически попадают сюда по мере появления.

---

## 📊 Статистика раздела

```dataview
TABLE WITHOUT ID
  length(rows) as "📚 Всего",
  length(filter(rows, (r) => r.статус = "готово")) as "✅ Готово",
  length(filter(rows, (r) => r.статус = "черновик")) as "✏️ Черновик",
  length(filter(rows, (r) => r.статус = "требует доработки")) as "⚠️ Доработки",
  length(filter(rows, (r) => r.сложность = "высокая")) as "🔴 Сложных"
FROM "Deep Learning"
WHERE статус
  AND file.folder != "Deep Learning"
  AND file.folder != "Deep Learning/Шаблоны"
GROUP BY true
```

---

## 🗂️ По разделам

### 🧠 Основы

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/Основы"
SORT file.name ASC
```

### ⚙️ Обучение

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/Обучение"
SORT file.name ASC
```

### 🏗️ Архитектуры

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/Архитектуры"
SORT file.name ASC
```

### 👁️ Компьютерное зрение

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/Компьютерное зрение"
SORT file.name ASC
```

### 📝 NLP и Transformers

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/NLP и Transformers"
SORT file.name ASC
```

### 🛠️ Практика и инструменты

```dataview
TABLE WITHOUT ID file.link as "Заметка", default(тип, "—") as "Тип", сложность, статус
FROM "Deep Learning/Практика и инструменты"
SORT file.name ASC
```
