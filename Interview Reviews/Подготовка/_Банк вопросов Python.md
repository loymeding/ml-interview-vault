---
tags: [interview-prep, question-bank, python]
type: topic-question-bank
topic: Python
status: active
created: 2026-07-13
cssclasses: [wide-page]
---

# 🐍 Банк вопросов — Python

> [!success] Развёрнутые ответы
> [[Interview Reviews/Подготовка/Python — развёрнутые ответы|Открыть карточку с готовыми объяснениями]]: ответы сгруппированы по темам ниже и покрывают все вопросы банка.

> [!tip] Фокус
> Здесь собраны вопросы, которые часто всплывают на ML/Data Science/Python-собеседованиях: не “что такое list”, а то, где проверяют понимание модели памяти, хэш-таблиц, ООП, декораторов и параллельности.

---

## 🧭 Карта блоков

| Блок | Что проверяют | Приоритет |
|---|---|---|
| Python в ML/разработке | сильные и слабые стороны языка | 🟡 средний |
| Объектная модель | ссылки, mutability, equality, copy | 🔥 высокий |
| `dict` / `set` / хэш-таблицы | коллизии, асимптотики, ключи | 🔥 высокий |
| ООП | `self`, `super`, инкапсуляция, MRO | 🔥 высокий |
| Память и GC | reference counting, циклы, generational GC | 🔥 высокий |
| Контекстные менеджеры и декораторы | `with`, `__enter__`, `__exit__`, wrappers | 🔥 высокий |
| Параллельность | GIL, threading, multiprocessing, asyncio | 🔥 высокий |
| Структуры данных и сложности | dynamic array, linked list, set operations, Big O | 🟡 средний |

---

## 1. Python в ML и разработке

- [ ] Почему Python популярен в ML и backend-разработке?
- [ ] Какие главные недостатки Python по сравнению с компилируемыми языками?
- [ ] Почему Python часто используют как “glue language” вокруг C/C++/CUDA-библиотек?
- [ ] Почему NumPy/PyTorch работают быстро, хотя Python сам по себе медленный?
- [ ] В каких случаях Python может стать узким местом в production-сервисе?

---

## 2. Объектная модель, mutability и ссылки

- [ ] Mutable vs immutable типы данных: какие типы относятся к каждой группе и почему это важно?
- [ ] Что происходит при переназначении immutable-объекта?
- [ ] Является ли `a = 3; a = 4` мутацией объекта?
- [ ] Что будет, если внутри `tuple` лежит `list`?
- [ ] Чем отличаются `==` и `is`?
- [ ] Что нужно реализовать в классе, чтобы два разных объекта были равны через `==`?
- [ ] Чем отличается поверхностное копирование от глубокого копирования?
- [ ] Почему изменяемые объекты опасно использовать как default-аргументы функции?
- [ ] Что такое interning строк и маленьких integer-объектов и почему на это нельзя опираться в логике?

---

## 3. `dict`, `set` и хэш-таблицы

- [ ] Что такое `dict` как структура данных?
- [ ] Какая средняя асимптотика доступа, вставки, удаления и проверки наличия в `dict`?
- [ ] Почему у `dict` может быть худший случай `O(n)`?
- [ ] Что такое коллизии в хэш-таблице?
- [ ] Какие методы борьбы с коллизиями существуют?
- [ ] Каким должен быть ключ в `dict`?
- [ ] Какая связь между `__hash__` и `__eq__`?
- [ ] Почему изменяемый объект обычно нельзя безопасно использовать как ключ словаря?
- [ ] Как устроен `set` и почему его операции похожи на операции `dict`?
- [ ] Какая асимптотика у операций `set`: `in`, `add`, `remove`, union, intersection, difference?
- [ ] Почему порядок ключей в современном `dict` сохраняется, но словарь все равно не стоит использовать как отсортированную структуру?

---

## 4. ООП в Python

- [ ] Что такое `self` в методах класса?
- [ ] Как вызвать метод родительского класса?
- [ ] Как работает `super()` и почему он особенно важен при множественном наследовании?
- [ ] Что такое инкапсуляция в Python?
- [ ] Как работают `_name`, `__name` и `@property` для инкапсуляции?
- [ ] Чем отличаются instance method, `@staticmethod` и `@classmethod`?
- [ ] Что такое MRO и всегда ли Python может построить порядок поиска метода?
- [ ] Когда композиция лучше наследования?
- [ ] Что делают методы `__repr__`, `__str__`, `__len__`, `__iter__` и зачем они нужны?

---

## 5. Память и сборка мусора

- [ ] Как устроено управление памятью в Python на уровне идей?
- [ ] Как Python понимает, что объект можно удалить из памяти?
- [ ] Что такое reference counting?
- [ ] Что такое циклические ссылки и почему одного reference counting недостаточно?
- [ ] Как работает garbage collector / generational GC?
- [ ] Что делает оператор `del` на самом деле?
- [ ] Почему объект может не удалиться сразу после `del name`?
- [ ] Что такое `__del__` и почему с ним нужно быть осторожным?
- [ ] Как искать утечки памяти в Python-приложении на практике?

---

## 6. Контекстные менеджеры

- [ ] Что такое контекстный менеджер и зачем нужен `with`?
- [ ] Как написать свой контекстный менеджер через `__enter__` и `__exit__`?
- [ ] Как написать контекстный менеджер через `contextlib.contextmanager`?
- [ ] Что происходит с исключением внутри блока `with`?
- [ ] Почему файлы, соединения с БД и locks удобно оформлять через context manager?

---

## 7. Декораторы

- [ ] Что такое декоратор?
- [ ] Как написать свой декоратор?
- [ ] Зачем использовать `functools.wraps`?
- [ ] Как написать декоратор с параметрами?
- [ ] Как сделать декоратор для кэширования?
- [ ] Как сделать декоратор для retry?
- [ ] Как сделать декоратор для логирования времени выполнения функции?
- [ ] В чем разница между декоратором функции и декоратором метода класса?

---

## 8. Логирование

- [ ] Какие уровни логирования есть в Python?
- [ ] Чем `print` хуже нормального логирования в production-коде?
- [ ] Что такое logger, handler, formatter?
- [ ] Как не допустить утечки персональных данных в логах?
- [ ] Как логировать исключения вместе со stack trace?

---

## 9. Многопоточность, multiprocessing и async

- [ ] Что такое GIL?
- [ ] Какие ограничения накладывает GIL на CPU-bound код?
- [ ] Почему `threading` подходит для I/O-bound задач?
- [ ] Когда использовать `multiprocessing`?
- [ ] Чем процесс отличается от потока с точки зрения памяти?
- [ ] Как писать параллельный код в Python при наличии GIL?
- [ ] Почему GPU-вычисления обычно не ускоряются Python-потоками напрямую?
- [ ] Что такое `asyncio`, coroutine, event loop?
- [ ] Чем `asyncio` отличается от `threading`?
- [ ] Когда `async/await` не даст ускорения?
- [ ] Какие типичные ошибки бывают при смешивании sync-кода и async-кода?

---

## 10. Структуры данных и сложности

- [ ] Как устроен `list` в Python как dynamic array?
- [ ] Что такое `capacity` у dynamic array?
- [ ] Почему `append` в `list` имеет амортизированную сложность `O(1)`?
- [ ] Почему вставка в начало `list` стоит `O(n)`?
- [ ] Чем linked list отличается от array list?
- [ ] Почему в Python чаще используют `list` и `deque`, а не linked list?
- [ ] Что такое `deque` и когда он лучше `list`?
- [ ] Какая сложность доступа, вставки и удаления в linked list?
- [ ] Что такое амортизированная сложность?
- [ ] Чем average-case, worst-case и amortized complexity отличаются друг от друга?
- [ ] Какие сложности у `sort()` и почему Python использует Timsort?

---

## 11. Алгоритмы и сложности

- [ ] Как объяснить `O(1)`, `O(log n)`, `O(n)`, `O(n log n)`, `O(n^2)` на примерах?
- [ ] Как оценить сложность вложенных циклов?
- [ ] Почему бинарный поиск требует отсортированных данных?
- [ ] Когда hash-based решение лучше sort-based решения?
- [ ] Когда сортировка за `O(n log n)` может быть лучше хэш-таблицы за `O(n)`?
- [ ] Как оценивать сложность рекурсивного алгоритма?
- [ ] Что такое trade-off между временем и памятью?

---

## ✅ Следующий шаг

> [!success] Как превращать в ответы
> Для каждого вопроса дальше можно добавить ссылку на существующую карточку или создать короткий ответ прямо в отдельной карточке по теме: `dict`, GC, GIL, decorators, context managers, `asyncio`.
