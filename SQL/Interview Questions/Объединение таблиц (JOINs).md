---
topic: Объединение таблиц (JOINs)
card_ref: "[[Объединение таблиц (JOINs)]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [INNER JOIN, условие ON, соответствие в обеих таблицах, отсутствие строк без совпадения]
optional_concepts: [отличие от OUTER JOIN]
text: Что возвращает INNER JOIN и что происходит со строками, для которых нет соответствия в одной из таблиц?
follow_up:
  - Представь, что в левой таблице 1 000 000 строк, а в правой — 10. При этом только 3 строки из правой имеют совпадения. Сколько строк максимально может вернуть INNER JOIN, если ключи в левой таблице не уникальны?

## Q002
type: compare
difficulty: junior
key_concepts: [LEFT JOIN сохраняет все строки левой таблицы, INNER JOIN теряет несовпадающие строки, NULL в правых колонках, выбор типа JOIN под задачу]
optional_concepts: [RIGHT JOIN как зеркало LEFT JOIN]
text: Чем LEFT JOIN отличается от INNER JOIN? Приведи пример бизнес-задачи, где важно выбрать именно LEFT JOIN, а не INNER JOIN.
follow_up:
  - Ты сделал LEFT JOIN таблицы пользователей с таблицей покупок и считаешь средний чек через AVG(amount). Даст ли это корректный результат для пользователей без покупок? Почему?

## Q003
type: trap
difficulty: middle
key_concepts: [NULL не равен NULL, строка с NULL не соединяется, LEFT JOIN оставляет строку с NULL из левой таблицы, поведение INNER JOIN с NULL]
optional_concepts: [IS NULL / IS NOT NULL для фильтрации, COALESCE как обходной путь]
text: В таблице заказов поле `customer_id` иногда содержит NULL (заказ оформлен анонимно). Ты делаешь INNER JOIN этой таблицы с таблицей клиентов по полю `customer_id`. Какие строки с NULL попадут в результат и почему?
follow_up:
  - Тебе всё же нужно включить анонимные заказы в итоговую выборку. Какой тип JOIN и какую дополнительную логику ты используешь? Как это повлияет на агрегаты, если дальше считать сумму заказов по клиентам?

## Q004
type: scenario
difficulty: middle
key_concepts: [дубликаты ключей увеличивают число строк, связь многие-ко-многим, искажение агрегатов, проверка уникальности ключа перед JOIN]
optional_concepts: [DISTINCT / GROUP BY для дедупликации перед JOIN, COUNT(*) как диагностика]
text: Аналитик посчитал суммарную выручку по регионам: сделал LEFT JOIN таблицы продаж (1M строк) с таблицей справочника регионов и применил SUM(revenue). Итоговая сумма оказалась в 3 раза больше, чем в исходной таблице продаж. Что, скорее всего, произошло и как это диагностировать?
follow_up:
  - Как ты изменишь запрос или пайплайн, чтобы гарантировать корректный результат даже при будущем появлении дублей в справочнике?

## Q005
type: chain
difficulty: middle
key_concepts: [SELF JOIN, иерархические структуры, псевдонимы таблицы, рекурсивные отношения]
optional_concepts: [рекурсивный CTE как альтернатива для глубоких иерархий]
text: В таблице `employees` есть колонки `employee_id` и `manager_id`, где `manager_id` ссылается на `employee_id` той же таблицы. Как получить список пар «сотрудник — имя его руководителя»?
follow_up:
  - Теперь иерархия стала многоуровневой (5+ уровней). Что произойдёт с подходом SELF JOIN при росте глубины, и какой инструмент SQL лучше подходит для произвольной глубины?
  - Сотрудник на верхнем уровне иерархии имеет `manager_id = NULL`. Какой тип JOIN нужно выбрать, чтобы он всё равно попал в результат?

## Q006
type: tradeoff
difficulty: middle
key_concepts: [CROSS JOIN как декартово произведение, экспоненциальный рост числа строк, целенаправленное использование CROSS JOIN, производительность]
optional_concepts: [CROSS JOIN для генерации тестовых данных или сетки значений]
text: Тебе нужно для каждого из 500 клиентов сгенерировать строку на каждый из 365 дней года — чтобы заполнить пропуски в временных рядах. Коллега предлагает использовать CROSS JOIN таблицы клиентов с таблицей дат. Другой коллега говорит, что это опасно и лучше сделать LEFT JOIN с уже имеющимися данными. Какой подход выбрать и почему?
follow_up:
  - Если клиентов станет 5 000 000, а период — 10 лет, как изменится твой выбор и что ты сделаешь, чтобы операция не положила кластер?

## Q007
type: debug
difficulty: senior
key_concepts: [дубликаты в ключах обоих таблиц, декартово произведение по ключу при many-to-many, утечка данных через JOIN, диагностика через COUNT до и после JOIN]
optional_concepts: [window-функции для дедупликации вместо GROUP BY, explain/query plan]
text: Дата-инженер написал пайплайн: берёт таблицу транзакций (transaction_id, user_id, amount) и таблицу сессий (session_id, user_id, page) и соединяет их по `user_id` через INNER JOIN. Далее модель обучается предсказывать `amount` по фичам из таблицы сессий. На тесте метрика почти идеальная, но в продакшене модель работает плохо. Найди ошибку в пайплайне и объясни, почему она приводит к утечке данных.
follow_up:
  - Как изменится ситуация, если один `user_id` имеет 1000 транзакций и 1000 сессий? Посчитай размер итоговой таблицы и объясни, как это влияет на обучение.
  - Предложи правильную схему объединения этих данных без утечки, учитывая, что сессии и транзакции нужно сопоставлять по времени (транзакция должна относиться к ближайшей предшествующей сессии).

## Q008
type: system_design
difficulty: senior
key_concepts: [стоимость JOIN на больших таблицах, broadcast join vs sort-merge join, партиционирование по ключу JOIN, выбор стратегии в зависимости от размера таблиц]
optional_concepts: [skew в ключах как причина спилла/OOM, bucket join в Spark/Hive]
text: У тебя есть два датасета в распределённой системе (например, Spark): таблица событий — 10 TB, партиционирована по дате; таблица справочника пользователей — 500 MB. Тебе нужно обогатить события атрибутами пользователей через JOIN по `user_id`. Опиши, как ты спроектируешь этот JOIN, чтобы он выполнялся эффективно, и какие риски возникают при типичных «перекосах» в данных.
follow_up:
  - Оказалось, что 1% user_id отвечает за 40% всех событий (hot keys). Как это изменит стратегию JOIN и что конкретно сломается без дополнительных мер?
  - Справочник пользователей обновляется каждые 30 минут. Как ты организуешь инкрементальное обновление обогащённой таблицы событий без полного пересчёта 10 TB?
