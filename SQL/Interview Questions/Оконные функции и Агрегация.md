---
topic: Оконные функции и Агрегация
card_ref: "[[Оконные функции и Агрегация]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [оконная функция не схлопывает строки, конструкция OVER(), PARTITION BY определяет границы окна]
optional_concepts: [порядок выполнения SQL]
text: Что такое оконная функция в SQL и в чём ключевое отличие от обычной агрегации через GROUP BY с точки зрения количества строк в результате?
follow_up:
  - Если оконная функция вычисляется позже, чем WHERE, можно ли в WHERE напрямую использовать псевдоним (alias), заданный для результата оконной функции в SELECT? Почему?

---

## Q002
type: compare
difficulty: junior
key_concepts: [ROW_NUMBER всегда уникален, RANK пропускает номера после дублей, DENSE_RANK не пропускает номера]
optional_concepts: [конкретный пример с числовым результатом]
text: В таблице продаж двое сотрудников продали одинаковое количество товаров — 100 единиц. Объясните, какой ранг получит каждый из них при использовании ROW_NUMBER(), RANK() и DENSE_RANK(). В каком бизнес-сценарии каждая из функций будет уместнее?
follow_up:
  - Теперь представьте, что на первом месте сразу три сотрудника с одинаковым результатом. Какой ранг получит четвёртый сотрудник при RANK() и DENSE_RANK() соответственно?

---

## Q003
type: trap
difficulty: middle
key_concepts: [оконные функции вычисляются после WHERE, фильтрация через CTE или подзапрос, нельзя использовать OVER() в WHERE]
optional_concepts: [HAVING не решает проблему с оконными функциями]
text: Коллега написал запрос, чтобы получить самую дорогую покупку для каждого клиента: `SELECT customer_id, amount, ROW_NUMBER() OVER(PARTITION BY customer_id ORDER BY amount DESC) as rn FROM purchases WHERE rn = 1`. Запрос падает с ошибкой. В чём проблема и как её корректно исправить?
follow_up:
  - После исправления через CTE коллега предлагает использовать HAVING вместо подзапроса. Сработает ли это? Почему HAVING не является эквивалентом в данном случае?

---

## Q004
type: scenario
difficulty: middle
key_concepts: [ROW_NUMBER с PARTITION BY, CTE для фильтрации по ранжированию, ORDER BY внутри OVER]
optional_concepts: [RANK vs ROW_NUMBER при одинаковых значениях целевого поля]
text: Аналитик просит: «Выведи для каждого клиента только его самую последнюю по дате покупку. Если дата у нескольких покупок совпадает — верни любую одну строку». Опишите логику построения запроса шаг за шагом.
follow_up:
  - Задача изменилась: нужно выводить не одну, а три последние покупки каждого клиента. При этом клиенты, у которых покупок меньше трёх, тоже должны попасть в выборку. Что изменится в запросе?

---

## Q005
type: chain
difficulty: middle
key_concepts: [порядок выполнения SQL: FROM → WHERE → GROUP BY → HAVING → SELECT → оконные функции → ORDER BY, оконные функции видят уже отфильтрованный набор данных]
optional_concepts: [DISTINCT выполняется после оконных функций]
text: В каком порядке SQL-движок выполняет следующие части запроса: JOIN, WHERE, GROUP BY, HAVING, оконные функции, ORDER BY? Почему знание этого порядка критично при написании запросов с оконными функциями?
follow_up:
  - Если до выполнения оконной функции через WHERE отфильтровать часть строк — изменится ли результат оконного вычисления по сравнению с запросом без фильтра? Приведите конкретный пример, где это приводит к неожиданному результату.

---

## Q006
type: compare
difficulty: middle
key_concepts: [GROUP BY схлопывает до одной строки на группу, SUM() OVER(PARTITION BY) сохраняет все строки, нарастающий итог требует ORDER BY внутри OVER]
optional_concepts: [ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW для накопительного итога]
text: Нужно для каждого сотрудника вывести его зарплату и общую сумму зарплат по его отделу в одной строке. Почему это нельзя реализовать через GROUP BY в том же виде? Как решить задачу через оконную функцию и в чём принципиальная разница подходов?
follow_up:
  - Задача усложнилась: нужен не суммарный итог по отделу, а нарастающий итог зарплат внутри отдела, отсортированных по дате найма. Что нужно изменить в конструкции OVER() и как это повлияет на результат?

---

## Q007
type: chain
difficulty: senior
key_concepts: [ROWS BETWEEN vs RANGE BETWEEN, недетерминированное поведение при дублях в ORDER BY, спецификация фрейма окна, UNBOUNDED PRECEDING / CURRENT ROW]
optional_concepts: [FOLLOWING в спецификации фрейма, влияние выбора фрейма на производительность]
text: Вы пишете запрос с нарастающим итогом: `SUM(amount) OVER(PARTITION BY customer_id ORDER BY order_date)`. Коллега утверждает, что если несколько заказов имеют одинаковую `order_date`, результат будет непредсказуемым. Прав ли он? Объясните почему, и расскажите, чем отличается поведение `ROWS BETWEEN` от `RANGE BETWEEN` в этом контексте.
follow_up:
  - Если явно добавить `ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` — гарантирует ли это детерминированный результат при дублях в ORDER BY? Что нужно сделать, чтобы результат был стабильным при любом порядке исполнения?

---

## Q008
type: scenario
difficulty: senior
key_concepts: [утечка данных через оконную функцию на полном датасете, порядок операций: split до feature engineering, оконное окно включает будущие наблюдения]
optional_concepts: [time-based split, групповая утечка при стандартной кросс-валидации на временных данных]
text: Data Scientist строит модель прогноза оттока клиентов. В рамках feature engineering он вычисляет скользящее среднее суммы покупок за последние 30 дней через оконную функцию SQL — на всём датасете сразу, а затем делит результат на train/test. Модель показывает отличные метрики на тесте, но в продакшене работает значительно хуже. В чём ошибка и как её исправить?
follow_up:
  - Предположим, вы перенесли вычисление признаков внутрь каждого fold'а при кросс-валидации. Достаточно ли этого, если в данных есть временная зависимость между строками одного клиента? Какой тип сплита следует применять и почему стандартный KFold здесь опасен?
