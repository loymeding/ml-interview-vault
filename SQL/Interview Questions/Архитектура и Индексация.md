---
topic: Архитектура и Индексация
card_ref: "[[Архитектура и Индексация]]"
generated: 2026-04-29
---

## Q001
type: fact
difficulty: junior
key_concepts: [B-Tree, логарифмическая сложность поиска, указатель на строку]
optional_concepts: [Hash-индекс и его ограничения]
text: Что такое индекс в реляционной БД и почему поиск по нему работает за O(log n), а не за O(n)?
follow_up:
  - В каком случае вместо B-Tree индекса стоит использовать Hash-индекс, и какова его сложность поиска?

---

## Q002
type: compare
difficulty: junior
key_concepts: [ACID vs BASE, вертикальное vs горизонтальное масштабирование, гибкость схемы]
optional_concepts: [eventual consistency]
text: Назовите два принципиальных отличия SQL от NoSQL баз данных. В каком сценарии вы бы выбрали MongoDB вместо PostgreSQL?
follow_up:
  - Вы выбрали MongoDB ради гибкости схемы, но через полгода продукт вырос и появилась потребность в сложных JOIN-ах. Как будете действовать?

---

## Q003
type: trap
difficulty: middle
key_concepts: [Full Table Scan, LIKE с ведущим wildcard, функции над индексированной колонкой]
optional_concepts: [функциональный индекс]
text: Коллега создал индекс на колонку `email` и уверен, что все запросы к ней теперь быстрые. Какой запрос всё равно приведёт к полному сканированию таблицы — и почему оптимизатор проигнорирует индекс?
follow_up:
  - Как можно переписать запрос `WHERE LOWER(email) = 'user@example.com'`, чтобы индекс всё же использовался, не меняя логику фильтрации?

---

## Q004
type: chain
difficulty: middle
key_concepts: [составной индекс, порядок колонок, принцип левого префикса]
optional_concepts: [selectivity колонки, кардинальность]
text: У вас есть составной индекс `(country, city, age)`. Для каких из следующих запросов этот индекс сработает, а для каких нет: (а) `WHERE country = 'RU'`, (б) `WHERE city = 'Moscow'`, (в) `WHERE country = 'RU' AND age = 25`?
follow_up:
  - Если нужно оптимизировать запросы как по `(country, city)`, так и по `(city, country)`, достаточно ли одного составного индекса? Что вы предложите?

---

## Q005
type: scenario
difficulty: middle
key_concepts: [горизонтальное масштабирование, отсутствие жёсткой схемы, BASE и eventual consistency]
optional_concepts: [денормализация данных в NoSQL]
text: Вы строите ML-пайплайн для рекомендательной системы. Фичи для каждого пользователя генерируются разными моделями и имеют разный набор полей. Объём данных — 500 млн пользователей, нагрузка растёт. Аналитик предлагает хранить всё в PostgreSQL. Какие аргументы вы приведёте за и против, и какую БД выберете для feature store?
follow_up:
  - Вы выбрали NoSQL-хранилище. Одна из моделей начала писать фичи с задержкой из-за eventual consistency, и модель-потребитель читает устаревшие данные. Как детектировать и смягчить эту проблему?

---

## Q006
type: tradeoff
difficulty: middle
key_concepts: [накладные расходы на запись при наличии индексов, скорость чтения vs скорость записи, OLAP vs OLTP]
optional_concepts: [частичный индекс, индекс на подмножестве строк]
text: Дата-инженер предлагает проиндексировать 10 колонок в таблице событий, чтобы аналитики могли строить произвольные запросы быстро. Таблица пополняется со скоростью 50 000 INSERT в секунду. Как вы оцените этот компромисс и что порекомендуете?
follow_up:
  - Предложите альтернативную архитектуру, которая решает задачу аналитики без деградации записи. Какие её слабые стороны?

---

## Q007
type: system_design
difficulty: senior
key_concepts: [горизонтальное шардирование, партиционирование таблиц, выбор ключа партиции/шарда]
optional_concepts: [hot spot проблема, consistent hashing]
text: Вам нужно спроектировать хранилище логов предсказаний ML-модели: 1 млрд записей в день, запросы — «дай все предсказания для user_id за последние 7 дней» и «дай агрегаты по модели за месяц». Как выбрать БД и ключ партиционирования, чтобы оба типа запросов были эффективны?
follow_up:
  - Один из user_id генерирует 30% всего трафика (hot user). Как ваша схема партиционирования справится с этим, и что нужно изменить?
  - Через год объём логов вырос в 10 раз и нужно пересмотреть схему шардирования. Как провести миграцию без даунтайма?

---

## Q008
type: trap
difficulty: senior
key_concepts: [ACID транзакции, изоляция транзакций, аномалии чтения (dirty read, phantom read)]
optional_concepts: [уровни изоляции READ COMMITTED vs SERIALIZABLE, MVCC]
text: Команда решила перейти на уровень изоляции `READ UNCOMMITTED` в PostgreSQL, чтобы ускорить аналитические запросы к таблице фичей. Объясните, какие конкретные аномалии данных могут возникнуть в ML-пайплайне из-за этого решения и чем это опасно для качества модели.
follow_up:
  - Как бы вы обеспечили высокую скорость аналитического чтения, не понижая уровень изоляции и не создавая грязных чтений? Назовите два подхода.
